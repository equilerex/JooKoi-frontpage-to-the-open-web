// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/agent-sandboxing.md
export const HTML = "<h1 id=\"stage-1-agent-code-execution-sandboxing\">Stage 1 — Agent code-execution sandboxing</h1>\n<p>AI coding tools can misbehave — delete the wrong file, read something they shouldn&#39;t, send data somewhere it shouldn&#39;t go. A sandbox restricts what the tool&#39;s process can actually do at the OS level, so a bad action stays contained instead of reaching the rest of the machine. Relevant if you use an AI coding assistant that runs commands on your machine; skip it if you only chat with an AI in a browser.</p>\n<p>Topic: whether &quot;sandboxing&quot; is becoming a real named security standard for AI coding agents, beyond the generic advice already in <a href=\"/library/ai-tooling-crash-course-for-developers/topics/security-and-supply-chain\"><code>security-and-supply-chain.md</code></a> (&quot;run unfamiliar tooling in an isolated context&quot;). Options only, no pick made.</p>\n<h2 id=\"what-is-an-agent-sandbox\">What is an agent sandbox?</h2>\n<p>A coding agent that can read files, run shell commands, and make network requests can, by design or by mistake, do real damage: delete the wrong directory, read an SSH key it had no reason to touch, or send data somewhere it shouldn&#39;t. A sandbox is an enforcement layer, sitting below the agent and outside its control, that restricts what a process is actually allowed to do at the OS level — regardless of what the model decides to try.</p>\n<p><strong>What&#39;s being restricted</strong>, typically some combination of:</p>\n<ul>\n<li><strong>Filesystem access</strong> — write access limited to the working directory (or an explicit allow-list), with certain paths (<code>~/.ssh</code>, shell rc files, credential stores) denied outright even if they&#39;re technically inside the project tree.</li>\n<li><strong>Network access</strong> — outbound connections blocked by default, or routed through a proxy that only permits an allow-listed set of domains.</li>\n<li><strong>Process/syscall access</strong> — the set of system calls a process can make is filtered, so even a compromised or misbehaving process can&#39;t do arbitrary things to the host kernel.</li>\n</ul>\n<p><strong>The risk this contains</strong>: a model that misinterprets an instruction, gets prompt-injected by something it read (a malicious dependency, a scraped web page, a poisoned instruction file — see <a href=\"/library/ai-tooling-crash-course-for-developers/topics/base-instruction-files\"><code>base-instruction-files.md</code></a> §7 for a concrete example), or just makes a bad tool call. Sandboxing doesn&#39;t require trusting the model to behave — it makes bad behavior structurally harder regardless of why it happened.</p>\n<p><strong>What can stop working inside a sandbox</strong>: anything the allow-list doesn&#39;t cover. A build step that reaches an unlisted package registry, a script that needs to read a credential file outside the project directory, or a tool that needs a network domain nobody thought to allow — all fail, often with a permission error rather than an obvious explanation. Loosening a sandbox to fix this reintroduces exactly the risk it existed to contain, so the practical work of using one is mostly tuning the allow-list, not fighting the concept.</p>\n<p><strong>Why a developer would turn one on</strong>: most sandbox implementations cost nothing to enable and cost little day-to-day friction once the allow-list is tuned, in exchange for containing the worst case — an agent&#39;s mistake staying inside a working directory versus reaching the rest of the machine.</p>\n<p><strong>Sandboxed is not the same as safe.</strong> A sandbox limits <em>blast radius</em> — what an agent can reach if something goes wrong. It does not limit what the agent can do <em>within</em> its allowed scope. An agent with write access to the whole working directory and network access to your package registry can still delete the wrong file, commit broken code, or leak a secret living inside the sandboxed path.</p>\n<p>It&#39;s a containment control, not a correctness or trust guarantee. Pair it with the usual practices — review diffs, don&#39;t blanket-trust a cloned repo&#39;s instructions, scope credentials narrowly — rather than treating it as a substitute for them.</p>\n<p>The rest of this document covers the deeper mechanics: what&#39;s actually converging across vendors, and how specific tools (Claude Code&#39;s <code>/sandbox</code> among them, as one example of several) implement this.</p>\n<h2 id=\"1-is-there-an-actual-named-standard\">1. Is there an actual named standard?</h2>\n<p>No single cross-vendor spec for sandboxing itself. 2026&#39;s agentic-AI standards convergence is real, but it&#39;s happening one layer up: <a href=\"https://modelcontextprotocol.io/\">MCP</a> (tool connectivity, governed by Anthropic/community), <a href=\"https://github.com/a2aproject/A2A\">A2A</a> (agent-to-agent, now under the Linux Foundation), <a href=\"https://github.com/webmachinelearning/webmcp\">WebMCP</a> (browser-facing tool exposure, W3C Community Group draft).</p>\n<p>Sandboxing isn&#39;t one of those layers. Each vendor still implements it independently, from a shared <em>pool</em> of isolation tech rather than a shared <em>protocol</em> — convergence on primitives, fragmentation on interface. If you&#39;ve seen &quot;an emerging security standard&quot; claimed somewhere, it&#39;s more accurately &quot;a converging set of practices around a shared toolkit&quot; than a ratified spec.</p>\n<h2 id=\"2-the-shared-toolkit-whats-actually-converging\">2. The shared toolkit (what&#39;s actually converging)</h2>\n<p>Three isolation primitives, used in different combinations:</p>\n<ul>\n<li><strong>OS-level syscall filtering</strong> — macOS Seatbelt (Apple&#39;s App Store sandbox framework, reused, invoked via <code>sandbox-exec</code>), Linux <a href=\"https://github.com/containers/bubblewrap\"><code>bubblewrap</code></a>/<a href=\"https://landlock.io/\">Landlock</a>, Windows AppContainer. Cheap, no VM overhead, but shares the host kernel — a kernel exploit escapes it.</li>\n<li><strong><a href=\"https://gvisor.dev/\">gVisor</a></strong> — a user-space kernel that intercepts syscalls and only allows a vetted subset through to the real kernel. Stronger isolation than bubblewrap, some performance/compatibility cost. Anthropic uses this for Claude web&#39;s sandboxed code execution.</li>\n<li><strong>MicroVMs (<a href=\"https://firecracker-microvm.github.io/\">Firecracker</a>)</strong> — hardware-virtualized, full kernel-boundary isolation, ~100-125ms cold start, snapshot/pause/resume in 5-30ms. Strongest guarantee, used by E2B and others built for concurrent multi-tenant execution.</li>\n</ul>\n<p>Named vendor choices, each confirmed against current docs/blog sources (not aggregator-only):</p>\n<ul>\n<li><strong>Claude Code</strong>: opt-in via <code>/sandbox</code> command (not default), documented in <a href=\"https://code.claude.com/docs/en/sandboxing\">Claude Code&#39;s sandboxing docs</a>. macOS uses Seatbelt natively; Linux/WSL2 needs <code>bubblewrap</code> + <code>socat</code> installed; WSL1 unsupported. Two isolation layers: filesystem (write access restricted to working directory, explicit deny-list for things like <code>~/.bashrc</code>, SSH keys; reads are broader), and network (proxied, allow-list of domains, prompts on new ones). Two run modes: auto-allow within those boundaries, or manual-approval-per-command. <code>/sandbox</code> is one example implementation among the vendor approaches below, not the definition of what a sandbox is.</li>\n<li><strong>Claude web / Anthropic&#39;s hosted code-execution tool</strong>: gVisor-based, MCP-integrated Python execution — chosen for gVisor&#39;s better fit at high concurrent-sandbox counts, not because it&#39;s inherently better than Firecracker for a single user.</li>\n<li><strong>OpenAI Codex CLI</strong>: macOS Seatbelt, Linux Landlock (default), Windows AppContainer — same category of OS-level primitive as Claude Code, different implementation per OS.</li>\n<li><strong>Dedicated sandbox-as-a-service platforms</strong> (relevant if building custom agent tooling, not just using an existing coding agent): <a href=\"https://e2b.dev/\">E2B</a> (Firecracker-based, fastest cold start in benchmarks ~717ms, strong SDK ergonomics, best fit for ephemeral single-session tasks), <a href=\"https://modal.com/\">Modal</a> (gVisor-based, GPU-friendly since gVisor doesn&#39;t block hardware passthrough the way some microVM setups can), <a href=\"https://www.daytona.io/\">Daytona</a> (snapshot-based, tuned for persistent dev-workflow use rather than one-shot execution — sources differ on whether its isolation layer is Docker- or gVisor-based; not independently confirmed here <!-- VERIFY: Daytona's underlying isolation primitive (Docker snapshots vs. gVisor) --> ). These are aimed at people building agent products, not typically something a solo dev wires in just to run Claude Code safely — see Section 4.</li>\n</ul>\n<h2 id=\"3-convergence-or-fragmentation\">3. Convergence or fragmentation?</h2>\n<p>Fragmented at the interface level, converging at the primitive level. Every serious vendor has landed on &quot;OS-level enforcement, not prompt-level trust&quot; as the design principle — that part is now closer to settled consensus than contested opinion. But there&#39;s no equivalent of MCP for sandboxes: no shared config format, no portable &quot;sandbox descriptor&quot; a tool from one vendor could hand to another. One relevant real signal: Anthropic&#39;s Claude Code sandbox runtime was released as an open source npm package (<a href=\"https://github.com/anthropic-experimental/sandbox-runtime\"><code>@anthropic-ai/sandbox-runtime</code></a>) usable outside Claude Code itself — a step toward a shared primitive, not yet a standard other vendors have adopted.</p>\n<h2 id=\"4-practical-takeaway-for-a-solo-home-dev-setup\">4. Practical takeaway for a solo home-dev setup</h2>\n<ul>\n<li>Turning on Claude Code&#39;s <code>/sandbox</code> costs nothing and matches the current best-practice consensus (kernel-level enforcement over trusting the model/prompt). It&#39;s a real, low-effort, already-available feature, not vaporware or enterprise-only.</li>\n<li>A named CVE (<a href=\"https://www.cve.org/CVERecord?id=CVE-2026-35022\">CVE-2026-35022</a>, a command-injection report against Claude Code CLI/Agent SDK auth-helper execution) is cited by some 2026 sources as motivating sandboxing adoption — but its CNA rejected the record, on the basis that the behavior is documented and warned-against intended behavior (non-interactive mode should only be used in trusted directories), not an unpatched vulnerability. Treat it as a disputed/rejected CVE, not a confirmed forcing-function.</li>\n<li>The sandbox-as-a-service platforms (E2B/Modal/Daytona/Firecracker-direct) solve a different problem — running <em>many</em> concurrent isolated agent sessions at scale, typically for a product, not a single local dev&#39;s Claude Code sessions. Not worth adopting for a personal setup unless a future project specifically needs to spin up agent-executed code in an ephemeral cloud sandbox (e.g., a tool that lets an agent run untrusted code against real infra). Worth knowing the names exist, not worth setting up now — matches the project&#39;s stated anti-speculative-infrastructure stance.</li>\n</ul>\n<h2 id=\"open-questions-weak-sourcing\">Open questions / weak sourcing</h2>\n<ul>\n<li>The &quot;emerging standard&quot; framing itself is not fully pinned down — no primary spec document found; conclusion above is inferred from the pattern across several 2026 vendor blogs and comparison posts (Modal, Northflank, Qovery, Blaxel), which are vendor-interested parties, not neutral standards bodies. Treat the &quot;convergence on primitives, not protocol&quot; conclusion as a reasonable synthesis, not a verified fact from a primary source.</li>\n<li>CVE-2026-35022&#39;s CNA rejection is confirmed against its <a href=\"https://www.cve.org/CVERecord?id=CVE-2026-35022\">CVE Record</a>; some 2026 vendor content still cites the CVE number as if it were a confirmed vulnerability, which it is not.</li>\n<li>Whether Claude Code&#39;s sandbox runtime npm package has seen real third-party adoption outside Anthropic&#39;s own tools — not checked; flagged as a claim from a single blog post, not a repo-health signal.</li>\n<li>No direct check performed on GitHub Copilot&#39;s sandboxing approach specifically (search results covered Codex and Claude but not Copilot in comparable depth) — gap, not a &quot;Copilot has none&quot; claim.</li>\n</ul>\n";
