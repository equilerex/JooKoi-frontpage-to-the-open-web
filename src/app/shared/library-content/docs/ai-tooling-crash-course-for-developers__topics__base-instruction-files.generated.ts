// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/base-instruction-files.md
export const HTML = "<h1 id=\"stage-1-base-instruction-files-agentsmd-claudemd\">Stage 1 — Base Instruction Files (AGENTS.md / CLAUDE.md)</h1>\n<p>A base instruction file is a short file an AI coding tool reads automatically, before doing any work on a project. It stops the agent guessing your conventions or getting told the same things every session.</p>\n<p>Relevant if you use one of these tools and want it to stop making wrong assumptions. Skip if you&#39;ve never set one up.</p>\n<p>Deep-dive on baseline topic #1, split out from topic #10 per the 2026-08-23 correction in <code>topic-index.md</code>. This doc covers the file itself: what goes in it, root-vs-nested precedence, provider loading mechanics. Cross-project memory/ledgers are a separate question — see <code>memory-and-progress-ledgers.md</code>.</p>\n<p><strong>Framing constraint</strong> (per <code>topic-index.md</code>&#39;s 2026-08-23 correction): <code>AGENTS.md</code> is the primary, provider-agnostic source of truth here — not <code>CLAUDE.md</code>. Provider files are researched as thin pointers <em>to</em> it. No pick is made in this doc, options and evidence only.</p>\n<h2 id=\"what-is-a-base-instruction-file\">What is a base instruction file?</h2>\n<p>A plain Markdown file, checked into a repo (or a user config folder), that a coding agent reads before it starts — no prompt required.</p>\n<p>Two names that matter:</p>\n<ul>\n<li><a href=\"https://agents.md/\"><code>AGENTS.md</code></a> — provider-agnostic, read by 30+ tools.</li>\n<li><code>CLAUDE.md</code> — <a href=\"https://code.claude.com/docs/en/memory\">Claude Code</a>&#39;s own name for the same idea.</li>\n</ul>\n<p>Cursor, Copilot, Gemini CLI, and most other agents have their own equivalents (table below).</p>\n<p><strong>Why it exists.</strong> An agent starting cold on a repo has no idea how you build, test, deploy, or what conventions you follow. It has to guess or ask, every session. This file is a standing answer to &quot;how does this project work,&quot; loaded once per session instead of re-explained every time. Same problem a README solves for humans — different reader, different needs: executable commands over prose, explicit boundaries over onboarding narrative.</p>\n<p><strong>What belongs in it:</strong></p>\n<ul>\n<li>Build/test/lint commands, with real flags — not &quot;run the tests.&quot;</li>\n<li>Code-style conventions the agent should match.</li>\n<li>Where things live in the repo.</li>\n<li>What&#39;s off-limits (files not to touch, patterns not to introduce).</li>\n<li>Project-specific gotchas the agent would otherwise trip over.</li>\n</ul>\n<p><strong>What doesn&#39;t:</strong> general product docs, architecture explainers for new hires, anything a human reads but an agent never acts on. That&#39;s a README or <code>docs/ARCHITECTURE.md</code>&#39;s job.</p>\n<p><strong>Global vs. repo-local.</strong> Most agents support two scopes:</p>\n<ul>\n<li>A repo file (checked into git, shared with everyone who clones it).</li>\n<li>A personal config file (<code>~/.claude/CLAUDE.md</code>, <code>~/.codex/AGENTS.md</code>) — applies across every project, never shared.</li>\n</ul>\n<p>Global holds your personal habits. Repo-local holds what&#39;s true about <em>this</em> codebase regardless of who&#39;s working on it.</p>\n<p><strong>Root vs. nested.</strong> A single root file covers the whole repo. Some agents also support one file per subdirectory (<code>frontend/AGENTS.md</code> alongside <code>backend/AGENTS.md</code>), loaded only when the agent works in that subtree — so a monorepo doesn&#39;t force every session to load rules for parts it isn&#39;t touching. Support varies by tool (§2).</p>\n<p><strong>Provider-agnostic vs. provider-specific.</strong> <code>AGENTS.md</code> works unmodified across any agent that reads it. <code>CLAUDE.md</code> and other vendor-named files are read only by their own tool. Common pattern: write one <code>AGENTS.md</code>, have provider files import or symlink to it — no duplicate instructions to maintain.</p>\n<p><strong>Why bother:</strong> less time re-explaining project context every session, more consistent output (agent matches your conventions instead of generic defaults), and a place to encode hard constraints (&quot;never touch <code>migrations/</code>&quot;) that would otherwise only live in your head.</p>\n<h2 id=\"1-governance-and-the-60000-repositories-claim\">1. Governance, and the &quot;60,000 repositories&quot; claim</h2>\n<p><code>AGENTS.md</code> is a real, governed convention. Released by OpenAI in August 2025, it&#39;s now one of three anchor project contributions to the <strong>Agentic AI Foundation (AAIF)</strong> — alongside <a href=\"https://modelcontextprotocol.io/\">MCP</a> (Anthropic) and <a href=\"https://github.com/block/goose\"><code>goose</code></a> (Block). Announced by the <a href=\"https://www.linuxfoundation.org/press/linux-foundation-announces-the-formation-of-the-agentic-ai-foundation\">Linux Foundation on 2025-12-09</a>, confirmed against <a href=\"https://agents.md/\"><code>agents.md</code></a> itself: &quot;now stewarded by the Agentic AI Foundation under the Linux Foundation.&quot; Founding Platinum members: AWS, Anthropic, Block, Bloomberg, Cloudflare, Google, Microsoft, OpenAI.</p>\n<p>The spec is thin by design. The canonical repo, <a href=\"https://github.com/agentsmd/agents.md\"><code>agentsmd/agents.md</code></a>, states plainly: &quot;AGENTS.md is just standard Markdown. Use any headings you like.&quot; No mandatory fields, no schema.</p>\n<p>The <strong>&quot;60,000+ repositories&quot; adoption figure</strong> (repeated in the Linux Foundation release and on <code>agents.md</code> itself) traces to a single origin: the project&#39;s own self-reporting. No independent third party reproduces the count, and it&#39;s a December 2025 figure still being carried forward as current. Treat it as a marketing data point, not a verified metric.</p>\n<!-- VERIFY: no independent count of AGENTS.md adoption found; figure is self-reported by the AAIF/agents.md project --><p>A different, unrelated number — 60.03% file-level agent-adoption among newly created GitHub projects, from <a href=\"https://arxiv.org/abs/2606.07448\">arXiv:2606.07448</a> — measures something else entirely. Don&#39;t conflate the two.</p>\n<h2 id=\"2-per-provider-loading-mechanics\">2. Per-provider loading mechanics</h2>\n<p>Confirmed against official docs where available. &quot;Not documented&quot; means the vendor&#39;s own docs don&#39;t state it — not that the answer is unknown.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Reads AGENTS.md natively?</th>\n<th>Nested discovery</th>\n<th>Documented precedence</th>\n<th>Global/user file</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Claude Code</td>\n<td>No — reads <code>CLAUDE.md</code>; bridged via <code>@AGENTS.md</code> import or symlink</td>\n<td>Yes, ancestors at launch + subdirs lazily</td>\n<td>Concatenated, root→cwd</td>\n<td><code>~/.claude/CLAUDE.md</code></td>\n</tr>\n<tr>\n<td>Cursor</td>\n<td>Yes, root + nested</td>\n<td>Yes, nested overrides parent on conflict</td>\n<td>Team → Project → User Rules, merged</td>\n<td>UI-based User Rules</td>\n</tr>\n<tr>\n<td>GitHub Copilot (IDE/coding agent)</td>\n<td>Yes, incl. nested</td>\n<td>Yes</td>\n<td>Not documented</td>\n<td>Web/CLI/JetBrains only</td>\n</tr>\n<tr>\n<td>Copilot CLI</td>\n<td>Yes</td>\n<td>Yes</td>\n<td>Explicitly none — &quot;does not define a general precedence order&quot;</td>\n<td><code>$HOME/.copilot/</code></td>\n</tr>\n<tr>\n<td>Gemini CLI</td>\n<td>No, needs <code>context.fileName</code> config</td>\n<td>Yes</td>\n<td>Concatenated</td>\n<td><code>~/.gemini/GEMINI.md</code></td>\n</tr>\n<tr>\n<td>OpenAI Codex CLI</td>\n<td>Yes (originator)</td>\n<td>Yes, root→cwd, one file per dir</td>\n<td>Nearer file overrides — concatenated in order</td>\n<td><code>~/.codex/AGENTS.md</code>, <code>AGENTS.override.md</code> checked first</td>\n</tr>\n<tr>\n<td>opencode</td>\n<td>Yes, preferred over CLAUDE.md</td>\n<td>Yes, walks up from cwd</td>\n<td>Nearest AGENTS.md → global → CLAUDE.md fallback</td>\n<td><code>~/.config/opencode/AGENTS.md</code></td>\n</tr>\n<tr>\n<td>Zed</td>\n<td>Yes, but ranked below 5 other filenames in a 9-entry list</td>\n<td>No — top-level of worktree only</td>\n<td>First match in ordered list wins</td>\n<td>Rules Library (app-level)</td>\n</tr>\n<tr>\n<td>Amp</td>\n<td>Yes</td>\n<td>Yes — most thorough: ancestors always, subtree on file access</td>\n<td>Per-directory fallback chain</td>\n<td><code>$HOME/.config/amp/AGENTS.md</code></td>\n</tr>\n<tr>\n<td>Windsurf/Cascade (Devin Desktop)</td>\n<td>Yes</td>\n<td>Yes, auto-scoped per directory</td>\n<td>Not documented</td>\n<td>Not documented</td>\n</tr>\n<tr>\n<td>Cline</td>\n<td>Yes (merged via a long-stalled PR)</td>\n<td>Workspace + global combined</td>\n<td>Workspace wins on conflict</td>\n<td><code>~/Documents/Cline/Rules</code></td>\n</tr>\n<tr>\n<td>Aider</td>\n<td>No — no auto-discovery of any kind</td>\n<td>n/a</td>\n<td>n/a</td>\n<td>Explicit <code>--read</code>/config only</td>\n</tr>\n<tr>\n<td>Google Jules</td>\n<td>Yes</td>\n<td>Root only</td>\n<td>n/a</td>\n<td>Not documented</td>\n</tr>\n</tbody></table>\n<p>Two flags:</p>\n<ul>\n<li><code>agents.md</code>&#39;s own &quot;supported tools&quot; list includes <a href=\"https://aider.chat/docs/usage/conventions.html\">Aider</a>, whose own docs contradict it. Treat that list as a self-claim, not a compatibility matrix.</li>\n<li>Precedence is mostly unpublished — only Codex, opencode, Zed, Amp, and Cursor state a resolution order. Copilot CLI explicitly disclaims having one.</li>\n</ul>\n<p>Provider docs checked: <a href=\"https://code.claude.com/docs/en/memory\">Claude Code memory</a>, <a href=\"https://cursor.com/docs/context/rules\">Cursor rules</a>, <a href=\"https://docs.github.com/copilot\">GitHub Copilot docs</a>, <a href=\"https://github.com/google-gemini/gemini-cli\">Gemini CLI</a>, <a href=\"https://opencode.ai/docs/rules/\">opencode rules</a>, <a href=\"https://zed.dev/docs/ai/rules\">Zed AI rules</a>, <a href=\"https://ampcode.com/manual\">Amp manual</a>, <a href=\"https://docs.devin.ai/desktop/cascade/agents-md\">Devin/Cascade AGENTS.md docs</a>, <a href=\"https://docs.cline.bot/customization/cline-rules\">Cline rules</a>, <a href=\"https://aider.chat/docs/usage/conventions.html\">Aider conventions</a>, <a href=\"https://jules.google/docs\">Google Jules docs</a>.</p>\n<h2 id=\"3-what-actually-belongs-in-the-file-official-guidance-vs-the-empirical-record\">3. What actually belongs in the file — official guidance vs. the empirical record</h2>\n<p><a href=\"https://code.claude.com/docs/en/memory\">Anthropic&#39;s own docs</a> give a concrete target: &quot;under 200 lines per CLAUDE.md file... longer files consume more context and reduce adherence.&quot; No measurement is published behind that guidance.</p>\n<p><strong>The only controlled tests of this specific claim contradict it.</strong> Three independent studies, checked directly:</p>\n<ul>\n<li><strong>ETH Zurich SRI Lab</strong> (<a href=\"https://arxiv.org/abs/2602.11988\">arXiv:2602.11988</a>, Gloaguen/Mündler/Müller/Raychev/Vechev) — SWE-bench Lite + a 138-issue benchmark, 4 agents including Claude Code and Codex. Context files did not generally improve task success (−0.5% to −2% for LLM-generated files, +4% for developer-written ones) and increased inference cost 19–23% regardless. Repository overviews specifically — &quot;despite being popular and recommended&quot; — proved unhelpful.</li>\n<li><strong>Damon McMillan</strong> (<a href=\"https://arxiv.org/abs/2605.10039\">arXiv:2605.10039</a>, solo author, weaker provenance) — factorial study across 1,650 real Claude Code sessions, varying file length 25–500 lines, rule position, structure. No detectable contrast on compliance after correction for multiple testing. The one real effect: <strong>within-session decay</strong> — roughly 5.6% lower compliance odds per additional function generated, regardless of file design.</li>\n<li><strong>Prakhar Khatri</strong> (<a href=\"https://arxiv.org/abs/2607.27250\">arXiv:2607.27250</a>, independent researcher, weakest provenance) — 288 runs, no significant effect on correctness from having a context file at all. Near-miss failures traced to implementation skill, not missing repo knowledge.</li>\n</ul>\n<p><strong>Honest synthesis:</strong> having a file at all matters — compliance went from 0% to 67.7% with vs. without one, per McMillan. Its length and internal structure are not shown to matter. If anything decays adherence, it&#39;s session length, not file length — a stronger argument for hooks (deterministic enforcement) than for editing file structure.</p>\n<p>Descriptively, <a href=\"https://github.blog/ai-and-ml/github-copilot/how-to-write-a-great-agents-md-lessons-from-over-2500-repositories/\">GitHub&#39;s own analysis of 2,500+ repositories</a> (Matt Nigh, GitHub Blog, 2025-11-19) found effective files converge on executable commands with flags, testing practices, real code-style examples over prose — &quot;one real code snippet showing your style beats three paragraphs describing it.&quot; No length prescription given.</p>\n<p><strong>Compaction, answered directly by the docs:</strong> root-level <code>CLAUDE.md</code> survives <code>/compact</code> — Claude re-reads it from disk and re-injects it. Nested files and <code>paths:</code>-scoped rules reload only when Claude reads files they apply to, so they decay under compaction until re-triggered. Argument for keeping load-bearing rules at root.</p>\n<h2 id=\"4-nested-files-and-the-local-override-pattern\">4. Nested files and the local-override pattern</h2>\n<p>Nested <code>AGENTS.md</code> (root for global rules, per-subtree files only where a subtree genuinely differs) is a named, repeatable practice — confirmed via <a href=\"https://dev.to/mcsee/ai-coding-tip-014-use-nested-agentsmd-files-3iec\">Maximiliano Contieri</a> (O&#39;Reilly author) and <a href=\"https://dev.to/datadog-frontend-dev/steering-ai-agents-in-monorepos-with-agentsmd-13g0\">Simon Boudrias</a> (Datadog).</p>\n<p>Boudrias adds a counter-argument: proximity-based discovery only works if the agent is already working from that subfolder, or is told to <code>@</code>-reference it. His fix: a root-level router file that explicitly dispatches to nested files by task type, not nesting alone.</p>\n<p>The local-override filename is <code>AGENTS.local.md</code>, not <code>AGENTS.md.local</code>. Mostly convention, not tooling — Contieri himself hedges &quot;support varies by tool; check your documentation.&quot;</p>\n<p>What&#39;s actually native:</p>\n<ul>\n<li><strong>Claude Code&#39;s <code>CLAUDE.local.md</code></strong> — documented, gitignore-friendly. Real gotcha: it only exists in the worktree it was created in; use an <code>@~/.claude/...</code> import to share across worktrees.</li>\n<li><strong>Codex CLI&#39;s <code>AGENTS.override.md</code></strong> — checked before <code>AGENTS.md</code> at both global and per-directory scope.</li>\n<li>opencode explicitly declined this (<a href=\"https://github.com/anomalyco/opencode/issues/16110\">issue #16110</a>, closed &quot;not planned&quot;). Everywhere else, a file by that name is simply not read.</li>\n</ul>\n<h2 id=\"5-adrs-as-agent-context-folklore-not-yet-a-spec\">5. ADRs as agent context — folklore, not yet a spec</h2>\n<p>Both sources cited for a 2026 &quot;agent-optimized ADR&quot; trend check out as real — <a href=\"https://www.braingrid.ai/blog/architecture-decision-records-for-ai-coding-agents\">BrainGrid</a> and <a href=\"https://www.actual.ai/blog/agent-optimized-adrs\">Actual AI</a> — but both are vendor content marketing, not independent research. No quantitative validation in either.</p>\n<p>The mechanism-level argument (an agent blind to <em>why</em> a decision was made will confidently refactor the reason away) is plausible and untested. <a href=\"https://adr.github.io/\"><code>adr.github.io</code></a> itself has no agent-related content as of this pass.</p>\n<p>The one attempt at a real standard, <a href=\"https://github.com/me2resh/agent-decision-record\"><code>me2resh/agent-decision-record</code></a> (&quot;AgDR&quot;), records decisions <em>made by</em> an agent, not decisions fed <em>to</em> one — a different problem. Low star count, effectively solo-maintained.</p>\n<p><strong>Bottom line: no canonical agent-optimized ADR format yet.</strong> Worth knowing the idea, not worth adopting a specific template.</p>\n<h2 id=\"6-drift-linting-and-generation-tooling-traction-flagged-bluntly\">6. Drift, linting, and generation tooling — traction flagged bluntly</h2>\n<ul>\n<li><strong><a href=\"https://github.com/agent-sh/agnix\"><code>agent-sh/agnix</code></a></strong> — a real linter/LSP (400+ self-claimed rules across 9 tools) with editor plugins and a GitHub Action. The only tool in this category with anything resembling a real project around it.</li>\n<li>One-person / near-zero traction, worth knowing but not depending on:<ul>\n<li><a href=\"https://github.com/severity1/claude-code-auto-memory\"><code>severity1/claude-code-auto-memory</code></a> — auto-maintains CLAUDE.md via isolated agents, healthiest of the small ones, still solo, pre-1.0.</li>\n<li><a href=\"https://github.com/BitRaptors/Archie\"><code>BitRaptors/Archie</code></a> — near-zero forks, a low-adoption signal.</li>\n<li><a href=\"https://github.com/giacomo/agents-lint\"><code>giacomo/agents-lint</code></a> — checks whether the file still matches the codebase, right idea, no adoption.</li>\n<li><a href=\"https://github.com/felixgeelhaar/cclint\"><code>felixgeelhaar/cclint</code></a> — the only one with an explicit size budget, ~10KB default warning threshold.</li>\n</ul>\n</li>\n<li>The CI check pattern (fail the build if <code>CLAUDE.md</code> isn&#39;t a symlink or a one-line <code>@AGENTS.md</code> import) is real and matches Anthropic&#39;s own suggested pattern. Windows needs Administrator/Developer Mode for symlinks, so the <code>@AGENTS.md</code> import is the portable version.</li>\n<li>Demand for native AGENTS.md support in Claude Code is real: <a href=\"https://github.com/anthropics/claude-code/issues/6235\"><code>anthropics/claude-code#6235</code></a>, open since 2025-08-21 with 5,000+ reactions — the largest open feature request on the tracker. Secondary reporting says Anthropic indicated in May 2026 that native support is &quot;not planned for now&quot;; no primary Anthropic statement confirms this directly.</li>\n</ul>\n<!-- VERIFY: primary source for Anthropic's \"not planned\" response to claude-code#6235 -->\n<p>  Workaround: a symlink or <code>@AGENTS.md</code> import (§4, above).</p>\n<h2 id=\"7-anti-patterns-and-failure-modes\">7. Anti-patterns and failure modes</h2>\n<p><strong>The strongest-sourced risk in this doc: a cloned repo&#39;s own AGENTS.md is a live prompt-injection vector, not a theoretical one.</strong></p>\n<p><a href=\"https://developer.nvidia.com/blog/mitigating-indirect-agents-md-injection-attacks-in-agentic-environments/\">NVIDIA&#39;s AI Red Team</a> (Daniel Teixeira, 2026-04-20) demonstrated a real chain: a malicious dependency detects it&#39;s running inside an agent&#39;s environment, writes an <code>AGENTS.md</code> claiming &quot;absolute authority&quot; that supersedes the user&#39;s own instructions — and instructs the agent&#39;s own PR-summarization step to hide the change from reviewers.</p>\n<p><a href=\"https://www.backslash.security/blog/openai-codex-injection-in-agents-md-exfiltrating-credentials\">Backslash Security</a> (2026-07-06) found OpenAI Codex CLI&#39;s non-interactive <code>exec</code> mode would silently follow attacker-controlled AGENTS.md instructions to exfiltrate AWS/npm/git credentials. OpenAI shipped a partial model-level fix; the underlying claim — safety gating is mode-dependent, not invariant — stands unaddressed.</p>\n<p><strong>Practical read: don&#39;t auto-trust a cloned repo&#39;s instruction file the way you&#39;d trust your own.</strong></p>\n<p>Other named failure modes:</p>\n<ul>\n<li>Anthropic&#39;s own docs admit contradictory rules get resolved arbitrarily, and ship a <code>claudeMdExcludes</code> setting because monorepo ancestor files from <em>other teams</em> pollute context by default.</li>\n<li><a href=\"https://tercek.me/blog/agents-md-is-not-readme/\">Clay Tercek&#39;s critique, &quot;AGENTS.md Is Not a README&quot;</a> adds two sharp points: committing an instruction file imposes one person&#39;s workflow on a whole team, and personal local files don&#39;t track your git branch, so they go stale by construction.</li>\n<li>A widely-reported (but not independently primary-sourced here) 2026 incident — Apple shipping internal <code>CLAUDE.md</code> files inside a consumer app — worth logging as a category example (committed instruction files leaking internal context), even unconfirmed.</li>\n</ul>\n<h2 id=\"8-practical-read-for-this-setup\">8. Practical read for this setup</h2>\n<p>Consistent with <code>topic-index.md</code>&#39;s framing constraint: research and write any future instruction-file work provider-agnostically — <code>AGENTS.md</code> first, <code>CLAUDE.md</code>/others as thin <code>@</code>-imports, not the reverse.</p>\n<p>The empirical record (§3) argues against spending effort trimming file length, and toward:</p>\n<ul>\n<li>Keep root-level content load-bearing — it survives compaction, nested content doesn&#39;t until re-triggered.</li>\n<li>Don&#39;t blanket-trust a cloned repo&#39;s own instruction file (§7).</li>\n<li>Treat within-session decay as the thing hooks should compensate for, not file editing.</li>\n</ul>\n<p>Once <code>~/.claude/CLAUDE.md</code> is revisited per <code>_architecture/BACKLOG.md</code>&#39;s flagged item (unreviewed merge of an old personal version and an AI-suggested rewrite), this doc&#39;s §2–4 is the reference to check it against.</p>\n<h2 id=\"sources\">Sources</h2>\n<p>Linux Foundation press release (2025-12-09, AAIF formation); <code>agents.md</code>; <code>aaif.io/projects/agents-md</code>; <code>github.com/agentsmd/agents.md</code>; arXiv 2606.07448; official docs — <code>code.claude.com/docs/en/memory</code>, <code>code.claude.com/docs/en/hooks</code>, Cursor (<code>cursor.com/docs/context/rules</code>), GitHub Copilot (<code>docs.github.com/copilot</code>), Gemini CLI (<code>github.com/google-gemini/gemini-cli</code>), OpenAI Codex CLI docs, <code>opencode.ai/docs/rules</code>, <code>zed.dev/docs/ai/rules</code>, <code>ampcode.com/manual</code>, <code>docs.devin.ai/desktop/cascade/agents-md</code>, <code>docs.cline.bot/customization/cline-rules</code>, <code>aider.chat/docs/usage/conventions.html</code>, <code>jules.google/docs</code>; arXiv 2602.11988 (ETH Zurich), 2605.10039 (McMillan), 2607.27250 (Khatri), 2509.14744 (PROFES 2025, note: numbers cited elsewhere for this paper did not match on direct fetch); GitHub Blog, Matt Nigh, &quot;How to write a great agents.md&quot; (2025-11-19); Maximiliano Contieri, &quot;Use Nested AGENTS.md Files&quot;; Simon Boudrias, DEV.to (Datadog); BrainGrid and Actual AI on agent-optimized ADRs; <code>adr.github.io</code>; <code>github.com/me2resh/agent-decision-record</code>; <code>github.com/agent-sh/agnix</code>; <code>github.com/severity1/claude-code-auto-memory</code>; <code>github.com/BitRaptors/Archie</code>; <code>github.com/giacomo/agents-lint</code>; <code>github.com/felixgeelhaar/cclint</code>; <code>anthropics/claude-code#6235</code>; NVIDIA AI Red Team, Daniel Teixeira (2026-04-20); Backslash Security, Amit Waizman (2026-07-06); Clay Tercek, &quot;AGENTS.md Is Not a README&quot; (2026-05-27).</p>\n";
