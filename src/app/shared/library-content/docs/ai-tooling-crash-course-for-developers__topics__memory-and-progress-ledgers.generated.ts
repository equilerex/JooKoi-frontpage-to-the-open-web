// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/memory-and-progress-ledgers.md
export const HTML =
  '<h1 id="stage-1-personal-cross-project-memoryledger">Stage 1 — Personal Cross-Project Memory/Ledger</h1>\n<p>This is about keeping a private notebook of what you worked on and why, across projects, separate from any official work log or shared team record. It matters because coding tools don&#39;t remember anything between sessions unless something is set up to hold onto it. Read this if you juggle several projects and keep losing track of what you did last week or why you made a certain call; skip it if you only work on one thing at a time and already remember the history fine.</p>\n<p>Deeper curation on baseline topic #10: a private, cross-repo record of what was worked on and why — independent of git log, not visible to colleagues, not tied to any single repo. Four candidates from Stage 0, evaluated here on setup/maintenance cost, the provider-independence test (&quot;if Claude disappeared tomorrow, how much of this would still be usable&quot;), and automation potential. No winner picked — this is options plus trade-offs, for you to choose from.</p>\n<p><strong>Expanded 2026-08-23</strong> after the user flagged this doc as too thin and conflating three separate concerns. See &quot;Scope clarification&quot; below before reading the rest — it&#39;s the map for where each idea belongs.</p>\n<hr>\n<h2 id="scope-clarification-2026-08-23-three-separate-things-not-one">Scope clarification (2026-08-23) — three separate things, not one</h2>\n<p>Earlier passes on this topic blurred three distinct concerns together. Splitting them out:</p>\n<ol>\n<li><strong>Repo-scoped context/architecture files</strong> — two different things share this bucket:<ul>\n<li><code>AGENTS.md</code>/<code>CLAUDE.md</code>, loaded automatically every session.</li>\n<li>Small context files placed next to the feature they describe (e.g. a file inside <code>accounts/</code>), <em>not</em> auto-loaded — found on demand, either because a developer points the agent at them or tooling discovers them.\nThe second one is a convention/tooling question about one repo, not a memory <em>system</em> — <strong>out of scope for this doc</strong>. It belongs with Stage-0 topic #1 (base instruction files), still-unwritten &quot;per-feature architectural context&quot; sub-topic — see <a href="/library/ai-tooling-crash-course-for-developers/README#1-contextmemory-files"><code>topic-index.md</code></a>. Different lifecycle, different trigger, different owner from the ledger below. <code>_architecture/BACKLOG.md</code> already flags topic #1 as needing its own deep-dive; the research below should seed that doc, not get absorbed here.</li>\n</ul>\n</li>\n<li><strong>A personal, cross-project second-brain/memory system</strong> — tracks <em>you</em> across all your work, independent of any one repo. <strong>This is what the rest of this document is actually about.</strong> The four candidates below, plus the newly-researched ones, all belong here.</li>\n<li><strong>Graphify</strong> — a knowledge-graph-from-any-input tool, primarily graph-and-query over an existing corpus, not a place you write new curated facts as you go. <strong>Mostly out of scope, with one correction</strong>: it turns out to have a secondary, ledger-adjacent layer (<code>save-result</code>, <code>reflect</code>, <code>.graphify_learning.json</code>) bolted on top of its primary graph/query job — not a clean &quot;totally separate purpose&quot; as earlier framed. See its own section below for the correction; the main pick still belongs with this crash-course folder&#39;s <code>README.md</code> &quot;Parked&quot; entry on Graphify, not here.</li>\n</ol>\n<h3 id="repo-scoped-context-files-research-exists-not-yet-written-up-belongs-to-topic-1">Repo-scoped context files — research exists, not yet written up (belongs to topic #1)</h3>\n<p>Captured here so it isn&#39;t lost, not analyzed in depth since it&#39;s out of this doc&#39;s scope. Note the first two bullets below are about <code>AGENTS.md</code>/<code>CLAUDE.md</code> specifically — files that <em>are</em> loaded automatically every session. The separate feature-local idea (files living beside <code>accounts/</code>-style feature code, loaded only on demand) is a distinct pattern with its own prior art — Malloy&#39;s <a href="https://docs.malloydata.dev/blog/2026-01-13-context-md-convention/">CONTEXT.md convention</a>, the <a href="https://github.com/the-michael-toy/llm-context-md">llm-context-md proposal</a>, and the <a href="https://github.com/Agentic-Insights/codebase-context-spec">Codebase Context Specification</a> — that belongs in the eventual &quot;per-feature architectural context&quot; deep-dive, not here.</p>\n<ul>\n<li><strong>Provider loading mechanics genuinely differ</strong>, confirmed via a 2026-08-07 primary-ish source (Alex Dunlop, <code>alexdunlop.com</code>, cross-checked against vendor docs same day): Claude Code reads <code>CLAUDE.md</code> specifically (needs an explicit <code>@AGENTS.md</code> import to pick up the other convention); Cursor reads <code>AGENTS.md</code> with no documented <code>CLAUDE.md</code> support; Copilot auto-detects and combines both with no guaranteed order; Gemini CLI defaults to <code>GEMINI.md</code>. Only three tools (opencode, Zed, Amp) publish an explicit nested-file precedence order when both filenames exist — everyone else either doesn&#39;t document it or explicitly disclaims a guaranteed order.</li>\n<li><strong>Nested <code>AGENTS.md</code> files are a named, repeatable practice</strong>: root file for near-universal rules, nested per-subtree files only where that subtree genuinely differs, an uncommitted <code>AGENTS.md.local</code> for personal preferences (Maximiliano Contieri, cross-posted Medium/DEV.to/Substack under the same identity). Official Claude Code guidance: keep <code>CLAUDE.md</code> under ~200 lines — longer measurably reduces instruction adherence. <!-- VERIFY: exact adherence-degradation figure not independently re-checked this pass --> These nested files are still auto-loaded at session start, same as the root file — they&#39;re not the same mechanism as the on-demand feature-local files described above.</li>\n<li><strong>ADRs are having a specific, mechanism-level 2026 comeback</strong>, not nostalgia: multiple 2026 dev-tooling blogs (BrainGrid, Actual AI) converge on the same claim — an agent blind to <em>why</em> a decision was made will confidently refactor the reason away. <code>AGENTS.md</code>/<code>CLAUDE.md</code> is standing convention loaded every session; an ADR is a point-in-time record of one decision — different jobs. &quot;Agent-optimized ADR&quot; (restructured for a machine reader) is a real described trend with no single canonical spec found yet.</li>\n<li><strong>Drift between multiple instruction files (root + nested + per-tool variants) is the community&#39;s clearly named pain point</strong> — not hypothetical. Responses found: a CI check failing if <code>CLAUDE.md</code> isn&#39;t a symlink/one-line <code>@AGENTS.md</code> import; <code>severity1/claude-code-auto-memory</code> (a Claude Code plugin watching edits and auto-updating <code>CLAUDE.md</code>); a still-unconsolidated crop of <code>AGENTS.md</code> generator repos (<code>BitRaptors/Archie</code> and five others) with no star/fork counts pulled yet — flag the whole generator category as <strong>new-but-promising, unverified</strong>, not a mature &quot;one clear winner&quot; space.</li>\n<li><strong>One unverified claim, don&#39;t cite as fact yet</strong>: a generator repo&#39;s own README claims AGENTS.md is &quot;now under the Linux Foundation&#39;s Agentic AI Foundation, used in 60,000+ repositories&quot; — that&#39;s the project&#39;s own self-description, not confirmed against a Linux Foundation source. <!-- VERIFY: Linux Foundation / Agentic AI Foundation stewardship claim --></li>\n</ul>\n<hr>\n<h2 id="1-claude-codes-built-in-memory-system">1. Claude Code&#39;s built-in memory system</h2>\n<p><strong>What it is.</strong> Since Claude Code 2.1.59 (Feb 2026), Auto Memory ships on by default: Claude writes and maintains its own Markdown files under <code>~/.claude/projects/&lt;project&gt;/memory/</code>, loading an index (<code>MEMORY.md</code>) at session start that points to topic-specific files (this session used exactly this mechanism to store the &quot;no commits&quot; preference). Claude treats its own memory as a hint to verify against real code, not a fact to act on blindly.</p>\n<p><strong>Setup/maintenance cost.</strong> Effectively zero setup — it&#39;s on by default and self-writing. Maintenance is passive: you read/edit/delete files like any Markdown, but the system populates itself as you work and correct it.</p>\n<p><strong>Provider independence.</strong> Weak on this specific axis. The files themselves are plain Markdown and readable/portable by hand, but the <em>mechanism that decides what to write and when</em> is Claude Code&#39;s own harness logic — nothing else currently reads or writes this format automatically. If Claude disappeared, the accumulated files would survive as inert notes; the &quot;occasionally auto-log a ledger entry&quot; behavior would not.</p>\n<p><strong>Automation potential.</strong> Highest of the four, and already built — no scripting required. This directly answers the &quot;can it write itself as I go&quot; want from Stage 0: it already does, today, for free.</p>\n<p><strong>Sourcing.</strong> Official Claude Code docs (<code>code.claude.com/docs/en/memory</code>), and independent write-ups confirming the same architecture (Ian Paterson&#39;s blog on the MEMORY.md/topic-file structure; Claude Directory&#39;s auto-memory guide). Consistent across primary docs and independent coverage — solid.</p>\n<hr>\n<h2 id="2-plain-dated-journal-files">2. Plain dated journal files</h2>\n<p><strong>What it is.</strong> A flat folder of dated Markdown files (<code>~/.agents-journal/YYYY-MM-DD.md</code>), written by hand or with light AI assistance, no special tooling.</p>\n<p><strong>Setup/maintenance cost.</strong> Lowest possible setup (a folder). Maintenance cost is entirely discipline — nothing populates it for you, so it only has value if you (or a prompted AI) actually writes to it each session.</p>\n<p><strong>Provider independence.</strong> Strongest of the four by construction — it&#39;s just files, no vendor mechanism involved at all. Trivially passes &quot;if Claude disappeared tomorrow.&quot;</p>\n<p><strong>Automation potential.</strong> Not self-populating on its own, but easy to bolt automation onto: a base prompt instruction (&quot;append a dated entry summarizing this session&#39;s work to <code>~/.agents-journal/</code>&quot;) turns this into a semi-automated version of option 1, minus the vendor lock-in, at the cost of it only firing when you remember to trigger it (or wire a session-end hook to do so — see baseline topic #6).</p>\n<p><strong>Sourcing.</strong> This is closer to a general documentation practice than a named tool — the search results here are mostly generic &quot;Markdown+Git for docs&quot; advice rather than a specific practitioner-endorsed &quot;personal engineering journal&quot; pattern. Weakest sourcing of the four; treat this as a well-understood pattern rather than a named, widely-cited practice.</p>\n<p><strong>Note:</strong> already hard-vetoed by the user (see <code>decisions/003-memory-ledger-picks.md</code>) — kept here only for completeness of the original four-candidate comparison, not as a live option.</p>\n<hr>\n<h2 id="3-personal-notes-vault-obsidian-class">3. Personal notes vault (Obsidian-class)</h2>\n<p><strong>What it is.</strong> A dedicated, local-first Markdown vault (Obsidian being the practitioner-standard example) used as a &quot;second brain&quot; — linked notes, backlinks, search, and increasingly AI-assisted querying over your own vault.</p>\n<p><strong>Setup/maintenance cost.</strong> Highest of the four — a real tool to learn (linking conventions, folder structure, plugin choices if you go that route). Pays off if you want search/cross-linking across years of notes; overkill if you just want a log.</p>\n<p><strong>Corporate-environment blocker: resolved 2026-08-23.</strong> Previously ruled out here for the corporate constraint (Linux-only native dependencies the firewall flagged). Obsidian&#39;s dependency set was forked and made corporate-compatible, and the npm lib was security-scanned into the org&#39;s own Artifactory. No longer a hard veto.</p>\n<p><strong>Status: active trial, not a decision.</strong> Git-backed Markdown plus hooks (the DIY design elsewhere in this doc) is attractive on paper — cheap, inspectable, no vendor lock-in — but keeping it fresh and organized as the note count grows is a real, unsolved risk. The preferred approach is to try an existing, maintained system before building something custom, and <strong>Obsidian is the first one being trialed</strong> (2026-08-26) — chosen partly to get hands-on experience with it, partly to see how well it can consume or coexist with this repo&#39;s own Markdown planning files. Nothing below should be read as a settled architecture pick; it&#39;s what&#39;s known about Obsidian going into that trial.</p>\n<p><strong>2026 developments worth knowing before evaluating this further:</strong></p>\n<ul>\n<li><strong>Smart Connections</strong> (the dominant Obsidian AI plugin) went from MIT-licensed to proprietary with a $99/yr paywall in January 2026 — the &quot;free in-vault AI plugin&quot; assumption no longer holds for the most popular option.</li>\n<li><strong>The real 2026 trend is bypassing in-vault plugins entirely</strong>: point an external agent (e.g. Claude Code) at the vault via MCP instead, treating the vault as plain markdown you own while the agent does the intelligence work externally. Multiple 2026 sources converge on this pattern (content-marketing-tier sourcing, not named practitioners — weight as directional, not authoritative). Note this pattern is itself affected by the MCP 2026-07-28 stateless spec revision documented in <code>mcp-model-context-protocol.md</code>&#39;s correction section — not verified whether Obsidian-MCP integrations have adopted the new wire format yet.</li>\n</ul>\n<p><strong>Provider independence.</strong> Very strong: vault contents are local Markdown files, independent of any AI account or model, and switching between Claude, GPT, or local models doesn&#39;t lose notes. This is explicitly called out as a selling point by current coverage, not just an incidental property.</p>\n<p><strong>Automation potential.</strong> Real, but requires setup: 2026 coverage points to connecting Claude Code to an Obsidian vault via MCP, turning the vault into a live workspace Claude can read/search/modify — and separately, structured note types plus encoded agent skills are reported to cut knowledge-management overhead from 30–40% of time to under 10% for at least one practitioner. This is the most capable automation path of the four, but also the only one requiring you to build the MCP/skill wiring yourself; it doesn&#39;t come free like option 1.</p>\n<p><strong>Sourcing.</strong> Reasonably well-sourced for the general pattern (Obsidian&#39;s own long track record, multiple 2026 write-ups on Obsidian+AI integration, the MCP-to-Obsidian pattern specifically). One source cited a specific 30–40%→10% overhead claim from a named practitioner blog — treat that number as one person&#39;s report, not a benchmark.</p>\n<hr>\n<h2 id="4-periodic-extraction-scripts-from-session-logs">4. Periodic extraction scripts from session logs</h2>\n<p><strong>What it is.</strong> A script that walks the local JSONL session logs every AI coding tool already writes (<code>~/.claude/projects/*.jsonl</code> for Claude Code, equivalents for Codex CLI, etc.) and compiles a summary ledger — prompts, tool calls, changed files — into Markdown.</p>\n<p><strong>Setup/maintenance cost.</strong> Real but bounded: multiple sources describe this as roughly a 20-line script (walk each JSONL file, emit a heading per user turn, format tool calls as fenced code blocks). Existing prior art includes purpose-built tools (<code>codeburn</code>, <code>tokscale</code> for token/cost tracking across 37+ tools; a <code>parse-sessions.py</code> pattern that takes a project slug and date range and produces a Markdown work summary) as well as roll-your-own scripts covered on DEV Community for browsing Claude Code/Codex CLI logs specifically.</p>\n<p><strong>Provider independence.</strong> Strong on the <em>output</em> (plain Markdown ledger, yours to keep), weaker on the <em>input</em> (each tool&#39;s JSONL log format is vendor-specific, so a script written against Claude Code&#39;s schema needs adjusting — not rewriting from scratch — for Copilot/Gemini equivalents). Better independence than option 1, worse than options 2/3.</p>\n<p><strong>Automation potential.</strong> This <em>is</em> the automation, run on a schedule (cron/scheduled task) rather than triggered per-session — the &quot;occasional local endpoint call&quot; idea from Stage 0 maps most directly onto this option. Doesn&#39;t require you to remember anything mid-session, unlike option 2&#39;s manual/hook-triggered version.</p>\n<p><strong>Sourcing.</strong> Genuinely well-attested — multiple independent tools and write-ups (GitHub repos, DEV Community posts) converge on the same underlying approach, and the JSONL log locations are documented by Claude Code itself. Solid.</p>\n<hr>\n<h2 id="5-named-memory-layersecond-brain-products-added-2026-08-23">5. Named memory-layer/second-brain products (added 2026-08-23)</h2>\n<p>Not in the original four — surfaced by the 2026-08-23 expansion pass specifically for the &quot;developer-scoped personal memory&quot; question, evidence-bar checked.</p>\n<ul>\n<li><strong>Mem0</strong> (github.com/mem0ai/mem0) — 60–63k stars (sources vary June–Aug 2026), 90,000+ developers per the vendor. Framework-agnostic memory layer (bolts onto LangChain/CrewAI/AutoGen/a custom loop), layered memory with fact-promotion, a memory-compression engine aimed at token reduction. Highest-adoption named entry in this category — real, checkable, already listed in <code>_ai-tooling-recommendations.md</code>.</li>\n<li><strong>Letta</strong> (formerly MemGPT) — agent-runtime-as-OS model: main context acts as RAM, an archival memory tier acts as disk, the agent manages its own allocation between them. Positioned for long-horizon coherence rather than simple fact recall — a different shape of problem than &quot;log what I did.&quot;</li>\n<li><strong>Cognee</strong> (github.com/topoteretes/cognee) — 29.7–30k stars, 2.3k forks (Q3 2026). Self-hosted knowledge-graph memory engine, real repo-health signal, self-hostable — relevant given the corporate/no-cloud-dependency constraint that shaped the Obsidian discussion above. The best-evidenced &quot;graph, but not a huge operational lift&quot; middle ground if the opinion piece&#39;s scaling concern (see below) is taken seriously.</li>\n<li><strong>Zep / Graphiti</strong> — temporal knowledge graph (entities as nodes, facts as time-bounded edges). <strong>Correction worth logging</strong>: Zep dropped its self-hostable Community Edition in 2026 — current options are Zep Cloud (managed, credit-based) or building directly on the raw open-source Graphiti library. <code>OpenZep</code> (community, Zep-API-compatible, built on Graphiti) exists as a self-hosted alternative — <strong>new-but-promising, not independently star-checked</strong>.</li>\n</ul>\n<p>None of these is purpose-built for the user&#39;s specific narrow ask (a small, curated, AI-authored fact-set per repo, hook-triggered, mirrored across a folder structure) — the closest real analogues are Cognee (self-hosted graph, avoids Zep&#39;s cloud-only pivot) and plain markdown with AI doing the upkeep (see next section), not a dedicated existing tool.</p>\n<hr>\n<h2 id="6-the-does-markdown-scale-debate-real-not-fringe-not-settled">6. The &quot;does markdown scale&quot; debate — real, not fringe, not settled</h2>\n<p><code>_architecture/local/opinion-piece-mem.md</code> argues markdown-based &quot;memory&quot; (Obsidian, OpenClaw&#39;s <code>MEMORY.md</code>) hits a real ceiling — no querying, no relationships, no schema, no concurrent-write safety — and proposes SQLite or an embedded graph DB (Kuzu) as the real fix once scale is a factor. The 2026-08-23 research pass found this is a genuine, live community tension, not a fringe take, but also not a settled one:</p>\n<ul>\n<li>PKM tooling in 2026 discourse splits into markdown-first (Obsidian, Logseq — local file ownership, no query engine) versus database-style (Tana — every note a structured, propertied, related object).</li>\n<li>The synthesis converging across multiple 2026 sources (content-marketing-tier, not named-practitioner-tier — directional, not authoritative): success depends less on file format itself and more on <strong>whether something automates the curation</strong> — &quot;if the system depends on you linking, tagging, and filing everything yourself, it becomes a second job you will eventually drop.&quot;</li>\n</ul>\n<p><strong>Read against this project&#39;s actual scope:</strong> the opinion piece&#39;s failure modes bite at database-scale (thousands of queryable records). The proposal on the table here is explicitly a <em>small, curated</em> set (<code>ARCHITECTURE.md</code>/<code>decisions.md</code> per repo, AI-authored) — the use case is designed to never grow into what the opinion piece is warning about. This doesn&#39;t kill the critique, but sharpens where it applies: the real risk isn&#39;t &quot;markdown doesn&#39;t scale,&quot; it&#39;s &quot;nothing automates the upkeep and it silently rots&quot; — which is a different, more actionable problem (worth solving with hooks/automation, per the still-open proposal in <code>decisions/003-memory-ledger-picks.md</code>) than &quot;switch to SQLite.&quot;</p>\n<hr>\n<h2 id="7-graphify-correction-to-earlier-framing-added-2026-08-23">7. Graphify — correction to earlier framing (added 2026-08-23)</h2>\n<p>Previously framed (see this crash-course folder&#39;s <code>README.md</code> &quot;Parked&quot; section) as having a &quot;totally different purpose&quot; from a memory ledger — pure graph/query over a fixed corpus, replay-basis. Confirmed real, single canonical project (<code>Graphify-Labs/graphify</code>, 109.7k stars, 10.7k forks, actively released — latest v0.9.48, 2026-08-20; matches this repo&#39;s own <code>/graphify</code> skill and <code>graphify-out/</code> convention, no naming collision). Its core job is confirmed as described: parses code/docs/SQL/PDFs/images/video into a queryable graph, deterministic local tree-sitter AST parsing for code (no vector store, nothing leaves the machine for code specifically), LLM-backed extraction for docs/PDFs/images, every edge tagged <code>EXTRACTED</code> (explicit in source) vs. <code>INFERRED</code> (resolved by graphify).</p>\n<p><strong>The correction:</strong> Graphify is not <em>purely</em> replay/analysis. It has a real, secondary, ledger-adjacent layer bolted on top: <code>graphify save-result</code> records Q&amp;A outcomes (useful/dead_end/corrected), <code>graphify reflect</code> aggregates those into a <code>LESSONS.md</code>, and a work-memory overlay (<code>.graphify_learning.json</code>) tags graph nodes preferred/tentative/contested with recency weighting. So the clean three-way split at the top of this doc isn&#39;t perfectly clean — Graphify is &quot;graph-and-query first, with a curated-outcomes layer bolted on,&quot; not a fully separate concern. No evidence found that anyone uses it as a substitute for a memory/progress ledger; its own docs frame the ledger-like features as supporting the graph, not the reverse.</p>\n<p><strong>Corporate-install blocker — partially addressed, not resolved.</strong> Graphify has a code-only, no-LLM-call extraction mode as well as a fully-local backend option, for corporate setups where a specific capability is policy-disabled rather than cloud access being blocked outright. Itemized detail on this and on this project&#39;s own corporate-environment specifics lives in the gitignored <code>_architecture/local/corporate-environment-audit.md</code>, not here — general educational docs shouldn&#39;t carry one employer&#39;s specific policy shape. Not found: anything confirming or denying whether the native-binary dependency set (numpy/rapidfuzz/tree-sitter parsers) is what&#39;s actually tripping corporate security scanning — the concern already logged in <code>_architecture/BACKLOG.md</code> is still just asserted, not reconfirmed against graphify&#39;s current dependency manifest.</p>\n<p>This still belongs primarily under the Graphify &quot;Parked&quot; note in <code>../README.md</code>, not as a memory-ledger candidate — logged here only to correct the &quot;totally separate purpose&quot; framing.</p>\n<hr>\n<h2 id="8-storagevault-layer-corporate-environment-research-pass-added-2026-08-23-cowork-research-pass">8. Storage/vault layer — corporate-environment research pass (added 2026-08-23, cowork research pass)</h2>\n<p>Direct answer to the open question in <code>decisions/003-memory-ledger-picks.md</code>: what storage/vault layer works for the <strong>centralized personal/cross-project memory</strong> design (a <code>~/.agents</code> tree mirroring real repos, small curated files per repo, AI-authored, hook-triggered) inside the corporate constraint — no <code>uv</code>/<code>pipx</code>, native-binary dependencies get flagged by security scanning, MCP is blocked by policy, no cloud dependency, Windows desktop.</p>\n<p><strong>This is a different question from the feature-local repo-context idea in §1 above.</strong> That design is opt-in and on-demand — a file living beside <code>accounts/</code> or a specific component, pointed to by the developer or discovered by tooling, never a fixed set read automatically. Nothing below applies to it; this is the separate personal-memory ledger question, where &quot;a fixed set of files, always loaded&quot; is the shape being evaluated.</p>\n<h3 id="obsidian-alternatives-most-fail-the-plain-file-agent-can-write-it-filter">Obsidian alternatives — most fail the &quot;plain file, agent can write it&quot; filter</h3>\n<p>The decisive filter for this design is a folder of markdown files an agent can write with an ordinary file write — no plugin, no API, no daemon required to read them back. That filter eliminates most of the field:</p>\n<ul>\n<li><strong>Fails outright — not plain files on disk</strong>: Anytype (encrypted proprietary object store, explicitly not accessible outside the app — confirmed via <code>doc.anytype.io</code>), Trilium/TriliumNext (single SQLite <code>document.db</code>, per <code>docs.triliumnotes.org</code>), Joplin (official FAQ: sync files are &quot;not meant to be user-editable&quot;), Memos (SQLite/MySQL/Postgres via API), Outline (Postgres+Redis+S3 stack per third-party self-hosting guides, not independently confirmed against Outline&#39;s own docs this pass), Tana (SaaS only, no self-hosted option found on its own pricing page).</li>\n<li><strong>A real reversal worth flagging</strong>: <strong>Logseq&#39;s DB version now stores the graph in SQLite, not markdown</strong> (<code>~/logseq/graphs/&lt;name&gt;/db.sqlite</code>, confirmed via <code>logseq/docs</code>&#39; own <code>db-version.md</code>, still beta with an explicit data-loss warning). The file-based version still exists, but the project&#39;s direction is away from it. A HN thread on the DB beta surfaced the exact complaint relevant here: &quot;I can no longer keep all my data as markdown files... half the edits are done by Claude.&quot; Not a safe long-term pick for this design.</li>\n<li><strong>Dendron is dormant, not just quiet</strong> — repo banner states verbatim &quot;currently in maintenance only, active development has ceased.&quot; Rule out.</li>\n<li><strong>Passes, with caveats:</strong><ul>\n<li><strong>Foam</strong> — VS Code extension over an ordinary folder of markdown+wikilinks, not an app that owns the data. 17.2k★, active 2026 release, Windows-fine. If the extension disappeared the files are unaffected. Best fit of the named vault tools.</li>\n<li><strong>SilverBullet</strong> — self-hosted, single Windows/macOS/Linux binary, official docs confirm plain-markdown storage, independently reviewed on LWN. Real option, but it&#39;s a background local web server — closer to the class of unsigned local binary corporate scanning tends to flag — and built for a much bigger vault than a small curated set.</li>\n<li><strong>Zettlr</strong> — Electron editor over plain folders, active, cross-platform. Works as a reader; the on-disk-format claim is vendor marketing, not independently documented in this pass.</li>\n</ul>\n</li>\n<li>No credible new 2026 entrant was found — search results below the fold were SEO listicle content, not treated as sources.</li>\n</ul>\n<h3 id="the-stronger-finding-named-prior-art-for-no-vault-at-all">The stronger finding: named prior art for &quot;no vault at all&quot;</h3>\n<p>For the centralized personal-memory design specifically (not the feature-local idea — see the disambiguation note above) — a small, fixed set of AI-authored markdown files per project, read at session start — this has a name in the field: the <strong>Memory Bank</strong> pattern.</p>\n<ul>\n<li><strong>Cline&#39;s own docs</strong> (<code>docs.cline.bot/prompting/cline-memory-bank</code>) define exactly this: a <code>memory-bank/</code> directory with six fixed files (<code>projectbrief.md</code>, <code>productContext.md</code>, <code>activeContext.md</code>, <code>systemPatterns.md</code>, <code>techContext.md</code>, <code>progress.md</code>), collaboratively written and updated at task start/milestones, explicitly &quot;requires no external database or special dependencies.&quot;</li>\n<li><strong>Independent, org-published corroboration</strong>: Modus Create&#39;s Agentic Coding Handbook (<code>tweag.github.io/agentic-coding-handbook</code>) restates the same six-file structure and states the design rule directly: keep it &quot;short, structured, and current,&quot; exclude &quot;unfiltered chat transcripts or raw meeting notes&quot; — an independent org arriving at the same &quot;not a transcript dump&quot; constraint already decided here.</li>\n<li><strong>The closest single match to this project&#39;s specific centralized-mirror twist</strong>: <code>github.com/jayzeng/agentmemory</code> — a central <code>~/.agent-memory/</code> (configurable) holding <code>MEMORY.md</code>/<code>SCRATCHPAD.md</code>/<code>daily/</code>/<code>topics/</code>, distributed as a SKILL.md into each tool&#39;s skills folder, no database, no MCP, Windows paths documented. Repo health is weak by this project&#39;s own bar — <strong>14 stars, 2 forks, one person</strong> — read as a design reference, not a dependency.</li>\n<li><strong>Honest gap</strong>: nobody named was found doing the <em>exact</em> variant of centralizing outside the repos in a path-mirrored tree. Every other named instance (Cline, Modus Create&#39;s writeup, <code>severity1/claude-code-auto-memory</code>, OpenClaw&#39;s own memory system) keeps the files <strong>inside</strong> the repo, version-controlled with the code — trading &quot;memory travels with the code and gets reviewed&quot; for &quot;doesn&#39;t work in a repo you can&#39;t commit personal files to,&quot; which is exactly the corporate constraint driving this design.</li>\n</ul>\n<h3 id="hook-triggered-agent-authored-memory-prior-art-mostly-disqualified-by-the-same-corporate-filter">Hook-triggered agent-authored memory — prior art, mostly disqualified by the same corporate filter</h3>\n<p>Confirmed first: Claude Code&#39;s <code>SessionStart</code> and <code>SessionEnd</code> hooks both exist today (<code>code.claude.com/docs/en/hooks</code>), each firing once per session — the mechanical trigger the proposed design assumes is real, not aspirational.</p>\n<p>Named tools checked, all against the corporate filter:</p>\n<ul>\n<li><code>thedotmack/claude-mem</code> — large user base, but requires Bun + <code>uv</code> + Chroma&#39;s native deps. Fails.</li>\n<li><code>coleam00/claude-memory-compiler</code> — closest <em>architecture</em> match: pure markdown output, no vector DB, no MCP. Ships requiring <code>uv</code> though — that&#39;s packaging, not design, so worth reading as a blueprint.</li>\n<li><code>obra/episodic-memory</code> — named practitioner, MIT license, but needs MCP + sqlite-vec + an ONNX runtime. Fails on three separate grounds.</li>\n<li><code>jayzeng/agentmemory</code> — see above.</li>\n<li><code>cyrusNuevoDia/capn-hook</code> — novel idea: entries carry file hashes so <code>ARCHITECTURE.md</code> cache-busts when the underlying code changes. Downloads a 300MB–2GB embedding model on first use though. Fails.</li>\n<li>Tomaz Bratanic&#39;s Neo4j hook-based design (<code>towardsdatascience.com</code>) — named practitioner, strong track record. Worth reading for the <em>shape</em>: cheap append online, LLM curation offline into small markdown files at semantic paths, triggered before compaction rather than only at session end. Needs a Neo4j server though. Fails.</li>\n<li><strong>OpenClaw&#39;s own memory system</strong> (<code>~/.openclaw/workspace/MEMORY.md</code> + dated notes + a pre-compaction &quot;silent flush&quot; + background &quot;dreaming&quot; consolidation) — the strongest convergent design evidence in this whole pass. An independent, mainstream harness arrived at nearly the same file taxonomy and the same pre-compaction trigger point. Worth citing as validation of the shape, even though it&#39;s not something to install.</li>\n</ul>\n<h3 id="embeddedqueryable-backend-the-opinion-pieces-sqlitekuzu-proposal-needs-a-correction">Embedded/queryable backend — the opinion piece&#39;s SQLite/Kuzu proposal needs a correction</h3>\n<p><code>local/opinion-piece-mem.md</code> (already folded into <code>decisions/003</code>) proposed SQLite or an embedded graph DB (Kuzu) once scale is a factor. Checked against the corporate filter:</p>\n<ul>\n<li><strong>Python&#39;s stdlib SQLite ships with FTS5 compiled in</strong> — confirmed directly against CPython&#39;s own Windows build config (<code>PCbuild/sqlite3.vcxproj</code>, <code>SQLITE_ENABLE_FTS5</code> in the preprocessor definitions). <code>import sqlite3</code> plus <code>CREATE VIRTUAL TABLE ... USING fts5(...)</code> needs zero installs — no pip, no wheel, nothing for corporate scanning to flag. This is the one backend in the whole search that cleanly passes.</li>\n<li><strong>Kuzu is dead — the opinion piece&#39;s specific recommendation is stale by about ten months.</strong> The parent project (<code>kuzudb/kuzu</code>) archived itself 2025-10-10, confirmed via the repo&#39;s own README and independently via The Register&#39;s coverage of the abandonment. Two community forks exist (<code>Kineviz/bighorn</code>, 131★, no releases, build-from-source; <code>Vela-Engineering/kuzu</code>, 36★, maintained by a VC firm using it internally, no long-term commitment stated) — both fail the corporate filter twice over (native C++ binaries, plus the maintenance risk of a sub-200-star fork).</li>\n<li><code>sqlite-vec</code>, DuckDB, LanceDB, and libSQL were all checked and all fail the native-compiled-binary filter that caused problems before — they&#39;re worth knowing about if the scope ever grows past &quot;small curated set,&quot; not worth adopting now.</li>\n<li><strong>The size argument stated plainly</strong>: for a small, curated set of files rather than a growing database, <code>pathlib</code>/<code>re</code> (or optional stdlib FTS5 as a rebuildable index) already covers retrieval. Nothing above buys anything until the corpus is large enough that grep is slow — and the design as specified doesn&#39;t get there.</li>\n</ul>\n<h3 id="one-directional-2026-data-point">One directional 2026 data point</h3>\n<p><code>nex-crm/wuphf</code> (a funded 2026 YC company building a CRM-adjacent agent tool) defaults its memory backend to <strong>&quot;git-native markdown&quot;</strong> — a local git repo of markdown notes — explicitly demoting its earlier Postgres backend to legacy. It fails the corporate filter itself (MCP-based access, non-OSI license), but it&#39;s evidence that at least one funded 2026 team made the same bet this design is making: markdown-in-git over a database, for agent memory specifically.</p>\n<p><strong>Sources for this section</strong>: <code>docs.cline.bot/prompting/cline-memory-bank</code>; <code>tweag.github.io/agentic-coding-handbook/WORKFLOW_MEMORY_BANK</code>; <code>github.com/jayzeng/agentmemory</code>; <code>github.com/thedotmack/claude-mem</code>; <code>github.com/coleam00/claude-memory-compiler</code>; <code>github.com/obra/episodic-memory</code>; <code>github.com/cyrusNuevoDia/capn-hook</code>; Tomaz Bratanic, &quot;Unified Agentic Memory Across Harnesses Using Hooks&quot; (Towards Data Science, 2026-05-08); <code>github.com/openclaw/openclaw</code> <code>docs/concepts/memory.md</code>; <code>github.com/foambubble/foam</code>; <code>github.com/silverbulletmd/silverbullet</code> + LWN review; <code>github.com/logseq/logseq</code> + <code>logseq/docs/db-version.md</code>; <code>github.com/dendronhq/dendron</code>; <code>github.com/kuzudb/kuzu</code> + The Register (2025-10-14); <code>github.com/Kineviz/bighorn</code>; <code>github.com/Vela-Engineering/kuzu</code>; CPython <code>PCbuild/sqlite3.vcxproj</code>; <code>github.com/nex-crm/wuphf</code>; <code>doc.anytype.io</code>; <code>docs.triliumnotes.org</code>; <code>joplinapp.org/help/faq</code>.</p>\n<hr>\n<h2 id="what-still-needs-a-real-trial-not-just-research">What still needs a real trial, not just research</h2>\n<p>Which of these actually sticks is not decidable from research alone — Stage 0 already flagged this as &quot;legitimately unclear which wins until you&#39;ve tried logging into one for a few weeks,&quot; and nothing found here overturns that. Two things are decidable now, though: option 1 (built-in memory) costs nothing to keep running regardless of what else gets adopted, since it&#39;s already on; and option 2 (plain journal, already vetoed) is out. The real live fork is between &quot;accept vendor coupling for zero-effort automation&quot; (1), &quot;own the pipeline for stronger independence&quot; (4), and &quot;a real notes/memory tool&quot; (3 or 5, now that Obsidian&#39;s corporate blocker is resolved) — that&#39;s a judgment call on how much the provider-independence constraint and automation-vs-ceremony tradeoff should weigh, not a research question. Preference stated: try an existing, maintained system before building anything custom, rather than picking the &quot;correct&quot; design up front — Obsidian is that first trial (see §3), not a final pick, and trialing more than one option in parallel is fine.</p>\n<hr>\n<h2 id="sources">Sources</h2>\n<p>Claude Code official docs (<code>code.claude.com/docs/en/memory</code>); Ian Paterson, &quot;Claude Code Memory System: MEMORY.md &amp; Topic Files&quot;; Claude Directory, &quot;Claude Code&#39;s Auto-Memory&quot; guide; Eric J. Ma, &quot;Mastering Personal Knowledge Management with Obsidian and AI&quot; (2026); multiple 2026 Obsidian+AI/MCP integration write-ups (eesel AI, NxCode, dsebastien.net); <code>codeburn</code> and <code>tokscale</code> GitHub repos (session-log/token tracking across AI coding tools); DEV Community posts on parsing Claude Code/Codex CLI JSONL session logs; labuladong.online on Claude Code session storage locations.</p>\n<p><strong>Added 2026-08-23 (storage/vault research pass, §8):</strong> <code>docs.cline.bot/prompting/cline-memory-bank</code>; <code>tweag.github.io/agentic-coding-handbook</code>; <code>github.com/jayzeng/agentmemory</code>; <code>github.com/thedotmack/claude-mem</code>; <code>github.com/coleam00/claude-memory-compiler</code>; <code>github.com/obra/episodic-memory</code>; <code>github.com/cyrusNuevoDia/capn-hook</code>; Tomaz Bratanic (Towards Data Science, 2026-05-08); <code>github.com/openclaw/openclaw</code>; <code>github.com/foambubble/foam</code>; <code>github.com/silverbulletmd/silverbullet</code>; <code>github.com/logseq/logseq</code>; <code>github.com/dendronhq/dendron</code>; <code>github.com/kuzudb/kuzu</code>; The Register (2025-10-14); CPython <code>sqlite3.vcxproj</code>; <code>github.com/nex-crm/wuphf</code>.</p>\n<p><strong>Added 2026-08-23:</strong> <code>github.com/mem0ai/mem0</code>; Vectorize.io&#39;s Mem0-vs-Letta and Zep-alternatives comparisons; <code>github.com/topoteretes/cognee</code>; OpenZep community project (unverified repo-health); reporting on Smart Connections&#39; 2026 license change; <code>github.com/Graphify-Labs/graphify</code> (direct repo check, v0.9.48); Graphify&#39;s own CLI docs (<code>save-result</code>/<code>reflect</code>/<code>--code-only</code>/<code>--backend ollama</code>); Alex Dunlop, &quot;CLAUDE.md vs AGENTS.md&quot; (alexdunlop.com, 2026-08-07); Maximiliano Contieri, &quot;Use Nested AGENTS.md Files&quot;; BrainGrid and Actual AI on agent-optimized ADRs; <code>github.com/severity1/claude-code-auto-memory</code>; <code>github.com/BitRaptors/Archie</code> (AGENTS.md generator, unverified repo-health).</p>\n';
