// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary.md
export const HTML =
  '<h1 id="stage-1-harness-engineering-context-engineering-repository-legibility">Stage 1 — Harness Engineering, Context Engineering, Repository Legibility</h1>\n<p>New topic (not covered by any prior stage-1 doc), surfaced via a ChatGPT-sourced breakdown (quarantine trail resolved and removed once processed). Covers what a harness actually is, how information flows through it, and what makes the surrounding codebase usable by it. No pick, practices to consider adopting, not a mandate.</p>\n<p><strong>Split 2026-09-05:</strong> agent loops moved out to their own doc, <a href="/library/ai-tooling-crash-course-for-developers/topics/loop-engineering-agent-loops"><code>loop-engineering-agent-loops.md</code></a> — a loop always runs inside some harness, but building one is a distinct enough topic to stand alone.</p>\n<h2 id="1-harness-engineering-what-it-actually-means">1. Harness engineering — what it actually means</h2>\n<p>Per OpenAI&#39;s own framing (<code>openai.com/index/harness-engineering/</code> — confirmed real via independent secondary coverage; the page itself blocks direct fetching but three independent write-ups, including InfoQ and Milvus&#39;s technical blog, converge on the same content): a harness is the designed environment around an agent. The primary engineering job shifts from writing code to &quot;designing environments, specifying intent, and building feedback loops.&quot;</p>\n<p>OpenAI&#39;s own worked example: over one million lines of a real product generated with zero manually-written code, by:</p>\n<ul>\n<li>making the repository structurally legible to agents,</li>\n<li>encoding the project&#39;s core principles/conventions directly into the repo,</li>\n<li>running scheduled background agent tasks that scan for convention deviations and submit their own refactoring PRs.</li>\n</ul>\n<p>The dump&#39;s list of what a harness covers — tool access, permissions/sandboxing, context loading, planning/task state, retries/recovery, test execution, verification, subagents, compaction/memory, logging, stop conditions — is a reasonable inventory. But the <em>level</em> matters: this project already has real harness components without naming them as such.</p>\n<ul>\n<li><code>AGENTS.md</code> files are context loading.</li>\n<li><code>_architecture/next-steps.md</code>/<code>current-state.md</code> and <code>llm-progress-complete.jsonl</code> are task state and logging.</li>\n<li>The Superpowers skill framework already active in this Claude Code session is a planning/verification layer.</li>\n</ul>\n<p>Naming it &quot;harness engineering&quot; doesn&#39;t change any of that. The value of the term: it names the <em>whole system</em> as one design surface, so a change to one part (adding a new skill) gets evaluated against how it affects the others (context budget, verification coverage) rather than in isolation.</p>\n<h2 id="2-context-engineering-real-gap-or-already-covered">2. Context engineering — real gap or already covered?</h2>\n<p>Checked against <a href="/library/ai-tooling-crash-course-for-developers/topics/session-and-token-economics"><code>session-and-token-economics.md</code></a> directly. That doc covers cost/economics of context (caching pricing, linear cost growth, compaction-vs-fresh tradeoffs) — a financial/mechanical framing.</p>\n<p>Anthropic&#39;s <code>effective-context-engineering-for-ai-agents</code> article (fetched directly) defines a <strong>different, complementary</strong> discipline: &quot;the set of strategies for curating and maintaining the optimal set of tokens (information) during LLM inference,&quot; guided by finding &quot;the smallest possible set of high-signal tokens that maximize the likelihood of some desired outcome.&quot; This is about <em>what</em> goes in the window, not what it costs once it&#39;s there.</p>\n<p>Concrete practices from the source, genuinely not covered in the existing session-economics doc:</p>\n<ul>\n<li><strong>Minimal, non-overlapping tool sets</strong> that return token-efficient information. Tool bloat and ambiguous tool overlap actively confuses the model, independent of any cost concern.</li>\n<li><strong>&quot;Just-in-time&quot; context</strong> — agents dynamically retrieve data via tools at the moment it&#39;s needed (file paths, stored queries, links as lightweight identifiers) instead of front-loading everything into the system prompt. Same principle Agent Skills&#39; progressive disclosure already implements at the skill level (covered in <a href="/library/ai-tooling-crash-course-for-developers/topics/skill-libraries-and-marketplaces"><code>skill-libraries-and-marketplaces.md</code></a>) — repeats at the tool-design level too.</li>\n<li><strong>Structured note-taking as persistent external memory</strong> — distinct from compaction (compaction summarizes what happened; note-taking is the agent deliberately writing durable facts to a file as it works, meant to be read back later). Close to, but not identical to, the Topic #10 memory-ledger question — cross-reference when that pick is made.</li>\n<li><strong>Diverse canonical examples over exhaustive edge-case lists</strong> — a prompt-writing guideline, applies directly to how skills and <code>AGENTS.md</code> files should be written.</li>\n</ul>\n<p>Net: real, complementary gap. Session-economics answers &quot;does this cost too much and is quality degrading.&quot; Context engineering answers &quot;is the right information in the window at all, and is there too much irrelevant material crowding it.&quot; Both matter, neither substitutes for the other.</p>\n<h2 id="3-repository-legibility">3. Repository legibility</h2>\n<p>Named explicitly in OpenAI&#39;s harness-engineering piece as one of three pillars of their million-line experiment: a structured repository, with the project&#39;s conventions encoded directly into it (not left as tribal knowledge), so an agent starting fresh can discover what it needs from the repo itself rather than a person&#39;s memory or a Slack thread.</p>\n<p>The dump&#39;s list — architecture notes, decision records, explicit test commands, known-failure documentation, generated codebase maps, schemas, executable validation, small coherent files — maps almost one-to-one onto conventions this repo and the JooKoi product repo already commit to: <code>JooKoi-frontpage-to-the-open-web/AGENTS.md</code> states &quot;Decisions are ADR-lite... one file per decision,&quot; names <code>ARCHITECTURE.md</code> as the whole-system doc (deferred to Stage 5, not built yet — a legibility gap by its own admission, not an oversight), and separates <code>sources/</code> (human-authored) from <code>data/</code> (machine-observed) explicitly so an agent can tell which clock governs which files without being told in conversation.</p>\n<p><strong>One real gap in this repo&#39;s own practice:</strong> executable checks as part of the system of record, not just written docs. OpenAI&#39;s background-agent pattern (scheduled tasks scanning for convention deviations, submitting their own fixes) is a step beyond what&#39;s here — this repo&#39;s conventions are currently enforced by whoever reads <code>AGENTS.md</code> before acting, not by anything that runs and checks. Given this repo&#39;s own stated anti-speculative-infrastructure stance (<code>docs/reasoning.md</code>), building a scheduled deviation-scanner now would be premature — not enough accumulated convention yet to make automated drift-checking worth the setup cost. A real future step once <code>skills/</code>/<code>prompts/</code> have real content, not something to build today.</p>\n<h2 id="4-cross-reference-check-verification-loops-and-subagents">4. Cross-reference check — verification loops and subagents</h2>\n<p>Per the source dump&#39;s overlap flags:</p>\n<ul>\n<li>&quot;Verification loops / eval-driven development&quot; is already covered by <a href="/library/ai-tooling-crash-course-for-developers/topics/review-and-verification-tooling"><code>review-and-verification-tooling.md</code></a> (IDE review tooling, Claude Code&#39;s own <code>/code-review</code>/<code>/ultrareview</code>, PR-Agent, the cold-start-critic pattern) — no meaningful gap found, the framing is consistent (make correctness machine-checkable, don&#39;t trust self-report).</li>\n<li>&quot;Parallel agents / subagent delegation&quot; is covered by <a href="/library/ai-tooling-crash-course-for-developers/topics/subagents-and-delegation"><code>subagents-and-delegation.md</code></a> and <a href="/library/ai-tooling-crash-course-for-developers/topics/session-and-token-economics"><code>session-and-token-economics.md</code></a> §B — the &quot;primary agent owns the task, specialized agents produce bounded findings, primary agent integrates and verifies&quot; pattern the dump describes is exactly the fork-based pattern already in active use across this whole curation project (the parent conversation dispatching bounded research forks and merging their results).</li>\n</ul>\n<p>No new doc needed for either.</p>\n<h2 id="connection-to-existing-conventions">Connection to existing conventions</h2>\n<p>Nothing here contradicts or requires changing <code>JooKoi-frontpage-to-the-open-web/AGENTS.md</code> or this repo&#39;s own structure — it&#39;s a vocabulary and completeness check, not a redesign. One concrete forward-looking note: when <code>ARCHITECTURE.md</code> in the JooKoi product repo eventually gets written (Stage 5, per its own roadmap), it&#39;s the direct repository-legibility artifact this research describes — write it with an agent&#39;s-eye view (what would a fresh-context agent need to discover here) rather than as human-facing documentation, per OpenAI&#39;s framing.</p>\n<h2 id="open-questions-weak-sourcing">Open questions / weak sourcing</h2>\n<ul>\n<li>OpenAI&#39;s harness-engineering page itself returned HTTP 403 on direct fetch (likely bot-blocking, not a content issue) — content confirmed via three independent secondary sources (InfoQ, Milvus&#39;s technical blog, a Medium write-up) converging on the same specifics, not via the primary page directly. Treat as high-confidence but not verbatim-sourced.</li>\n<li>The dump&#39;s individual claim-to-source mappings aren&#39;t fully reliable even when the underlying links are real (see <a href="/library/ai-tooling-crash-course-for-developers/topics/loop-engineering-agent-loops"><code>loop-engineering-agent-loops.md</code></a> for a caught example, a misattribution about agent loops) — consistent with this project&#39;s broader finding that AI-search summaries sometimes misattribute specific claims to real sources that say something adjacent but different.</li>\n<li>Anthropic&#39;s context-engineering article was fetched successfully and its claims quoted directly — high confidence.</li>\n<li>ACP and A2A (also new topics from the same Batch 5 dump) are covered in a separate document by a parallel research pass — not duplicated here.</li>\n</ul>\n';
