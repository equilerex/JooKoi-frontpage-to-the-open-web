// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/subagents-and-delegation.md
export const HTML = "<h1 id=\"stage-1-subagents-multi-agent-delegation\">Stage 1: Subagents / Multi-Agent Delegation</h1>\n<p>A subagent is a helper agent the parent spins up for one bounded piece of work, in its own context. Whatever it reads or runs stays there — only its final report comes back. Useful if your coding sessions run long and context fills up. Skip if sessions stay short.</p>\n<p>Stage 0 already drew the line: fresh-context subagents for <em>scope</em> (well-supported) vs. multi-agent voting for <em>confidence</em> (contested). This doc covers the subagent concept itself, then compares Claude Code, Copilot CLI, and Gemini CLI.</p>\n<h2 id=\"what-is-a-subagent\">What is a subagent?</h2>\n<p>A separate agent instance the parent invokes for one bounded task. Parent sends a task description → subagent runs its own tool loop (read files, run commands, search) → reports back a result (summary, findings, diff) → parent decides what to do with it.</p>\n<p>The defining feature: <strong>context isolation</strong>.</p>\n<h3 id=\"why-the-pattern-exists\">Why the pattern exists</h3>\n<p>Context windows are fixed-size. Long sessions fill them fast: reading a dozen files to find one function, a build log, a repo-wide grep. None of that raw material matters once you have the answer — but if it happened in the main conversation, it stays there forever, slowing and costing more on every later turn.</p>\n<p>Subagents push that disposable work into a side context that gets thrown away. Only the conclusion comes back.</p>\n<h3 id=\"what-happens-to-a-subagents-context\">What happens to a subagent&#39;s context</h3>\n<ul>\n<li>Starts fresh — no parent history unless explicitly included in the brief.</li>\n<li>Everything it reads/runs stays in its own window.</li>\n<li>Only the final report returns. Parent&#39;s context grows by that report, not by the work behind it.</li>\n</ul>\n<p>Bonus: fresh context means no anchoring bias. A subagent reviewing code for bugs isn&#39;t carrying the same assumptions that built the code.</p>\n<h3 id=\"when-delegation-saves-tokens\">When delegation saves tokens</h3>\n<p>Win condition: raw output volume &gt;&gt; useful conclusion, and the parent has no further need for the raw material.</p>\n<ul>\n<li>Broad codebase exploration (&quot;find every place this API is called&quot;)</li>\n<li>Reading logs/build output/test results to extract one fact</li>\n<li>Research that ends in a synthesized answer — sources don&#39;t need to stay in context</li>\n<li>Independent investigations that don&#39;t depend on each other</li>\n</ul>\n<h3 id=\"when-delegation-costs-more\">When delegation costs more</h3>\n<ul>\n<li>One-line fixes, single-file edits — anything doable in 1-2 tool calls</li>\n<li>Tasks needing most of the current conversation restated in the brief (nothing got isolated)</li>\n<li>Several subagents all re-reading the same shared files (duplicated cost)</li>\n</ul>\n<h3 id=\"good-candidates\">Good candidates</h3>\n<ul>\n<li>Independent, self-contained investigations (&quot;how does auth work here&quot;)</li>\n<li>Several unrelated fixes that don&#39;t touch the same files</li>\n<li>Research where only the summary matters afterward</li>\n</ul>\n<h3 id=\"poor-candidates\">Poor candidates</h3>\n<ul>\n<li>Sequential work where step 2 needs step 1&#39;s full output, not a summary — isolation breaks this</li>\n<li>Tiny mechanical changes</li>\n<li>Anything needing the full running conversation to decide correctly</li>\n<li>Parallel subagents editing the same file (conflicts)</li>\n</ul>\n<h3 id=\"automatic-vs-explicit-delegation\">Automatic vs. explicit delegation</h3>\n<p>Both exist. Copilot CLI decides on its own sometimes (see below). Claude Code supports both: it can suggest delegation, or you invoke <code>Task</code>/a named subagent directly.</p>\n<p><strong>When to ask for one yourself:</strong> the task will clearly generate a pile of disposable exploration (&quot;map this module,&quot; &quot;find every caller of X&quot;), or you want a genuinely independent second opinion. Not sure it&#39;ll generate much noise? Do it inline, delegate next time if it turns out bigger.</p>\n<h3 id=\"parallel-delegation\">Parallel delegation</h3>\n<p>Independent subagents can run concurrently — saves wall-clock time too, not just context. Only works when tasks are truly independent (no shared files, no mid-task coordination needed). Practical ceiling: roughly 3-5 concurrent subagents. Past that, managing the outputs eats the gain.</p>\n<h3 id=\"simple-subagents-vs-orchestration-frameworks\">Simple subagents vs. orchestration frameworks</h3>\n<p>Claude Code / Copilot CLI / Gemini CLI subagents: bounded handoff. Delegate once, run once, report back, done. No persistent state, no subagent-to-subagent chat.</p>\n<p>Frameworks like CrewAI, AutoGen, LangGraph go further: persistent roles (agents with memory across many tasks), shared state/queues, direct inter-agent messaging. That&#39;s a different architecture — built for standalone multi-agent <em>products</em>, not for delegating a subtask mid-session.</p>\n<h3 id=\"when-multi-agent-is-overkill\">When &quot;multi-agent&quot; is overkill</h3>\n<p>Most dev work needs one agent that occasionally delegates bounded subtasks — investigate, fix, review. Reach for a full framework only when the actual deliverable <em>is</em> a multi-agent system. Don&#39;t use one to make a single session feel fancier. Unstructured swarms with no single owner mostly produce noise unless roles, handback artifacts, and merge criteria are explicit (see below).</p>\n<h2 id=\"implementation-comparison\">Implementation comparison</h2>\n<p>Same concepts everywhere. Syntax and defaults differ — secondary detail.</p>\n<h3 id=\"claude-code\">Claude Code</h3>\n<p><strong>Delegate when:</strong> research touching 10+ files, work splitting into 3+ independent pieces, sequential pipelines with distinct phases, or anything needing a genuinely clean slate. Rule of thumb (<a href=\"https://www.claude.com/blog/subagents-in-claude-code\">Anthropic&#39;s guidance</a>): if a task would flood the session with search results or logs you&#39;ll never look at again, delegate. See the <a href=\"https://docs.claude.com/en/docs/claude-code/sub-agents\">subagents docs</a> for configuring reusable roles.</p>\n<p><strong>Good briefs:</strong> scope tightly, name the output format (summary / findings list / recommendation), request independent tasks in parallel rather than one at a time.</p>\n<p>Developer note: worth compiling a list of recurring tasks where prewritten scraper/helper scripts and premade prompts would cut token cost on repeated subagent runs.</p>\n<p><strong>Anti-patterns:</strong> sequential dependent work, parallel subagents on the same file, tiny tasks, a custom subagent for every scenario instead of a handful of well-scoped ones, tight mid-flight coordination.</p>\n<h3 id=\"copilot-cli-real-actively-tuned\">Copilot CLI — real, actively tuned</h3>\n<p>Two mechanisms:</p>\n<ol>\n<li><strong>Automatic.</strong> CLI decides on its own (unfamiliar-repo exploration, long commands). GitHub&#39;s 2026 &quot;smarter delegation&quot; update made this more selective after the prior version over-delegated. A/B test: 23% fewer tool failures per session (27% search, 18% edit), modest wait-time gains, no quality loss. 100% of production traffic now.</li>\n<li><strong>Explicit.</strong> <code>/delegate</code> (or <code>&amp;</code>) hands a task to a GitHub-hosted Copilot cloud agent, which works on a branch in the background and opens a draft PR. Different shape from in-session subagents — &quot;hand off and check later,&quot; not &quot;spawn a helper here.&quot;</li>\n</ol>\n<p>No install needed for either.</p>\n<h3 id=\"gemini-cli-shipped-april-2026-v036\">Gemini CLI — shipped April 2026 (v0.36)</h3>\n<p>Same shape as Claude Code&#39;s: own instructions, own context window, optional restricted tool set, runs in parallel. Ships several built-in subagents (general assistant, CLI helper, codebase investigator) — nothing to author from scratch. One real difference: Gemini 2.5 Pro&#39;s context window (1M+ tokens) dwarfs Claude&#39;s per-subagent window (~200-400K) — matters if a subagent needs to hold a huge amount of source material at once.</p>\n<p><strong>Bottom line:</strong> Copilot and Gemini CLI both have real, built-in subagent primitives now. The gap that used to exist has closed.</p>\n<h2 id=\"bounded-roles-vs-unstructured-swarms\">Bounded roles vs. unstructured swarms</h2>\n<pre><code>primary agent owns the task\nspecialized subagents produce bounded findings\nprimary agent integrates and verifies\n</code></pre>\n<p>Not: agents freely coordinating with no single owner. That mostly produces noise unless roles, handback artifacts, and merge criteria are explicit. Same &quot;3-5 concurrent, avoid tight coupling&quot; guidance above, named as its own anti-pattern.</p>\n<h2 id=\"third-party-orchestration-frameworks-mostly-not-the-relevant-layer-here\">Third-party orchestration frameworks — mostly not the relevant layer here</h2>\n<p>CrewAI/AutoGen/LangGraph solve a different problem: building standalone multi-agent <em>applications</em>, not delegating a task inside a coding session.</p>\n<ul>\n<li><strong><a href=\"https://github.com/crewAIInc/crewAI\">CrewAI</a></strong> — lowest barrier, &quot;agents as roles/tasks,&quot; good for prototypes. Teams commonly graduate off it for production.</li>\n<li><strong><a href=\"https://github.com/langchain-ai/langgraph\">LangGraph</a></strong> — production-grade: durable execution, checkpointing, human-in-the-loop, <a href=\"https://www.langchain.com/langsmith\">LangSmith</a> observability. More boilerplate than a two-agent prototype needs.</li>\n<li><strong><a href=\"https://github.com/microsoft/autogen\">AutoGen</a></strong> — treat as deprecated. Microsoft moved it to maintenance mode; its own README points new users to <a href=\"https://github.com/microsoft/agent-framework\">Microsoft Agent Framework</a> instead. Don&#39;t adopt for anything new.</li>\n</ul>\n<p>None fit &quot;delegate a subtask within my coding session&quot; — that&#39;s what native subagents already do. Only relevant if a future project&#39;s actual product needs a standalone multi-agent architecture, which nothing in scope does.</p>\n<h2 id=\"sources\">Sources</h2>\n<ul>\n<li><a href=\"https://www.claude.com/blog/subagents-in-claude-code\">How and when to use subagents in Claude Code</a> — Anthropic, primary source</li>\n<li><a href=\"https://docs.claude.com/en/docs/claude-code/sub-agents\">Claude Code subagents documentation</a> — Anthropic, official reference</li>\n<li><a href=\"https://github.blog/ai-and-ml/how-we-made-github-copilot-cli-more-selective-about-delegation/\">How we made GitHub Copilot CLI more selective about delegation</a> — GitHub Engineering, primary source, A/B test numbers</li>\n<li><a href=\"https://www.itechguides.com/github-copilot-cli-create-custom-agents-and-delegate-work-to-copilot-cloud-agent/\">GitHub Copilot CLI Custom Agents and /delegate Guide</a> — secondary, corroborates <code>/delegate</code> mechanics</li>\n<li><a href=\"https://www.infoq.com/news/2026/04/subagents-gemini-cli/\">Subagents in Gemini CLI Enable Task Delegation and Parallel Agent Workflows</a> — InfoQ, corroborated by geminicli.one, pasqualepillitteri.it on version/date</li>\n<li>Multiple 2026 framework-comparison pieces (Pickaxe, Galileo, DataCamp, OpenAgents) — cross-checked, consistent on AutoGen&#39;s maintenance-mode status and LangGraph&#39;s production lean</content></li>\n</ul>\n";
