// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/prompt-engineering.md
export const HTML =
  '<h1 id="prompt-engineering-writing-the-actual-words-that-go-to-the-model">Prompt Engineering — Writing the Actual Words That Go to the Model</h1>\n<p>New topic, added 2026-09-05 on the user&#39;s call: high-impact enough to deserve its own doc, not a footnote in something else. Every single thing anyone writes to an LLM, a chat message, a system prompt, a skill file, an <code>AGENTS.md</code>, a tool description, is a prompt. This is the one technique that applies no matter what else surrounds the model: no harness, no agent loop, no tools required. It&#39;s also the cheapest thing to improve, since it costs nothing but rewriting text.</p>\n<p><strong>Status: skeleton doc.</strong> Core mechanics below are stable, well-documented practice. The resource list and technique coverage are due a proper research pass (per this repo&#39;s convention, that pass runs through cowork, not inline) before this gets marked complete.</p>\n<h2 id="what-it-actually-covers">What it actually covers</h2>\n<p>Getting the model to reliably produce what you actually want, on a single call, by controlling:</p>\n<ul>\n<li><strong>What you ask for</strong> — the literal task instruction.</li>\n<li><strong>How you show it what &quot;good&quot; looks like</strong> — examples, format constraints, tone.</li>\n<li><strong>What context you hand it</strong> — background, constraints, prior decisions (this overlaps with context engineering; prompt engineering is the wording, context engineering is the selection — see <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary#3-context-engineering--real-gap-or-already-covered"><code>harness-engineering-vocabulary.md</code></a>).</li>\n</ul>\n<h2 id="core-techniques-worth-knowing">Core techniques worth knowing</h2>\n<ul>\n<li><strong>Be explicit and specific.</strong> &quot;Write a good commit message&quot; underperforms &quot;write a commit message under 50 characters for the subject, explain why not what in the body.&quot; Vague asks get vague, average-of-training-data answers.</li>\n<li><strong>Show, don&#39;t just tell (few-shot examples).</strong> One or two concrete examples of the input/output shape you want outperforms a paragraph of abstract instruction. Diverse examples beat exhaustive edge-case lists, per Anthropic&#39;s own context-engineering guidance.</li>\n<li><strong>Give the model a role or persona when it changes the answer shape</strong>, not as decoration. &quot;You are a senior security reviewer&quot; changes what gets flagged; &quot;you are a helpful assistant&quot; changes nothing and is filler.</li>\n<li><strong>State the output format explicitly</strong> if it matters (JSON, a specific heading structure, a length limit). The model will guess a reasonable default otherwise, and reasonable defaults don&#39;t match your downstream parser.</li>\n<li><strong>Structure long or multi-part prompts</strong> with clear delimiters, Claude specifically is trained to work well with XML-style tags (<code>&lt;context&gt;</code>, <code>&lt;task&gt;</code>, <code>&lt;examples&gt;</code>) to separate sections a model might otherwise blur together.</li>\n<li><strong>Iterate like debugging, not guessing.</strong> If output is wrong, change one variable (the instruction, an example, the format spec) and re-test, rather than rewriting the whole prompt and losing track of what fixed it.</li>\n<li><strong>Ask the model to reason before answering, on hard tasks.</strong> This is where prompt engineering touches the &quot;thinking mode&quot; mechanism from <code>0-how-llms-actually-work-under-the-hood.md</code>, more room to work through a problem in text before committing to a final answer measurably helps on math/code/multi-step tasks.</li>\n</ul>\n<h2 id="where-it-stops-being-enough">Where it stops being enough</h2>\n<p>Prompt engineering optimizes one call. It has nothing to say about:</p>\n<ul>\n<li>what happens <em>before</em> the prompt is assembled, curating what data/context even makes it in, that&#39;s context engineering.</li>\n<li>what happens <em>after</em> the model responds, running tools, retrying, checking work, that&#39;s harness and loop engineering.</li>\n<li>coordinating more than one agent, that&#39;s graph engineering.</li>\n</ul>\n<p>A perfectly engineered prompt run through a bad harness still produces bad outcomes at the system level, and a mediocre prompt in a well-designed harness with verification loops can still land. This doc is the base layer, not the whole stack.</p>\n<h2 id="resources-pending-full-research-pass">Resources (pending full research pass)</h2>\n<ul>\n<li><a href="https://docs.claude.com">Anthropic&#39;s own prompt engineering documentation</a> — first-party, current, covers Claude-specific behavior (XML tags, extended thinking prompts, system prompt design).</li>\n<li>OpenAI&#39;s prompt engineering guide — first-party equivalent for GPT-family models, useful for contrast on where techniques are model-agnostic vs. provider-specific.</li>\n</ul>\n<p><strong>Open item:</strong> proper resource curation (courses, practitioner write-ups, technique comparisons across providers) is logged as pending work, not done here — see the crash-course <code>TODO.md</code>.</p>\n';
