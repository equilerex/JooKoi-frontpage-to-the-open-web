// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/session-and-token-economics.md
export const HTML =
  '<h1 id="session-token-economics-and-subagent-delegation">Session &amp; Token Economics, and Subagent Delegation</h1>\n<p>AI tools don&#39;t charge by the hour or by the word exactly — they charge by tiny chunks of text called &quot;tokens,&quot; and the more back-and-forth history and files piled into a conversation, the more tokens get processed (and paid for) every single time, even for a short question. This topic is about understanding that cost, and about the fact that a conversation left running too long can also start giving worse answers, not just cost more. Worth knowing about if you pay for AI usage or run long working sessions with an AI assistant; skip it if you only send the occasional short question and don&#39;t worry about cost.</p>\n<p>How coding agents spend tokens, what that costs, and when to compact a session, start fresh, or delegate to a subagent — plus a look at how subagent delegation is implemented across different AI coding tools.</p>\n<h2 id="section-a-tokens-and-context-from-first-principles">Section A — Tokens and context, from first principles</h2>\n<h3 id="what-is-a-token">What is a token?</h3>\n<p>A token is the unit an LLM actually reads and writes — not a word, not a character. Text gets split into subword pieces by a <a href="https://platform.openai.com/tokenizer">tokenizer</a>: common words are often one token, longer or rarer words split into two or more pieces, and punctuation/whitespace count too. Rough rule of thumb for English: <strong>~4 characters per token</strong>, or <strong>~750 words per 1,000 tokens</strong> (per <a href="https://platform.openai.com/tokenizer">OpenAI&#39;s own tokenizer guidance</a>). Code tokenizes less predictably than prose — dense symbols, indentation, and identifiers can push the ratio either way — so treat 4 chars/token as a ballpark, not a formula.</p>\n<h3 id="what-is-a-context-window">What is a context window?</h3>\n<p>The context window is the total number of tokens a model can hold and attend to in a single request — everything the model &quot;sees&quot; when it generates the next response. It&#39;s one shared budget covering:</p>\n<ul>\n<li>the system/instruction prompt (harness config, project rules, tool definitions)</li>\n<li>the full conversation history so far (every prior user and assistant turn)</li>\n<li>tool call inputs and outputs (file reads, search results, command output)</li>\n<li>the new turn itself</li>\n</ul>\n<p>If the total exceeds the model&#39;s context window, older content has to be dropped or summarized before the model can respond at all — that&#39;s what compaction (below) exists to do.</p>\n<h3 id="what-actually-fills-up-context-in-an-agent-coding-session">What actually fills up context in an agent coding session</h3>\n<p>In a typical coding-agent session, context accumulates from:</p>\n<ul>\n<li><strong>Instruction files</strong> — project-level config the harness auto-loads (e.g. a <code>CLAUDE.md</code>, <code>.cursorrules</code>, <code>.github/copilot-instructions.md</code>, an <code>AGENTS.md</code>)</li>\n<li><strong>Conversation history</strong> — every message exchanged so far, including ones from many turns ago</li>\n<li><strong>Tool outputs</strong> — the full text of every file read, every search result, every command&#39;s stdout/stderr</li>\n<li><strong>File contents</strong> — anything the agent opened to read or edit, often re-included on later turns even if unchanged</li>\n</ul>\n<p>None of this is unique to one product — every agentic coding tool (terminal-based or IDE-integrated) assembles context the same way; only the exact instruction-file name and loading rules differ.</p>\n<h3 id="why-context-size-matters">Why context size matters</h3>\n<p>Two independent effects, not one:</p>\n<ol>\n<li><strong>Cost.</strong> Providers bill by tokens processed. More accumulated context means more tokens sent on every single turn, even the ones that don&#39;t need most of that history.</li>\n<li><strong>Quality.</strong> Beyond a certain amount of accumulated context, models get measurably worse at finding and using the relevant piece of it — irrelevant or stale content dilutes attention even when it&#39;s technically still &quot;in view.&quot; This is sometimes called the <strong><a href="https://www.trychroma.com/research/context-rot">&quot;needle in a haystack&quot; problem</a></strong>: buried among enough hay, the needle gets missed even though it&#39;s physically present. <a href="https://www.trychroma.com/research/context-rot">Chroma&#39;s 2025 research</a> tested 18 frontier models and found every one degraded as input length grew — often well before hitting the model&#39;s hard context limit (e.g. meaningful degradation at 50K tokens in a 200K-token window). A related, older finding is the <a href="https://arxiv.org/abs/2307.03172">&quot;lost in the middle&quot; effect</a> (Liu et al., 2023): models retrieve information best when it&#39;s near the start or end of context, and worst when it&#39;s buried in the middle.</li>\n</ol>\n<p>The practical upshot: &quot;do I have context room left&quot; and &quot;is this session still working well&quot; are different questions, and the second one degrades first.</p>\n<h3 id="how-tokens-are-billed">How tokens are billed</h3>\n<p>Most LLM APIs bill <strong>input tokens</strong> (everything sent to the model) and <strong>output tokens</strong> (everything the model generates) separately, and output is typically priced higher per token than input — generating text costs more than reading it. Subscription products (rather than pay-per-token API access) often convert this into a request-count-equivalent or a rolling usage quota instead of exposing raw token counts, but the underlying meter is still tokens.</p>\n<h3 id="what-is-prompt-caching">What is prompt caching?</h3>\n<p>Prompt caching lets a provider reuse the already-processed version of a prefix (the earlier, unchanged part) of your input instead of reprocessing it from scratch on every call. It exists because agent sessions resend a lot of identical content turn after turn — the same system prompt, the same instruction files, the same early conversation — and reprocessing all of that from zero every time is wasteful. A cache hit is billed at a steep discount versus a fresh read (commonly on the order of a 90% discount, though exact figures and mechanisms vary by provider — see below); a cache write (the first time a prefix is cached) usually costs a small premium over normal input pricing, since the provider is doing extra work to make it reusable.</p>\n<p>Caching requires the cached portion to match <strong>exactly</strong> — any edit earlier in the prefix (a changed instruction, an added/removed tool, an image appearing) invalidates the cache from that point forward, and the next call pays full write price again.</p>\n<h3 id="what-is-compaction-or-compression">What is compaction (or &quot;compression&quot;)?</h3>\n<p>Compaction is a process — automatic in some harnesses, manually triggered in others — that summarizes or discards older conversation history to make room in the context window. Instead of carrying every prior turn verbatim, a compaction pass replaces the discarded portion with a condensed summary, freeing up space for new work.</p>\n<p><strong>What gets lost:</strong> compaction is lossy by construction. Exact wording, precise tool outputs, small caveats and edge cases mentioned once, and any decision the summarizer didn&#39;t judge important enough to keep can all disappear. A compacted session remembers the gist of what happened, not the transcript.</p>\n<h3 id="what-happens-on-a-fresh-session">What happens on a fresh session?</h3>\n<p>Starting a new session drops all prior context entirely, unless something was deliberately saved outside the conversation (a file, a memory entry, a written spec). The upside: no inherited cost, no accumulated stale context, no risk of the &quot;needle in a haystack&quot; effect from unrelated earlier work. The downside: anything that mattered from the old session and wasn&#39;t written down is gone.</p>\n<h2 id="section-b-why-growing-context-compounds-cost-and-risk">Section B — Why growing context compounds cost and risk</h2>\n<p>Put simply, in mechanical terms:</p>\n<ol>\n<li><strong>More accumulated context costs more per turn</strong>, even with caching. Caching lowers the <em>rate</em> on the cached prefix (a steep discount, not a 100% discount), but every turn still pays full price for whatever&#39;s new since the last cached point, and the cache-rate charge still scales with however much history is being reused. A session ten times longer costs meaningfully more per turn than a fresh one, cached or not.</li>\n<li><strong>Stale or irrelevant context isn&#39;t just a cost problem — it&#39;s a quality problem.</strong> Per the needle-in-a-haystack research above, padding context with things the model doesn&#39;t need can make it worse at using the things it does need, independent of what it costs.</li>\n<li><strong>Caching only protects the part that stays identical.</strong> Any edit upstream of the cache point (a changed file, a new tool, a different setting) invalidates the cached prefix and forces a full-price reprocess — so a long, varied session is more exposed to unexpectedly losing its discount than a short, stable one.</li>\n<li><strong>A fresh session avoids both problems (cost growth and context rot) but loses whatever wasn&#39;t externalized.</strong> It&#39;s a clean reset, not a free one — the new session has to re-acquire any state that mattered.</li>\n<li><strong>Compaction is the middle ground.</strong> It keeps a compressed version of what happened so a session can keep going without paying full cost for the entire history — at the price of losing detail a plain summary won&#39;t preserve.</li>\n</ol>\n<h2 id="section-c-decision-guide-continue-compact-restart-or-delegate">Section C — Decision guide: continue, compact, restart, or delegate</h2>\n<p>Rough scale, so the numbers below mean something: <strong>~20K tokens</strong> is roughly a few long files or a half-hour of back-and-forth conversation; <strong>~100K tokens</strong> is roughly a small-to-medium codebase&#39;s worth of source files, or a long multi-hour debugging session; <strong>~200K tokens</strong> (a common context window ceiling as of 2026) is roughly a large module or several dozen files — the point past which most sessions are deep into needle-in-a-haystack territory even if the hard limit hasn&#39;t been hit.</p>\n<table>\n<thead>\n<tr>\n<th>Option</th>\n<th>Trigger</th>\n<th>Benefit</th>\n<th>Cost / risk</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Continue as-is</strong></td>\n<td>Session is short-to-medium, task is still tightly scoped, no signs of the model losing track</td>\n<td>No overhead, cache discount stays intact</td>\n<td>Cost and rot-risk both creep up the longer this goes on</td>\n</tr>\n<tr>\n<td><strong>Compact the session</strong></td>\n<td>Mid-task, earlier turns hold decisions/constraints still needed, no external doc already captures them</td>\n<td>Frees context room without losing the whole session; cheaper than restarting from scratch on a live task</td>\n<td>Loses exact wording, exact tool output, anything the summarizer didn&#39;t flag as important</td>\n</tr>\n<tr>\n<td><strong>Start a fresh session</strong></td>\n<td>Everything that matters is already in files/memory/a written spec; the conversation itself isn&#39;t the source of truth for anything</td>\n<td>Zero inherited cost, no accumulated rot, clean slate</td>\n<td>New session must re-read whatever carries the state; anything left only in chat is gone</td>\n</tr>\n<tr>\n<td><strong>Delegate to a subagent</strong></td>\n<td>A subtask needs a lot of exploration/reading/searching whose raw output the parent session will never need again (e.g. &quot;find where X is implemented,&quot; &quot;read these 10 files and summarize&quot;)</td>\n<td>Parent session only pays for the dispatch prompt and the final report — the subagent&#39;s own reading/searching stays out of the parent&#39;s growing context entirely</td>\n<td>Total tokens across the whole operation aren&#39;t reduced — the subagent still burns its own (separate, non-cache-shared) tokens; only the parent&#39;s accumulated context is spared</td>\n</tr>\n</tbody></table>\n<p>None of these is free — the right choice depends on where the state that matters currently lives, and whether the immediate need is &quot;keep this exact conversation going&quot; or &quot;get an answer without polluting it.&quot;</p>\n<h2 id="section-d-seeing-your-own-usage">Section D — Seeing your own usage</h2>\n<p>How to actually check what a session is costing, from cheapest/simplest to most detailed. Categories are generic — specifics vary by harness.</p>\n<ul>\n<li><strong>Built-in status/usage commands.</strong> Most terminal-based coding agents expose a slash or status command that shows current context/token usage in-session — e.g. <code>/context</code> or <code>/status</code> in Claude Code, or a usage panel in Gemini CLI. Check the specific harness&#39;s docs for the exact command; the capability itself is near-universal.</li>\n<li><strong>IDE status displays and extensions.</strong> IDE-integrated tools (Copilot, Cursor, Cline, etc.) commonly surface a running token/request count in a status bar or sidebar panel rather than a slash command, since there&#39;s no terminal prompt to type into.</li>\n<li><strong>Local session logs.</strong> Most harnesses write a session transcript or log to disk as you work — useful for after-the-fact inspection (what actually got sent, how big it was) even without a live dashboard.</li>\n<li><strong>Global per-harness state folders.</strong> Session state, history, and config commonly live in a per-user folder — e.g. <code>~/.claude/</code> for Claude Code, <code>~/.gemini/</code> for Gemini CLI, <code>~/.copilot/</code> or the VS Code extension&#39;s global storage for Copilot. These folders are the raw material for any custom analysis, since they hold the actual logged sessions.</li>\n<li><strong>External local-first analytics tooling.</strong> <a href="https://github.com/rajbos/ai-engineering-fluency">AI Engineering Fluency</a> is a concrete example: it&#39;s an open-source VS Code/JetBrains extension (also with a CLI component) that reads local Copilot/agent usage data and displays it as a status-bar token counter (today&#39;s usage plus a rolling 30-day total) alongside a broader &quot;fluency score&quot; dashboard covering how the tool is used — modes, tool calls, context references, MCP tool usage. It works across Chromium-based VS Code forks (VS Code, Cursor, Windsurf, VSCodium, and others). All data is read and stored locally — nothing is sent to a remote service — which is what makes it a &quot;local-first&quot; example rather than a hosted analytics product.</li>\n<li><strong>Custom scripts.</strong> Since session logs are usually just structured local files (JSON or JSONL in most harnesses), a short script that parses them and sums token counts per session/day is often the fastest path to a personalized view, if the built-in status output isn&#39;t granular enough.</li>\n</ul>\n<!-- VERIFY: exact log file formats and paths for each harness (Claude Code, Gemini CLI, Copilot CLI/extension) drift with releases — check current docs before relying on a specific path. --><h2 id="section-e-subagent-delegation-across-coding-tools">Section E — Subagent delegation across coding tools</h2>\n<p>Subagents run their own dispatch with a separate, fresh context window from the parent session. The parent pays for the dispatch prompt and whatever report comes back — not for what the subagent internally read, searched, or generated. This is the mechanism Section C&#39;s &quot;delegate to a subagent&quot; row describes: total tokens spent across the whole operation aren&#39;t reduced, but the parent session&#39;s own growing context — the thing that drives Section B&#39;s cost/quality effects — is spared.</p>\n<p>This pattern is now implemented, not experimental, across multiple mainstream tools:</p>\n<ul>\n<li><strong>Claude Code</strong> supports subagents and forked agents with independent context, invoked either explicitly or (in agentic use) proactively by the model itself when a task looks like it would flood the parent session with content that won&#39;t be needed again.</li>\n<li><strong>GitHub Copilot CLI</strong> has both automatic in-session delegation and an explicit handoff (<code>/delegate</code> or similar) to a cloud-hosted background agent that works independently and opens a draft PR when done.</li>\n<li><strong>Gemini CLI</strong> shipped native subagents (own instructions, own context, restricted tool access, parallel execution) as of its v0.36 release in 2026, per the <a href="https://developers.googleblog.com/en/subagents-have-arrived-in-gemini-cli/">Google Developers Blog announcement</a>.</li>\n</ul>\n<p>Where these tools differ is administrative control, not whether the underlying capability exists: enterprise policies can restrict which model a subagent may use (falling back to the parent&#39;s permitted model if blocked), or gate a feature behind an opt-in flag during early rollout. Neither of those removes subagent delegation as a capability — they constrain how it&#39;s configured in a managed environment. Any specific difference in how <em>readily</em> a given tool chooses to delegate on its own (versus waiting to be asked) is more a matter of product tuning/maturity than a hard architectural gap, and isn&#39;t something with a clean, citable head-to-head benchmark as of this writing.</p>\n<!-- ASIDE: corporate/managed-environment audits of specific tool configurations (which features are enabled/disabled under a specific org\'s policy) are environment-specific and not representative of the product\'s general capability — worth checking your own environment\'s admin settings directly rather than assuming from a doc. --><h2 id="open-questions-weak-sourcing">Open questions / weak sourcing</h2>\n<ul>\n<li>Delegation &quot;proactiveness&quot; (how eagerly a tool decides to spawn a subagent unprompted) has no found head-to-head benchmark across tools — treated as inference from feature-maturity timing, not a citable claim.</li>\n<li>Context-rot&#39;s exact onset point is task- and model-dependent; the <em>existence</em> of the effect is well-supported (see Chroma&#39;s research above), but any specific token threshold quoted for a specific model should be re-verified against current benchmarks before being treated as fixed.</li>\n<li>Exact cache-discount percentages and mechanisms differ by provider and change over time — treat the ~90%/10-cent-on-the-dollar figures above as the general shape, not a locked-in number; check the relevant provider&#39;s current pricing docs before doing real cost math.</li>\n</ul>\n';
