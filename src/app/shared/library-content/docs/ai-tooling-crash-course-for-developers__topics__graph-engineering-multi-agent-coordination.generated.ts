// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/graph-engineering-multi-agent-coordination.md
export const HTML =
  '<h1 id="graph-engineering-multi-agent-coordination-and-knowledge-graphs">Graph Engineering — Multi-Agent Coordination and Knowledge Graphs</h1>\n<p>Surfaced 2026-09-05, from a review of doc 0&#39;s terminology. Distinct from <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> on purpose: a harness is the software wrapped around one model call (tools, memory, retries), and a loop is one agent repeating that call toward a goal. Graph engineering sits a level above both — it coordinates multiple agents or loops, or structures data as an explicit network — and works the same whether the agents underneath run on a heavy harness, a thin one, or none at all. It doesn&#39;t need a harness to exist as a concept, which is why it doesn&#39;t live inside that doc.</p>\n<p>Two distinct things travel under the same name, worth keeping separate:</p>\n<h2 id="1-agent-coordination-graphs-execution-graphs">1. Agent coordination graphs (execution graphs)</h2>\n<p>Nodes are agents or deterministic code steps (a Planner, a Researcher, an Evaluator). Edges are the routing between them: sequential, conditional, or parallel. A shared state object travels along the edges and gets read/modified by each node as work passes through.</p>\n<p>Example shape: route a prompt to a Researcher node, pass its output to a Skeptic node for review, use a conditional edge to either loop back for more research or advance to a Synthesizer node for final output. Making the routing explicit stops agents from looping without structure or silently dropping context between handoffs.</p>\n<p><a href="https://www.langchain.com/blog/3-years-of-graph-engineering-with-langgraph">LangGraph</a> is the named framework here, and the one part of this concept with an actual checkable source (LangChain&#39;s own blog, not just secondary write-ups). This is the same shape as this repo&#39;s own fork-dispatch-and-merge pattern described in <a href="/library/ai-tooling-crash-course-for-developers/topics/subagents-and-delegation"><code>subagents-and-delegation.md</code></a>: a primary agent dispatches bounded work to specialized agents and integrates their results. LangGraph formalizes that as an explicit graph instead of an implicit one baked into the conversation flow.</p>\n<h2 id="2-knowledge-retrieval-graphs">2. Knowledge / retrieval graphs</h2>\n<p>A different sense of the same term: storing information as explicit typed relationships instead of flat text chunked for similarity search.</p>\n<table>\n<thead>\n<tr>\n<th>Vector RAG</th>\n<th>Knowledge graph</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Finds semantically similar passages</td>\n<td>Traverses explicit, typed relationships</td>\n</tr>\n<tr>\n<td>Good for single-document answers</td>\n<td>Good for multi-hop questions across scattered facts</td>\n</tr>\n<tr>\n<td>Cheaper, faster for one-off queries</td>\n<td>More expensive to build and maintain</td>\n</tr>\n</tbody></table>\n<p>GraphRAG fuses the two: a model browses a structured graph to synthesize answers that need linking multiple disparate facts, not just retrieving one similar passage.</p>\n<p>This sense connects to <a href="/library/ai-tooling-crash-course-for-developers/topics/dev-scoped-second-brain-rag"><code>dev-scoped-second-brain-rag.md</code></a>, which currently covers only vector RAG over a personal codebase/notes. The graph-based alternative isn&#39;t covered there yet — worth a follow-up pass if that doc gets revisited.</p>\n<h2 id="why-its-getting-named-now">Why it&#39;s getting named now</h2>\n<p>As multi-agent setups scale, an ad hoc chain of prompts and loose tool calls gets hard to reason about: which agent is allowed to do what, where authority transitions happen, what happens on failure. An explicit graph makes those trust boundaries and failure paths visible artifacts instead of something buried in a long conversation transcript.</p>\n<h2 id="sourcing-note-flagged-per-this-repos-evidence-bar">Sourcing note, flagged per this repo&#39;s evidence bar</h2>\n<p>&quot;Graph engineering&quot; as a named discipline is a viral mid-2026 term, and most write-ups describing it (blog aggregators, X posts, YouTube explainers) don&#39;t meet the checkable-source bar on their own. LangGraph itself is real and checkable — an actual framework with real docs and adoption. The broader &quot;prompt → context → harness → loop → graph&quot; stack framing some sources attach to it is a labeled new-but-promising exception, not an established consensus term. Treat the framework as solid, the taxonomy around it as provisional. No pick made here — this is a definitions/orientation doc, not a recommendation to adopt LangGraph or any specific graph framework.</p>\n';
