// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/README.md
export const HTML =
  '<h1 id="topic-index-whats-actually-agreed-on-2026">Topic Index — What&#39;s Actually Agreed On (2026)</h1>\n<p>A survey, not a shopping list. Each topic below states what&#39;s settled vs. contested in the field, then links to the deep-dive doc(s) that cover it in this repo — or says plainly if no deep-dive exists yet. Personal reactions/mark-labels from the original pass are kept separately in <code>local/stage-0-raw-notes.md</code> (gitignored, not shareable content — this file is the shareable version).</p>\n<hr>\n<h2 id="1-contextmemory-files">1. Context/memory files</h2>\n<p>Every major agentic coding tool now reads a plain-markdown instruction file at project root before acting: <code>CLAUDE.md</code> (Claude Code), <code>AGENTS.md</code> (cross-tool convention, adopted by Codex CLI, Cursor, others), <code>.github/copilot-instructions.md</code> (Copilot). Settled pattern: keep it short, pointers not prose, and treat provider-specific files as thin wrappers around one provider-agnostic source (<code>AGENTS.md</code>) rather than duplicating content per tool. <code>AGENTS.md</code> is the primary source of truth, not <code>CLAUDE.md</code> — provider-specific content is the exception, only for what a tool genuinely can&#39;t get any other way.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/base-instruction-files"><code>base-instruction-files.md</code></a> — governance/adoption verification, per-provider loading mechanics, the (contradicted) 200-line-length guidance, nested/local-override patterns, ADR-as-context status, drift tooling, and anti-patterns including a confirmed prompt-injection vector via cloned repos&#39; own AGENTS.md files. No pick made.</p>\n<p><em>Not the same topic as #10 — that&#39;s session/progress ledgers, cross-project memory of decisions. This is the base instruction file itself. Open item: <code>~/.claude/CLAUDE.md</code> hasn&#39;t been reviewed against this doc&#39;s findings yet, see <code>_architecture/BACKLOG.md</code>. Also open: no dedicated doc yet for &quot;per-feature/per-quirk architectural context&quot; — related but unresolved by <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> and <a href="/library/ai-tooling-crash-course-for-developers/topics/personal-harness-architecture"><code>personal-harness-ARCHITECTURE.md</code></a>, blocked pending a pick from item 10.</em></p>\n<h2 id="2-prompt-skill-libraries">2. Prompt &amp; skill libraries</h2>\n<p>Anthropic&#39;s <code>SKILL.md</code> format (a folder with a markdown file plus optional scripts/resources, progressively disclosed into context only when triggered) is a de facto standard for Claude Code and is being mirrored by other tools&#39; &quot;custom command&quot; systems. The unsettled part: whether skills should be personal, project-local, or org-distributed as plugins — practice varies by how much the author expects reuse.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/skill-libraries-and-marketplaces"><code>skill-libraries-and-marketplaces.md</code></a> — spec state, anti-patterns, marketplaces, individual atomic skills vs. libraries. Reference lists: <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a>.</p>\n<h2 id="3-session-token-economics">3. Session &amp; token economics</h2>\n<p>Consensus: long-running sessions degrade output quality (&quot;context rot&quot;) well before they hit the hard token limit, because irrelevant history competes for the model&#39;s attention on every turn. Agreed mitigations: one coherent unit of work per session, proactive <code>/compact</code>/<code>/clear</code>, and prompt caching (rewarding session <em>shape</em> — stable prefix, volatile content appended — over raw brevity). Still evolving: automated context-management hooks that decide <em>for</em> you when to compact.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/session-and-token-economics"><code>session-and-token-economics.md</code></a> — caching cost mechanics, compaction vs. fresh, subagent token impact by provider (including a firsthand corporate-environment audit).</p>\n<h2 id="4-subagents-multi-agent-delegation">4. Subagents / multi-agent delegation</h2>\n<p>Agreed use case: delegating work whose <em>intermediate</em> tool output you don&#39;t need to keep — broad exploration, isolated research, parallel independent tasks. Agreed anti-pattern (debated in degree, not direction): using multiple agents as a substitute for review quality (&quot;ask 3 agents, take the majority&quot;) — correlated error, not independent verification. The distinction: <em>fresh-context subagent for scope</em> (well-supported) vs. <em>multi-agent voting for confidence</em> (contested).</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/subagents-and-delegation"><code>subagents-and-delegation.md</code></a> — cross-provider status, bounded-roles-vs-swarms pattern.</p>\n<h2 id="5-mcp-model-context-protocol">5. MCP (Model Context Protocol)</h2>\n<p>Adoption moved from experimental to mainstream fast: by mid-2026, MCP-backed agents are in production at the large majority of enterprise AI teams tracked in industry surveys. In restricted corporate environments, the documented workaround is packaging the same capability as an Agent Skill that wraps a direct API/CLI call instead of running an MCP server. Three-primitive framing gaining traction: <strong>Skills</strong> for reusable capability + know-how, <strong>MCP</strong> for governed live connectivity, <strong>CLI/local execution</strong> underneath both.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol"><code>mcp-model-context-protocol.md</code></a> — servers, security, CLI-vs-MCP tradeoff. Related: <a href="/library/ai-tooling-crash-course-for-developers/topics/protocol-landscape-acp-a2a"><code>protocol-landscape-acp-a2a.md</code></a> (how MCP relates to ACP/A2A).</p>\n<h2 id="6-hooks-automation-triggers">6. Hooks / automation triggers</h2>\n<p>Settled as a category: hooks (shell commands firing on tool-call, session-start, session-end, etc.) are the accepted mechanism for deterministic guardrails an LLM shouldn&#39;t be trusted to self-enforce. Not contested that they&#39;re useful; what&#39;s a matter of personal taste is <em>how much</em> automation to hang off them.</p>\n<p><em>No dedicated deep-dive doc yet — a lighter-pass topic, not yet researched beyond this baseline note.</em></p>\n<h2 id="7-local-model-use">7. Local model use</h2>\n<p>Converging, previously contested: for a single consumer GPU in the 8GB class, the realistic role is opportunistic — batch/offline jobs where latency doesn&#39;t matter — not a default coding-assistant tier. Frontier cloud models still substantially outperform anything that fits in 8GB VRAM for actual coding/reasoning work.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/local-model-hardware-fit"><code>local-model-hardware-fit.md</code></a> — hardware fit, quantization tradeoffs, Ollama vs. alternatives.</p>\n<h2 id="8-review-discipline">8. Review discipline</h2>\n<p>Agreed: self-review by the same model/context that wrote the code is weak evidence, and multi-agent &quot;consensus&quot; review is correlated rather than independent for the same reason as item 4. The stronger pattern — sometimes called a cold-start or fresh-context critic — is a separate agent with no shared conversation history, forced into a typed verdict, ideally from a different model family. Demanding pasted command output over claimed correctness is close to universal agreement.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/review-and-verification-tooling"><code>review-and-verification-tooling.md</code></a> — off-the-shelf/IDE-native review tooling, open-source review agents.</p>\n<h2 id="9-dotfiles-portability">9. Dotfiles / portability</h2>\n<p>The mainstream answer is a dedicated dotfile manager (chezmoi being the most-referenced, with GNU Stow and plain symlink scripts as lighter alternatives), trading a steeper tool for templated per-machine divergence.</p>\n<p><strong>Decided against, 2026-09-01 — see <a href="../_architecture/plans/decisions/009-no-binary-dependencies.md"><code>decisions/009-no-binary-dependencies.md</code></a>.</strong> No dotfile manager, no binary, no install step anywhere in the shippable layer. A binary fails on a locked corporate machine, which is exactly where the stack has to work, and the divergence problem it solves is already handled by the <code>_jookoi-</code> prefix plus one vault per environment. Applying the stack is a manual copy.</p>\n<p><em>No dedicated deep-dive doc — the pick closed the topic rather than a research pass doing it.</em></p>\n<h2 id="10-personal-cross-project-memoryledger">10. Personal cross-project memory/ledger</h2>\n<p>No single agreed tool here — a real gap, not just an unresearched one. Candidates named: Claude Code&#39;s own built-in memory system, plain dated journal files, a personal notes vault (Obsidian-class), periodic extraction scripts mining AI session transcripts, gitignored per-repo notes.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/memory-and-progress-ledgers"><code>memory-and-progress-ledgers.md</code></a> — full evaluation of all candidates, no pick made yet. <strong>2026-08-23 addition:</strong> storage/vault-layer research for the corporate-environment-viable design (§8 of that doc) — most named vault products fail the corporate no-native-binaries/no-MCP filter; the closest named prior art is the Cline-style &quot;Memory Bank&quot; pattern, not a vault product.</p>\n<h2 id="11-usage-analytics-self-assessment-tooling">11. Usage analytics / self-assessment tooling</h2>\n<p>Confirmed: <strong>AI Engineering Fluency</strong> (VS Code extension, author Rob Bos / rajbos) — reads local session logs from VS Code, Copilot CLI, Claude Code, Gemini CLI, Cursor and others, surfaces token usage/cost/usage-pattern insights in the editor status bar, local-first. Beyond this one extension, the category is small and not yet consolidated.</p>\n<p><em>No dedicated deep-dive doc — confirmed via this baseline pass, open follow-up (more options in this category) not yet pursued.</em></p>\n<h2 id="12-security">12. Security</h2>\n<p>The volume of new repos/scripts/skills available makes it tempting to just install and use things, but shipping malicious code inside them is easier than ever — closer to old-school piracy risk (grab a pile of loot, find a surprise inside) than typical package-manager trust.</p>\n<p>Deep dive: <a href="/library/ai-tooling-crash-course-for-developers/topics/security-and-supply-chain"><code>security-and-supply-chain.md</code></a> — verification checklist, real 2026 incidents (ClawHub/OpenClaw, Snyk ToxicSkills), low-effort solo-dev practices.</p>\n<h2 id="13-frontend-debugging-browser-interaction-ai-integration-tooling">13. Frontend debugging, browser interaction, AI integration tooling</h2>\n<p><em>No dedicated stage-1 deep-dive doc — this ground was covered instead through direct verification passes on user-supplied tool lists. See <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a> → &quot;Design-to-code / browser verification tooling&quot; (chrome-devtools-mcp, browser-use, Skyvern, Stagehand, etc.).</em></p>\n<h2 id="14-ide-inline-completion-and-general-tooling-webstorm-vs-code">14. IDE inline completion and general tooling (WebStorm / VS Code)</h2>\n<p><em>No dedicated deep-dive doc yet — a lighter-pass topic, not yet researched beyond this baseline note.</em></p>\n<hr>\n<h2 id="surfaced-later-not-part-of-the-original-14-topics">Surfaced later (not part of the original 14 topics)</h2>\n<p>These emerged mid-process from later research/conversations, not the original Stage 0 pass:</p>\n<ul>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> — harness engineering, context engineering, repository legibility as a connected vocabulary/completeness pass. Agent loops split out separately, see below.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/loop-engineering-agent-loops"><code>loop-engineering-agent-loops.md</code></a> — building an agent loop that actually finishes instead of drifting or self-reporting false completion.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/prompt-engineering"><code>prompt-engineering.md</code></a> — the base layer underneath every other topic here: writing the actual words sent to the model. Skeleton doc, resource curation still pending.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/personal-harness-architecture"><code>personal-harness-ARCHITECTURE.md</code></a> — concrete build proposals: deterministic context bundling, capability lifecycle management, task state as a dependency graph, async/non-blocking long-running commands.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/protocol-landscape-acp-a2a"><code>protocol-landscape-acp-a2a.md</code></a> — ACP, A2A, CLI-vs-MCP for local capabilities.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/agent-sandboxing"><code>agent-sandboxing.md</code></a> — code-execution sandboxing, Claude Code&#39;s <code>/sandbox</code> command.</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/dev-scoped-second-brain-rag"><code>dev-scoped-second-brain-rag.md</code></a> — RAG over your own codebase/ADRs/notes (not general life-organization).</li>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/graph-engineering-multi-agent-coordination"><code>graph-engineering-multi-agent-coordination.md</code></a> — coordinating multiple agents/loops as an explicit node/edge graph (LangGraph), and the separate knowledge-graph-vs-vector-RAG sense of the term. Newer, less-settled terminology, sourcing flagged as provisional.</li>\n<li>Coding-agent harness/model landscape — kept as reference tables in <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a>, not a separate topic doc (no distinct educational content beyond listings).</li>\n<li><a href="./topics/stack-distribution-ARCHITECTURE.md"><code>stack-distribution-ARCHITECTURE.md</code></a> — how the shippable stack layer (<code>AGENTS.md</code>/<code>skills/</code>/<code>prompts/</code>/<code>utility-scripts/</code>) should be organized and distributed: skill-sync mechanism, drop-in-folder vs. installer, root-<code>AGENTS.md</code> collision risk. No pick made — requirements + open research questions only, 2026-08-27.</li>\n<li><a href="./_architecture/plans/2026-08-30-jookoi-paper-trail.md"><code>../../_architecture/plans/2026-08-30-jookoi-paper-trail.md</code></a> — the memory system: per-project context file set, feature-local <code>CONTEXT.md</code>, and a personal vault repo for repos where committing isn&#39;t allowed. Not a researched topic doc — the user&#39;s own design, with a prior-art scan (2026-08-27) and a full spec (2026-08-30). Lives in <code>_architecture/plans/</code>, not here, deliberately.</li>\n</ul>\n<h2 id="surfaced-from-cross-repo-consolidation-not-part-of-the-original-14-added-2026-08-23">Surfaced from cross-repo consolidation (not part of the original 14, added 2026-08-23)</h2>\n<ul>\n<li><a href="/library/ai-tooling-crash-course-for-developers/topics/planning-before-implementation"><code>planning-before-implementation.md</code></a> — evidence-based planning methodology: when to skip planning, elicitation, decomposition, critique (cold-start review, premortem), when to skip heavyweight spec-driven ceremony. Ported from the old Cowork-session location — this was the one file that never made it across when the projects split. See <code>decisions/004-cross-repo-consolidation-plan.md</code>.</li>\n</ul>\n<h2 id="surfaced-2026-08-23-no-deep-dive-yet-where-agents-actually-work-global-folders-session-logs-temp-worktrees">Surfaced 2026-08-23, no deep-dive yet: where agents actually work — global folders, session logs, temp worktrees</h2>\n<p>Distinct topic, flagged by the user, not yet researched. Coding agents don&#39;t only touch the project directory — they keep session logs/temp files/state in global per-tool folders outside the repo (e.g. <code>~/.claude/</code>), and some workflows have an agent do its actual work in a temporary directory or git worktree entirely outside the visible project, branching and merging back later. This is real &quot;AI basics&quot; territory — someone new to agentic tooling needs to know this is happening, where to look, and what it means for review/trust. Likely touches token-efficiency and session-economics topics (3) but is its own subject, not a subset. <strong>Do not start researching this yet — log only.</strong> See <code>_architecture/BACKLOG.md</code> for the scoped follow-up task.</p>\n<h2 id="surfaced-2026-08-23-no-deep-dive-yet-scanningverifying-third-party-skills-and-repos-for-malicious-content">Surfaced 2026-08-23, no deep-dive yet: scanning/verifying third-party skills and repos for malicious content</h2>\n<p>Distinct from topic 12 (Security) above, which covers verification <em>practice</em> and named incidents generally. This is narrower and more concrete: an actual tool/process pick for scanning a specific skill, plugin, or repo for malicious prompts/code before adopting it — not yet researched as its own question. Check <code>security-and-supply-chain.md</code> when it&#39;s reviewed (still unread) for whether it already answers this; if not, this needs its own research pass.</p>\n<p><strong>Resolved 2026-08-23 (cowork research pass):</strong> deep-dive doc now exists — <a href="/library/ai-tooling-crash-course-for-developers/topics/skill-scanning-and-verification"><code>skill-scanning-and-verification.md</code></a>. Confirms <code>security-and-supply-chain.md</code> does not already answer this (that doc covers general practice/incidents, not a specific scanning tool/process). Key finding: purpose-built scanners are now plentiful (NVIDIA SkillSpector, Snyk agent-scan, Cisco skill-scanner) but largely disagree with each other and are evadable — treat any single scan result as one weak signal, not a verdict. No pick made.</p>\n<h2 id="topics-to-add">Topics to add:</h2>\n<ul>\n<li>0 how do llm&#39;s actually function under the hood explained semi-eli5 style ? (way too many people, incl. devs not understanding the topic)</li>\n</ul>\n<hr>\n<h2 id="sources">Sources</h2>\n<ul>\n<li>Anthropic Claude Code documentation and this session&#39;s own harness behavior (memory system, hooks, subagents, SKILL.md, prompt caching) — primary source for items 1–6, 8.</li>\n<li>Industry survey coverage of MCP enterprise adoption, July 2026 (andrew.ooo, digitalapplied.com, a2a-mcp.org roadmap coverage) — item 5.</li>\n<li>Xebia engineering blog and DevOps Journal (2026) on the &quot;AI Engineering Fluency&quot; VS Code extension by Rob Bos (rajbos) — item 11.</li>\n<li>chezmoi project (public repo, active maintenance, cross-platform templating) — item 9&#39;s baseline. Evaluated and rejected, see <code>decisions/009-no-binary-dependencies.md</code>.</li>\n</ul>\n';
