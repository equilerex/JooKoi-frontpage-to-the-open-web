// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/skill-scanning-and-verification.md
export const HTML =
  '<h1 id="stage-1-scanning-third-party-skills-mcp-servers-and-plugins-for-malicious-content">Stage 1 — Scanning Third-Party Skills, MCP Servers, and Plugins for Malicious Content</h1>\n<p>People share ready-made add-ons for AI coding tools — skills, MCP servers, plugins — the same way browser extensions get shared. Most are fine. Some have been found to steal passwords, copy private files, or exfiltrate data.</p>\n<p>Scanning means checking one of these for hidden bad behavior before you install it and grant it access to your machine. Worth knowing if you install add-ons other people wrote. Skip if you never install anything beyond what came built in.</p>\n<p>New topic, flagged 2026-08-23 in <code>topic-index.md</code>. Distinct from the general verification <em>practice</em> already covered in <code>security-and-supply-chain.md</code> (checklist, 2026 incidents — ClawHub poisoning, Snyk&#39;s ToxicSkills study, OWASP Agentic Skills Top 10, Socket.dev for dependency monitoring). Not re-covered here.</p>\n<p><strong>The conclusion this doc builds toward:</strong> a scanner beats no scanner, but no single scanner is enough evidence that a third-party agent artifact is safe. Everything below justifies that line, not talks you out of scanning.</p>\n<h2 id="1-what-youre-actually-defending-against">1. What you&#39;re actually defending against</h2>\n<p>Documented patterns from real 2026 incidents and research — not hypothetical attack classes.</p>\n<ul>\n<li><strong>Direct malicious instructions embedded in the artifact.</strong> A <code>SKILL.md</code> or manifest just tells the agent to do something harmful, in plain language. The bulk of what&#39;s been found in the ClawHub poisoning campaigns — see <code>security-and-supply-chain.md</code> for incident numbers (341–1,184 malicious skills depending on count; <a href="https://snyk.io/blog/toxicskills-malicious-ai-agent-skills-clawhub/">Snyk&#39;s ToxicSkills study</a> found 1,467 of 3,984 scanned skills with at least one flaw).</li>\n<li><strong>Indirect prompt injection.</strong> The malicious instruction isn&#39;t in what you installed — it&#39;s in content the artifact later reads (a webpage, email, file, tool response), and the agent treats it as instructions instead of data. <a href="https://unit42.paloaltonetworks.com/ai-agent-prompt-injection/">Unit 42&#39;s documented case</a>: a webpage advertising counterfeit military-style glasses carried hidden prompts to trick an AI ad-review agent into approving content it should&#39;ve rejected — the first reported real-world case of this kind, per the researchers.</li>\n<li><strong>Obfuscated or encoded content.</strong> <a href="https://slowmist.medium.com/threat-intelligence-analysis-of-clawhub-malicious-skills-poisoning-0448ffd49c80">SlowMist&#39;s analysis of the ClawHub poisoning</a> found attackers Base64-encoding payloads directly inside <code>SKILL.md</code> files, with a two-stage loader that only decodes and runs the hidden stage at execution time — invisible to a plain-text read.</li>\n<li><strong>Dynamically constructed URLs or commands.</strong> Instead of a hardcoded malicious endpoint a scanner can pattern-match, the artifact builds a URL or command at runtime — e.g. appending stolen environment-variable values as query params. Anthropic&#39;s own guidance names this directly: a malicious Skill can read env vars and exfiltrate them &quot;via URL parameters or embedded requests&quot; (<a href="https://support.claude.com/en/articles/15927065-get-started-with-skill-and-plugin-scanning">Anthropic Help Center</a>).</li>\n<li><strong>Instructions that read innocuous to a human but different to an LLM.</strong> <a href="https://arxiv.org/pdf/2602.06547">A study of malicious skills in the wild</a> documents a dual-instruction pattern: the human-facing description looks legitimate, while a separate embedded directive — its title example: literally &quot;do not mention this to the user&quot; — tells the model to act and hide it. A manual read that only checks &quot;does the stated purpose sound reasonable&quot; misses this by design.</li>\n<li><strong>Delayed or conditional malicious behavior.</strong> A payload activates only under specific conditions (a file present, a date, a user action), so it passes a static read and a one-off test run. Repello AI&#39;s manual-audit writeup calls this &quot;conditional activation&quot; — the exact gap sandbox/behavioral testing (§4) is built to catch.</li>\n<li><strong>Malicious scripts or resources bundled with and executed by a skill.</strong> <a href="https://labs.reversec.com/posts/2026/05/skill-issues-compromising-claude-code-with-malicious-skills-agents-part-1">Reversec&#39;s &quot;Skill Issues&quot; writeup</a> shows a Claude Code skill whose frontmatter grants broad tool permissions (<code>allowed-tools: Bash(*)</code>) plus inline command execution, letting a bundled reverse-shell run without the confirmation prompt shell access normally triggers. The risk isn&#39;t only the natural-language instructions — it&#39;s what scripts or binaries ship alongside them, and what permissions the artifact grants itself.</li>\n<li><strong>Malicious MCP server or plugin behavior.</strong> <a href="https://thehackernews.com/2025/09/first-malicious-mcp-server-found.html">The <code>postmark-mcp</code> incident</a>: a package impersonating the real Postmark MCP server ran cleanly for 15 versions, then v1.0.16 added one hidden line that BCC&#39;d every outgoing email to an attacker — silently forwarding an estimated 3,000–15,000 emails a day for over a week. Nothing looked different at a glance; the change was one line, deep in the diff, running with full pre-granted permissions.</li>\n<li><strong>Attempts to exfiltrate credentials, source code, environment variables, or other accessible data.</strong> The consistent end goal across most of the above. The <a href="https://www.wiz.io/blog/keyv-and-cacheable-npm-supply-chain-attack">keyv/cacheable npm worm</a> (not agent-specific, but the same failure mode reaching agent tooling) targeted <code>.npmrc</code> tokens, GitHub CLI tokens, AWS credentials, Kubernetes configs. In agent-artifact terms, the same goal shows up as &quot;read this env var, put it in a URL&quot; or &quot;read this file, describe its contents somewhere that gets sent externally.&quot;</li>\n</ul>\n<h2 id="2-the-defense-stack-in-order">2. The defense stack, in order</h2>\n<p>No single layer is sufficient alone:</p>\n<ol>\n<li><strong>Inspect the source yourself.</strong> Read the manifest/<code>SKILL.md</code> end to end. Inventory every bundled file — a skill with no stated need for code shouldn&#39;t ship scripts or binaries. Look for the §1 patterns: encoded blobs, dynamically built URLs, env-var/credential-shaped references, instructions that contradict or exceed the stated purpose.</li>\n<li><strong>Use scanners — plural.</strong> One scanner is one static/semi-static pass with known blind spots (§4). Running several, from different vendors with different detection approaches, catches more than any one alone.</li>\n<li><strong>Cross-check signals — scanners disagree with each other (§4).</strong> Don&#39;t take a clean result from one scanner as final. Check it against another tool, the repo&#39;s own history/context, or a manual read.</li>\n<li><strong>Restrict the permissions granted to the artifact.</strong> Grant only what the stated purpose actually requires. Treat any request for more as a red flag on its own (this is what the Reversec example in §1 exploited).</li>\n<li><strong>Sandbox execution where possible.</strong> The single highest-leverage step, and the one most often skipped for convenience. See <code>agent-sandboxing.md</code> for how — not repeated here.</li>\n<li><strong>Prefer reputable, actively maintained sources.</strong> Popularity and age aren&#39;t proof of safety on their own (<code>security-and-supply-chain.md</code> covers why — compromised-legitimate-package incidents), but a first-party or well-established maintainer is still a meaningfully better starting point than an anonymous, week-old account.</li>\n<li><strong>Treat scanner output as one piece of evidence, not a guarantee.</strong> This is the conclusion, not a footnote — see §4 and the top of this doc.</li>\n</ol>\n<h2 id="3-scanner-landscape-as-of-august-2026">3. Scanner landscape (as of August 2026)</h2>\n<p>Purpose-built scanners went from nonexistent to plentiful over 2026:</p>\n<ul>\n<li><strong>NVIDIA <code>SkillSpector</code></strong> (<a href="https://github.com/NVIDIA/SkillSpector"><code>github.com/NVIDIA/SkillSpector</code></a>, Apache-2.0) — scans a local dir, single <code>SKILL.md</code>, zip, or GitHub URL directly, pre-install. Two-pass: static (regex + Python AST + taint-tracking + live OSV.dev dependency lookups) plus optional LLM semantic pass. 0–100 risk score, SARIF/JSON/markdown output. Can run <em>as</em> an MCP server exposing a <code>scan_skill()</code> tool to gate installs at runtime.</li>\n<li><strong>Snyk <code>agent-scan</code></strong> (<a href="https://github.com/snyk/agent-scan"><code>github.com/snyk/agent-scan</code></a>, Apache-2.0) — covers skills and MCP servers in one pass, descended from Invariant Labs&#39; MCP security research. Risk-scores prompt injection, tool poisoning, tool shadowing, toxic flows. Also runs zero-install as a browser drag-and-drop tool (<a href="https://labs.snyk.io/experiments/skill-scan"><code>labs.snyk.io/experiments/skill-scan</code></a>).</li>\n<li><strong>Cisco AI Defense <code>skill-scanner</code></strong> (<a href="https://github.com/cisco-ai-defense/skill-scanner"><code>github.com/cisco-ai-defense/skill-scanner</code></a>, YARA + YAML rules + LLM pass) — its own docs admit &quot;no findings ≠ no risk.&quot; No MCP coverage claimed.</li>\n<li><strong>Sentry&#39;s <code>skill-scanner</code></strong> — a Skill that scans Skills, bundled in <a href="https://github.com/getsentry/skills"><code>getsentry/skills</code></a>. Checks permissions requested vs. actually used, plus the usual injection/exfiltration/secret categories.</li>\n<li><strong><code>NMitchem/SkillScan</code></strong> — most technically interesting design: a three-stage audit/predict/test pipeline that actually runs the skill in a sandboxed microVM against honeypot canary credentials. Very small, effectively one-person project — worth reading for the canary-file technique, not worth depending on alone.</li>\n</ul>\n<p><strong>MCP-specific drift/rug-pull detection is a separate, narrower gap.</strong> A tool&#39;s declared behavior can change silently after you&#39;ve already approved it. <a href="https://github.com/DataScience-EngineeringExperts/mcp-warden"><code>DataScience-EngineeringExperts/mcp-warden</code></a> canonicalizes an MCP server&#39;s declared tool/resource/prompt surface and pins it to a lock file, failing on drift. Small project, but the technique — hash the declared surface once, alert on any change — is simple enough to reimplement directly.</p>\n<p><strong>What Anthropic itself offers.</strong> Automatic skill/plugin scanning exists but is Enterprise-plan-only (<a href="https://support.claude.com/en/articles/15927065-get-started-with-skill-and-plugin-scanning">Anthropic Help Center</a>): runs automatically on upload/edit across Claude, Claude Cowork, and Enterprise marketplaces, pass/warn/fail in 1–2 minutes. No individual/Pro/Max equivalent, no documented way to manually scan an arbitrary skill even on Enterprise.</p>\n<p>What individuals <em>do</em> get: the community marketplace (<code>anthropics/claude-plugins-community</code>) runs automated validation/safety screening and SHA-pins each plugin to a specific commit, and the <code>/plugin</code> Discover tab shows a &quot;Will install&quot; pane before you commit — every command, agent, skill, hook, MCP server, and LSP server a plugin adds. Anthropic&#39;s own docs still disclaim they &quot;can&#39;t verify that they work as intended&quot; even for the vetted marketplace.</p>\n<h2 id="4-why-no-single-scanner-is-enough-the-evidence">4. Why no single scanner is enough — the evidence</h2>\n<p>This section shapes how everything above should actually be used.</p>\n<ul>\n<li><strong>Scanners barely agree with each other.</strong> <a href="https://theweatherreport.ai/posts/skill-scanner-disagreement/">Ilya Kabanov&#39;s independent study</a> (2026-03-24) ran seven scanners (VirusTotal, Snyk, Socket, Cisco, others) across 238,180 unique skills. On the one marketplace where five scanners&#39; coverage overlapped, they agreed on only 33 of 27,111 skills — 0.12%. Of skills flagged by at least one scanner, 72% were flagged by exactly one.</li>\n<li><strong>Static scanning is straightforwardly evadable.</strong> HKUST researchers (<a href="https://arxiv.org/abs/2607.02357">arXiv 2607.02357</a>, July 2026) built <code>SkillCloak</code>: across eight scanners and 1,613 in-the-wild malicious skills, a packing technique bypassed every scanner over 90% of the time; a semantics-preserving rewrite cleared over 80% of static scanners and 96% of a hybrid static+LLM scanner. Their behavior-based counterpart, <code>SkillDetonate</code>, caught 97% of injected payloads across original, obfuscated, and packed forms — sandbox/behavioral testing meaningfully outperforms static scanning, at a real time cost.</li>\n<li><strong>Scanners also over-flag heavily without context.</strong> Researchers from TU Wien and collaborators (<a href="https://arxiv.org/abs/2603.16572">arXiv 2603.16572</a>) found marketplace scanners classify up to 46.8% of skills as malicious in isolation, but only 0.52% stay suspicious once cross-checked against the skill&#39;s own GitHub repo context (age, commit history, stars, whether the code matches the stated purpose) — roughly a 99% reduction in flagged skills.</li>\n</ul>\n<p><strong>Synthesis:</strong> a clean scan proves very little on its own. A dirty scan (marketplace-level automated screening) proves less than it looks like without repo context. The signal that actually discriminates well is repo-provenance context plus sandboxed behavioral testing, not static pattern-matching alone. Scanning still isn&#39;t worthless — a scan result is one input into a judgment call, not the judgment call itself.</p>\n<h2 id="5-does-the-current-mcp-spec-close-any-of-this">5. Does the current MCP spec close any of this?</h2>\n<p>Checked directly against the <a href="https://modelcontextprotocol.io/specification/2025-06-18/server/tools">official spec</a> and the <a href="https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/">2026-07-28 release candidate notes</a>:</p>\n<ul>\n<li><strong>Tool annotations exist, but the spec tells clients not to trust them.</strong> Tools can carry optional <code>annotations</code> (destructive, read-only, idempotent). The spec is explicit: &quot;clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers&quot; — advisory metadata a malicious server can just lie about, not a verified property.</li>\n<li><strong>Human-in-the-loop consent is a &quot;SHOULD,&quot; not enforced.</strong> The spec recommends implementations &quot;SHOULD&quot; prompt for confirmation on sensitive operations and show tool inputs before calling — guidance for client implementers, not a protocol-enforced guarantee every client honors.</li>\n<li><strong>No tool-definition signing or integrity verification in the base spec.</strong> No cryptographic hash, version binding, or signature ties a tool&#39;s definition to what a user previously approved. The &quot;rug pull&quot; pattern — a server changing a tool&#39;s behavior after approval, no re-consent — isn&#39;t addressed. Fixes exist in research (e.g. <a href="https://arxiv.org/pdf/2506.01333">ETDI</a>, binding tool definitions to signed JWTs) but aren&#39;t in the adopted spec.</li>\n<li><strong>The 2026-07-28 RC&#39;s security changes are about authorization and audit, not artifact trust.</strong> It hardens OAuth/OIDC flows (mix-up-attack mitigation per RFC 9207), constrains unsolicited server-to-client requests, and sandboxes server-shipped UI inside an iframe with the same consent path as a direct tool call. None of it changes whether a tool&#39;s declared behavior can be trusted or verified against tampering.</li>\n</ul>\n<p><strong>Bottom line:</strong> the current spec gives you a defined consent hook and an explicit &quot;don&#39;t trust annotations blindly&quot; rule — more than earlier versions had. It still doesn&#39;t provide signing, drift detection, or any built-in way to verify a tool hasn&#39;t changed since approval. <code>mcp-warden</code>&#39;s hash-and-pin approach (§3) remains a client-side workaround, not something the protocol does for you.</p>\n<h2 id="6-personal-stack">6. Personal stack</h2>\n<p>This doc is the general landscape and framework, not the specific pick. Scanners actually being trialed against real candidate skills/MCP servers are tracked in <a href="../../_architecture/plans/decisions/005-skill-scanner-picks.md"><code>decisions/005-skill-scanner-picks.md</code></a> — currently NVIDIA&#39;s scanner plus Snyk&#39;s <code>agent-scan</code>, run together rather than either alone, per §4. Check that file for what&#39;s actually adopted; this one stays on the general landscape.</p>\n<h2 id="sources">Sources</h2>\n<ul>\n<li><a href="https://support.claude.com/en/articles/15927065-get-started-with-skill-and-plugin-scanning">Anthropic Help Center — Get started with skill and plugin scanning</a>; <a href="https://code.claude.com/docs/en/discover-plugins">Claude Code plugin discovery docs</a></li>\n<li><a href="https://github.com/NVIDIA/SkillSpector"><code>github.com/NVIDIA/SkillSpector</code></a>; <a href="https://github.com/snyk/agent-scan"><code>github.com/snyk/agent-scan</code></a>; <a href="https://labs.snyk.io/experiments/skill-scan">Snyk skill-scan web tool</a>; <a href="https://github.com/cisco-ai-defense/skill-scanner"><code>github.com/cisco-ai-defense/skill-scanner</code></a>; <a href="https://github.com/getsentry/skills"><code>github.com/getsentry/skills</code></a>; <a href="https://github.com/NMitchem/SkillScan"><code>github.com/NMitchem/SkillScan</code></a>; <a href="https://github.com/DataScience-EngineeringExperts/mcp-warden"><code>github.com/DataScience-EngineeringExperts/mcp-warden</code></a></li>\n<li>Ilya Kabanov, <a href="https://theweatherreport.ai/posts/skill-scanner-disagreement/">&quot;Seven scanners for malicious AI agent skills agree on only 0.12%&quot;</a> (theweatherreport.ai, 2026-03-24)</li>\n<li>HKUST et al., <a href="https://arxiv.org/abs/2607.02357">&quot;Cloak and Detonate: Scanner Evasion and Dynamic Detection of Agent Skill Malware&quot;</a> (arXiv 2607.02357, July 2026)</li>\n<li><a href="https://arxiv.org/abs/2603.16572">&quot;Context Matters: Repository-Aware Security Analysis of the Agent Skill Ecosystem&quot;</a> (arXiv 2603.16572)</li>\n<li><a href="https://arxiv.org/pdf/2602.06547">&quot;&#39;Do Not Mention This to the User&#39;: Detecting and Understanding Malicious Agent Skills in the Wild&quot;</a> (arXiv 2602.06547)</li>\n<li>Reversec, <a href="https://labs.reversec.com/posts/2026/05/skill-issues-compromising-claude-code-with-malicious-skills-agents-part-1">&quot;Skill Issues: Compromising Claude Code with malicious skills &amp; agents — Part 1&quot;</a></li>\n<li>Unit 42 (Palo Alto Networks), <a href="https://unit42.paloaltonetworks.com/ai-agent-prompt-injection/">&quot;Fooling AI Agents: Web-Based Indirect Prompt Injection Observed in the Wild&quot;</a></li>\n<li>The Hacker News, <a href="https://thehackernews.com/2025/09/first-malicious-mcp-server-found.html">&quot;First Malicious MCP Server Found Stealing Emails in Rogue Postmark-MCP Package&quot;</a>; <a href="https://snyk.io/blog/malicious-mcp-server-on-npm-postmark-mcp-harvests-emails/">Snyk&#39;s writeup of the same incident</a></li>\n<li>SlowMist, <a href="https://slowmist.medium.com/threat-intelligence-analysis-of-clawhub-malicious-skills-poisoning-0448ffd49c80">ClawHub malicious-skills poisoning analysis</a></li>\n<li>Wiz, <a href="https://www.wiz.io/blog/keyv-and-cacheable-npm-supply-chain-attack">keyv and cacheable npm supply chain attack</a></li>\n<li>Model Context Protocol, <a href="https://modelcontextprotocol.io/specification/2025-06-18/server/tools">official specification — Tools</a>; <a href="https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/">2026-07-28 release candidate notes</a>; ETDI proposal, <a href="https://arxiv.org/pdf/2506.01333">arXiv 2506.01333</a></li>\n<li><a href="../../_architecture/plans/decisions/005-skill-scanner-picks.md"><code>decisions/005-skill-scanner-picks.md</code></a> — this project&#39;s actual scanner trial, referenced rather than duplicated here</li>\n</ul>\n';
