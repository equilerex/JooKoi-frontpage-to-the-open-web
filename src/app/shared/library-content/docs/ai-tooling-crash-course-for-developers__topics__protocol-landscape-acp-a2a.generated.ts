// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/protocol-landscape-acp-a2a.md
export const HTML = "<h1 id=\"stage-1-protocol-landscape-acp-a2a-and-cli-vs-mcp\">Stage 1 — Protocol Landscape: ACP, A2A, and CLI-vs-MCP</h1>\n<p>Different rulesets for how an AI agent talks to other things: the files/programs it operates on, the editor it runs in, or agents built by someone else. Skim this if you&#39;re curious how these plug together. Skip it if you only use one and don&#39;t care how it&#39;s wired.</p>\n<h2 id=\"0-what-problem-is-this-solving\">0. What problem is this solving?</h2>\n<p>An AI coding agent needs to talk to several different things — files/APIs it operates on, the editor/terminal a person uses, sometimes other agents. Each is a separate integration problem, standardized separately rather than with one protocol:</p>\n<ul>\n<li><strong><a href=\"https://modelcontextprotocol.io/specification\">Model Context Protocol (MCP)</a></strong> — agent ↔ tools/data. Defines how an AI app (the &quot;host,&quot; e.g. an IDE or CLI agent) gets context (files, DB rows, search results) and calls functions (&quot;tools&quot;) exposed by a separate server. The protocol a developer is most likely to touch directly — installing an MCP server for GitHub, Postgres, Slack, etc.</li>\n<li><strong><a href=\"https://agentclientprotocol.com\">Agent Client Protocol (ACP)</a></strong> — agent ↔ editor. Standardizes how a coding agent talks to the editor/IDE hosting it (diffs, open files, plans), so one editor can plug in any ACP-compliant agent instead of custom per-pairing integration.</li>\n<li><strong><a href=\"https://a2a-protocol.org/latest/\">Agent-to-Agent (A2A)</a></strong> — agent ↔ agent, across separately-built systems. Lets independently-owned agents (different vendors, different frameworks) discover each other&#39;s capabilities and hand off tasks without knowing each other&#39;s internals.</li>\n</ul>\n<p>Rough mental model: MCP = &quot;give my agent tools and data.&quot; ACP = &quot;let my agent plug into any editor.&quot; A2A = &quot;let my agent talk to someone else&#39;s agent.&quot;</p>\n<p>A fourth, non-protocol option shows up constantly alongside these: for something small and local, just run a <strong>CLI</strong> command — no protocol, no standing server. That&#39;s the &quot;do I even need a protocol here&quot; question, and it&#39;s often the right answer for one repo, one machine, one-off work.</p>\n<p>Where each shows up for a developer:</p>\n<ul>\n<li>MCP — installing a tool integration for a coding agent.</li>\n<li>ACP — an editor advertises &quot;bring your own agent&quot; support.</li>\n<li>A2A — mostly multi-agent <em>products</em> (a company&#39;s agents calling a partner&#39;s agent), not solo dev.</li>\n<li>CLI-vs-MCP — every time &quot;should I build an MCP server for this, or just write a script&quot; comes up.</li>\n</ul>\n<p>Not a ranking — these solve different problems and normally coexist in one system (MCP to reach a database, ACP to talk to the editor, a CLI for a quick local check, all in the same session).</p>\n<hr>\n<p>New topic surfaced from a ChatGPT-sourced dump (quarantine trail resolved and removed once processed). Covers the interface layer one level above individual tools — what connects to what, and when a protocol is even the right answer vs. a plain script. No pick — this is a map, not a recommendation to adopt any of these now.</p>\n<p><strong>Updated 2026-08-23</strong> (cowork research pass): ACP and A2A sections refreshed below. Both prior gaps (ACP&#39;s unnamed maintainer, A2A&#39;s unconfirmed 403&#39;d source) are now resolved. This doc&#39;s own MCP framing was checked against the 2026-07-28 stateless-protocol revision documented in <code>mcp-model-context-protocol.md</code> — §4&#39;s security citation already references that revision correctly, nothing else here needs correcting.</p>\n<h2 id=\"1-the-four-layer-picture\">1. The four-layer picture</h2>\n<ul>\n<li><strong>Agent ↔ tools/data</strong> — MCP (covered in depth in <a href=\"/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol\"><code>mcp-model-context-protocol.md</code></a>).</li>\n<li><strong>Agent ↔ editor/IDE</strong> — Agent Client Protocol (ACP).</li>\n<li><strong>Agent ↔ agent</strong> — A2A (Agent-to-Agent).</li>\n<li><strong>Agent ↔ local machine, small one-off ops</strong> — increasingly just a CLI script, no protocol.</li>\n</ul>\n<p>Genuinely different layers, not competing standards. Treating &quot;ACP vs MCP&quot; or &quot;CLI vs MCP&quot; as one winner-take-all choice is the wrong frame — each answers a different question.</p>\n<h2 id=\"2-agent-client-protocol-acp\">2. Agent Client Protocol (ACP)</h2>\n<p><strong>What it is:</strong> standardizes communication between editors/IDEs and coding agents (confirmed from <a href=\"https://agentclientprotocol.com\"><code>agentclientprotocol.com</code></a>). Explicitly LSP-shaped: LSP let one editor speak to any language server without custom per-pairing work; ACP aims for the same with coding agents. Reuses MCP&#39;s JSON representations where possible, adds agentic-coding-specific types (e.g. structured diff display) MCP doesn&#39;t have.</p>\n<p><strong>What it is not:</strong> doesn&#39;t replace MCP or Skills. MCP is agent↔tools; ACP is editor↔agent — an agent using ACP to talk to your editor still uses MCP (or CLI scripts) to talk to your filesystem, GitHub, etc.</p>\n<p><strong>Maturity — both prior gaps resolved (2026-08-23 pass):</strong></p>\n<ul>\n<li>Jointly governed by <strong>Zed Industries and JetBrains</strong> (not Zed alone) — confirmed via the spec&#39;s <code>/community/governance</code> page.</li>\n<li>Two lead maintainers hold veto authority: Ben Brandt (Zed), Sergey Ignatov (JetBrains, appointed 2026-02-18).</li>\n<li>Canonical repo moved to <a href=\"https://github.com/agentclientprotocol/agent-client-protocol\"><code>github.com/agentclientprotocol/agent-client-protocol</code></a> (out from under <code>zed-industries</code>), ~4.0k★/351 forks, published <code>MAINTAINERS.md</code>/<code>GOVERNANCE.md</code>, official SDKs (Rust, TypeScript, Python, Java, Kotlin).</li>\n<li><strong>Not</strong> an AAIF project, despite both maintaining orgs being AAIF members — governance page says they&#39;re &quot;working toward transitioning to an independent foundation,&quot; no date given.</li>\n</ul>\n<p><strong>Adoption — real and broad, not Zed-only:</strong></p>\n<ul>\n<li>~40 listed agents (Claude Agent, Codex CLI, Gemini CLI, GitHub Copilot, Cline, Goose, OpenCode, Cursor, others).</li>\n<li>Editor/client support: JetBrains IDEs, Neovim, Emacs, VS Code extensions, Qt Creator, more (<code>/get-started/clients</code>, <code>/get-started/agents</code>).</li>\n<li>Caveat: self-listed directory pages — &quot;claims support,&quot; not verified conformance depth.</li>\n<li>Remote-agent support not shipped: stdio is still the only production transport. A Transports Working Group formed 2026-04-22 to standardize HTTP/WebSockets — currently &quot;draft proposal in progress.&quot;</li>\n<li>Other 2026 developments: an ACP Registry for cross-client agent discovery/install; Rust/TypeScript SDKs hit 1.0 on 2026-06-25.</li>\n</ul>\n<h2 id=\"3-agent-to-agent-a2a\">3. Agent-to-Agent (A2A)</h2>\n<p><strong>What it is:</strong> protocol for agent↔agent communication — distinct from MCP (agent↔tool) and ACP (agent↔editor). Announced by Google (April 2025), donated to the Linux Foundation with founding orgs AWS, Cisco, Google, Microsoft, Salesforce, SAP, ServiceNow.</p>\n<p><strong>Governance update, confirmed 2026-08-23</strong> (previously-blocked Axios source now superseded by primary sources): on <strong>2026-08-17</strong> — six days before this pass — <strong>A2A joined the Agentic AI Foundation (AAIF)</strong>, the same Linux Foundation entity hosting MCP, <code>goose</code>, and <code>AGENTS.md</code>. Confirmed directly against AAIF&#39;s blog/project pages (<a href=\"https://aaif.io/blog/a2a-joins-aaif\"><code>aaif.io/blog/a2a-joins-aaif</code></a>, <code>aaif.io/projects/agent2agent</code>). The protocol&#39;s own site (<a href=\"https://a2a-protocol.org/latest/\"><code>a2a-protocol.org</code></a>) hadn&#39;t been updated to mention AAIF as of this pass — stale, not wrong, since AAIF is itself a Linux Foundation body.</p>\n<p>A2A hit <strong>v1.0</strong> (first stable spec) in March 2026: multi-tenancy, modernized security flows, signed Agent Cards for cryptographic identity. Linux Foundation&#39;s press release (2026-04-09) reports <strong>150+ participating organizations</strong> and enterprise production use — named implementers include Google Cloud, Microsoft (Azure AI Foundry, Copilot Studio), AWS (Bedrock AgentCore Runtime), Cisco, IBM, Salesforce, SAP, ServiceNow, plus LangGraph/CrewAI framework integration. Treat &quot;150+ organizations&quot; as a self-reported foundation participation count, not an audited deployment count.</p>\n<p><strong>Relevance to this stack, honestly assessed:</strong> still low — a solo developer isn&#39;t building a multi-agent <em>product</em> where independently-owned agents negotiate with each other. The adoption breadth above changes &quot;is this real,&quot; not &quot;is this relevant to me.&quot; A2A matters when the multi-agent system <em>is</em> the product (comparable to when CrewAI/LangGraph/AutoGen becomes relevant — see <a href=\"/library/ai-tooling-crash-course-for-developers/topics/subagents-and-delegation\"><code>subagents-and-delegation.md</code></a> §&quot;Third-party orchestration frameworks&quot;). Not a near-term concern here.</p>\n<h2 id=\"4-cli-scripts-vs-mcp-for-local-capabilities\">4. CLI-scripts vs. MCP for local capabilities</h2>\n<p>The practical decision that actually matters for a personal setup, more than either protocol above.</p>\n<p><strong>The pattern:</strong> for small, repo-local operations (<code>npm run test:affected</code>, a lint-and-report script, a &quot;find component context&quot; helper), an ordinary shell command a coding agent can already run is often simpler than standing up a dedicated MCP server:</p>\n<ul>\n<li>No extra server process or protocol layer to keep alive.</li>\n<li>Already versioned with the repo, already familiar, already testable normally.</li>\n<li>Works with any coding agent that can run a shell command — no MCP client support required.</li>\n<li>Permissions are whatever your existing shell/sandbox already enforces.</li>\n</ul>\n<p><strong>Where this doesn&#39;t apply:</strong> MCP earns its cost for genuinely <em>external</em>, <em>reusable</em>, <em>multi-client</em> integrations — a database connector, a GitHub API wrapper, anything meant to be discovered and called the same way from Claude Code, Cursor, and a teammate&#39;s IDE alike. <a href=\"/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol\"><code>mcp-model-context-protocol.md</code></a> §5 documents this exact pattern from the opposite direction (Skill-wrapping-a-CLI as the low-cost MCP alternative). General rule: CLI-first for anything local and single-purpose, MCP when it needs to be a standing, multi-client, discoverable service.</p>\n<p><strong>The security correction:</strong> &quot;use a CLI script instead of MCP&quot; isn&#39;t automatically safer. A shell command with broad filesystem/network access isn&#39;t inherently more contained than a narrowly-scoped MCP tool. The real principle is least privilege, explicit approval, sandboxing, auditability — regardless of mechanism.</p>\n<p>MCP&#39;s own current security docs (<a href=\"https://modelcontextprotocol.io/specification/2026-07-28/basic/security_best_practices\"><code>modelcontextprotocol.io</code></a>, <code>2026-07-28</code>, fetched directly) confirm this:</p>\n<ul>\n<li><strong>Local MCP Server Compromise</strong> — a malicious local MCP server binary runs with the same privileges as the client, capable of data exfiltration or destructive commands exactly like an unreviewed shell script.</li>\n<li><strong>Confused deputy problem</strong> — a proxy server with a static client ID can be tricked into leaking an authorization code to an attacker.</li>\n<li><strong>Token passthrough</strong> — a server forwards a client&#39;s token to a downstream API without validating it was issued for that server.</li>\n</ul>\n<p>Both confused-deputy and token-passthrough are explicit <code>MUST</code>/<code>MUST NOT</code> requirements in the current spec, not informal guidance. The source dump&#39;s other two claimed risks — prompt injection and untrusted tool metadata (&quot;tool poisoning&quot;) — are real but come from a different part of MCP&#39;s security story, already covered in <a href=\"/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol\"><code>mcp-model-context-protocol.md</code></a> §4 via Cloud Security Alliance research. This page&#39;s scope is mostly OAuth/SSRF/local-execution risk — broader and more detailed than what&#39;s currently cited there (SSRF, state-handle hijacking, localhost redirect impersonation, mix-up attacks). <strong>Follow-up flagged:</strong> <code>mcp-model-context-protocol.md</code>&#39;s security section should cite this primary source directly.</p>\n<h2 id=\"5-agent-skills-as-an-open-standard-one-claim-checked\">5. Agent Skills as an open standard — one claim checked</h2>\n<p>The source dump cited <a href=\"https://github.com/agentskills/agentskills\"><code>github.com/agentskills/agentskills</code></a> as &quot;the Agent Skills specification released as an open standard.&quot; Worth scrutinizing since <a href=\"/library/ai-tooling-crash-course-for-developers/topics/skill-libraries-and-marketplaces\"><code>skill-libraries-and-marketplaces.md</code></a> already establishes <code>platform.claude.com/docs</code> as the canonical spec source — a second repo claiming to be &quot;the standard&quot; could be overstatement or an unofficial fork.</p>\n<p>Direct fetch confirms it&#39;s not a rival: the repo&#39;s own README says the format &quot;was originally developed by Anthropic, released as an open standard, and has been adopted by a growing number of agent products.&quot; It frames itself as the open-ecosystem home for that same standard, and shows real activity — 24.6k stars, 1.8k forks, 145 commits, active issues/PRs. Consistent with, not contradicting, <code>skill-libraries-and-marketplaces.md</code>. No correction needed there.</p>\n<p><strong>2026-08-23 delta check:</strong> a follow-up fetch returned ~20.6k★/123 commits — lower than the 24.6k★/145 commits above. GitHub star counts don&#39;t typically drop, so this is more likely a snapshot/rendering discrepancy than an actual decline, but unresolved — treat both numbers as approximate. Governance unchanged: still not an AAIF project, still Anthropic-originated, community-run under the neutral <code>agentskills</code> org, Apache-2.0 code / CC-BY-4.0 docs.</p>\n<h2 id=\"6-practical-read-for-this-setup\">6. Practical read for this setup</h2>\n<p>Given this user&#39;s actual context — solo home developer, Angular-monorepo-adjacent work, already running Claude Code with subagents and MCP research done separately:</p>\n<ul>\n<li><strong>Worth acting on now:</strong> CLI-first instinct for small repo-local operations. Before reaching for a new MCP server scoped to one repo, check whether a plain script does the job.</li>\n<li><strong>Worth knowing about, not urgent to build around:</strong> ACP now has real breadth (~40 agents, multiple editors, joint Zed/JetBrains governance) — not a single-vendor experiment. Worth a look if a future editor switch or multi-agent-in-one-IDE need arises. Remote-agent/HTTP transport still isn&#39;t shipped, so nothing changes for a local stdio-only setup today.</li>\n<li><strong>Not relevant yet:</strong> A2A. Revisit only if a future project&#39;s product architecture involves independently-owned agents negotiating with each other.</li>\n</ul>\n<h2 id=\"open-questions-weak-sourcing\">Open questions / weak sourcing</h2>\n<ul>\n<li><strong>Resolved 2026-08-23:</strong> ACP&#39;s maintainers (Zed + JetBrains, named leads) and the A2A governance/adoption picture — both previously flagged unconfirmed, now sourced from primary pages (§2, §3).</li>\n<li>ACP Registry&#39;s release date is unresolved — spec&#39;s <code>/updates</code> page dates it 2026-03-09, Zed&#39;s announcement blog dates it 2026-01-28. Possibly two distinct events (launch vs. stabilization) — don&#39;t cite a single date without rechecking.</li>\n<li>ACP&#39;s stated goal of an independent foundation has no timeline — not confirmed whether it&#39;s active work or aspiration.</li>\n<li>Agent Skills&#39; star count is inconsistent: 24.6k★ (original pass) vs. ~20.6k★ (2026-08-23 delta check) — see §5, unresolved.</li>\n<li><a href=\"/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol\"><code>mcp-model-context-protocol.md</code></a> §4 covers tool-poisoning/prompt-injection via secondary aggregated coverage; this doc&#39;s §4 fetched a different, more detailed primary MCP security page directly (confused deputy, token passthrough, SSRF, local-server compromise, state-handle hijacking). Complementary, not duplicative — but <code>mcp-model-context-protocol.md</code> would benefit from citing this fuller primary source directly.</li>\n</ul>\n";
