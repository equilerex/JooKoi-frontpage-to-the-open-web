// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/skill-libraries-and-marketplaces.md
export const HTML =
  '<h1 id="stage-1-prompt-skill-libraries-stage-0-topic-2">Stage 1 — Prompt &amp; Skill Libraries (Stage 0 topic #2)</h1>\n<p>An AI coding assistant is like a new hire who&#39;s smart but doesn&#39;t know your habits yet. A &quot;skill&quot; is a laminated instruction card for a recurring job — hand it over once, get the same result every time instead of re-explaining. A &quot;library&quot; or &quot;marketplace&quot; is a shelf of cards other people wrote, so you can grab one instead of writing your own. Useful if you use an assistant regularly and keep retyping the same instructions. Skip if you only ask one-off questions.</p>\n<p>Survey, not a pick. Covers what a skill is, current <code>SKILL.md</code> spec, authoring anti-patterns, distribution, and what&#39;s broadly adopted. Nothing here is adopted by this stage — read and choose.</p>\n<h2 id="1-skill-basics">1. Skill basics</h2>\n<p><strong>What is it?</strong> A packaged, reusable set of instructions (and optionally code) an agent loads on demand for a specific kind of task. Save it once, invoke it by name whenever the task matches.</p>\n<p><strong>What&#39;s inside?</strong> A folder with a <code>SKILL.md</code>:</p>\n<ul>\n<li>YAML frontmatter — at minimum a <code>description</code> (what it does, when to use it). <code>name</code> is optional, defaults to the folder name.</li>\n<li>Markdown body — the instructions.</li>\n<li>Optionally: reference docs, example collections, a <code>scripts/</code> folder.</li>\n</ul>\n<p>Minimal working example:</p>\n<pre><code class="language-yaml">---\ndescription: Stage changes and write a Conventional Commits message. Use when the user asks to commit or wants a commit message written from the diff.\n---\n\nStage the relevant changes, then write a commit message in Conventional Commits\nformat (type(scope): summary). Keep the subject under 50 characters.\n</code></pre>\n<p>That&#39;s a complete skill.</p>\n<p><strong>Skill vs. prompt.</strong> A prompt is one-off — typed, used, gone. A skill is reusable and <em>discoverable</em>: named, and its <code>description</code> tells the agent when it applies, so it surfaces on future matching tasks without you re-explaining.</p>\n<p><strong>Skill vs. <code>AGENTS.md</code>/<code>CLAUDE.md</code>.</strong> A base instruction file loads in full, every session, regardless of task. A skill&#39;s full body loads only when relevant — only its <code>description</code> sits in context at session start. This is <strong>progressive disclosure</strong>: dozens of skills can exist without bloating every session.</p>\n<p><strong>How does the agent know when to use one?</strong> By matching the task against each skill&#39;s <code>description</code>. This is the actual retrieval mechanism, not decoration. &quot;Helps with commits&quot; competes poorly against &quot;stage changes and write a Conventional Commits message from the diff; use when asked to commit.&quot;</p>\n<p><strong>Explicit invocation:</strong> yes. Skills double as slash commands — <code>/skill-name</code> runs it directly, bypassing the agent&#39;s own judgment.</p>\n<p><strong>Where do they live (Claude Code)?</strong> Per <a href="https://code.claude.com/docs/en/skills">its docs</a>:</p>\n<ul>\n<li>Personal/global: <code>~/.claude/skills/&lt;skill-name&gt;/SKILL.md</code> (every project)</li>\n<li>Project-specific: <code>.claude/skills/&lt;skill-name&gt;/SKILL.md</code> (shared via version control)</li>\n</ul>\n<p>Format is an open standard — <a href="https://agentskills.io">Agent Skills</a> — so the same <code>SKILL.md</code> shape works across multiple tools.</p>\n<p><strong>Simple vs. complex.</strong> As simple as the one-file example above. As complex as a <code>SKILL.md</code> overview linking out to <code>reference.md</code>, <code>examples.md</code>, a <code>scripts/</code> folder — each loaded only when needed, keeping the always-in-context part tiny.</p>\n<p><strong>What are scripts/resources for?</strong> Two jobs: (1) offload fragile/deterministic logic to real code instead of prose the model re-derives every time, (2) keep large reference material off-context until actually read.</p>\n<p><strong>Compound/workflow skills.</strong> Some orchestrate a multi-step procedure or delegate to a subagent or other skills — still just <code>SKILL.md</code> files, complexity lives in the instructions and bundled scripts.</p>\n<p><strong>When to write one?</strong> When a specific need actually repeats — not because a marketplace listing looks useful. Trigger: you&#39;ve typed the same instruction three times, or a section of your base instruction file has grown into a multi-step procedure.</p>\n<p><strong>Can AI write one for you?</strong> Yes — Anthropic ships <code>skill-creator</code> in <a href="https://github.com/anthropics/skills"><code>anthropics/skills</code></a>, scaffolds a <code>SKILL.md</code>, tightens the <code>description</code>, helps write eval scenarios. Reading its source doubles as a format example.</p>\n<h2 id="2-less-is-more">2. Less is more</h2>\n<p>Every installed skill&#39;s <code>description</code> sits in context on every turn, even when the body never loads — a recurring token cost, and another chance the agent picks the wrong skill for an ambiguous task.</p>\n<ul>\n<li>Keep a small set you actually invoke, not a collection you might need someday.</li>\n<li>A precise, narrow skill beats a general one that half-fits several needs.</li>\n<li>Check if adapting an existing skill gets you there before writing a new one.</li>\n<li>Write your own when a workflow is specific enough — don&#39;t wait for someone else to publish one. A five-minute need justifies a five-line <code>SKILL.md</code>.</li>\n</ul>\n<h2 id="3-current-spec-state">3. Current spec state</h2>\n<p>Open format: <a href="https://agentskills.io">Agent Skills</a>; Claude Code&#39;s extensions at <a href="https://code.claude.com/docs/en/skills">code.claude.com/docs/en/skills</a>.</p>\n<ul>\n<li><strong>Frontmatter:</strong> only <code>description</code> recommended (not strictly required — falls back to the first markdown paragraph). <code>name</code> optional. Write <code>description</code> in third person — it&#39;s injected straight into the system prompt, and inconsistent point-of-view breaks discovery.</li>\n<li><strong>Progressive disclosure:</strong> only <code>name</code>+<code>description</code> load at session start. Full body loads when relevant or invoked directly. Referenced files load only when actually read — zero cost until touched.</li>\n<li><strong>Size discipline:</strong> keep <code>SKILL.md</code> body under 500 lines; split into reference files beyond that. Keep reference files <strong>one level deep</strong> — an agent may only partially read a file referenced from another referenced file.</li>\n<li><strong>Degrees of freedom:</strong> high freedom (prose heuristics) for judgment calls, low freedom (exact script, no deviation) for fragile/sequential operations like migrations.</li>\n</ul>\n<h2 id="4-anti-patterns">4. Anti-patterns</h2>\n<p>Two sources converge: <strong>build from your own repeated need, not from a marketplace browse.</strong></p>\n<ul>\n<li>Don&#39;t build a skill for something the model already knows or that rarely changes. Skills earn their cost loading <em>current</em> knowledge the model lacks — fast-moving APIs, your team&#39;s conventions, a fragile procedure — not re-explaining fundamentals.</li>\n<li>Progression ladder: direct prompt → (repeat 3x) → slash command → (too complex) → skill. Skipping straight to &quot;download a skill for this&quot; before hitting that threshold is the anti-pattern.</li>\n<li>One clear default with an escape hatch beats multiple options (&quot;use pdfplumber... for scanned PDFs use X&quot; beats listing five libraries).</li>\n<li>Scripts: handle errors explicitly, justify every numeric constant — same discipline as normal code review.</li>\n<li>Evaluation-first workflow: run a real task without the skill, note actual gaps, write a few eval scenarios against those gaps, <em>then</em> write minimal instructions to pass them. Skills built by imagining requirements instead of observing failures are the anti-pattern this workflow prevents.</li>\n</ul>\n<p>Source: Anthropic&#39;s official best-practices doc (primary), cross-checked against a practitioner substack post on newcomer anti-patterns.</p>\n<h2 id="5-distribution">5. Distribution</h2>\n<p>No longer manual-copy-only:</p>\n<ul>\n<li><strong>Claude Code plugins</strong> bundle skills plus optional hooks/MCP servers into one installable package, via <code>/plugin</code>.</li>\n<li><strong>Scope at install:</strong> user-scope → <code>~/.claude/skills/</code> (every project); project-scope → <code>.claude/skills/</code> (team, via VCS).</li>\n<li><strong>Scale, mid-2026:</strong> official Anthropic marketplace lists hundreds of plugins; community marketplaces list low thousands of skills, individual repos bundling hundreds each.</li>\n</ul>\n<p>Manual copying still works for a one-off personal skill. For anything reused across machines or shared, the plugin/marketplace path is now the real option.</p>\n<h2 id="6-recommended-skills">6. Recommended skills</h2>\n<p>Narrow on purpose — only one entry with clear, checkable, broad adoption. Candidate repos worth <em>evaluating</em> (not adopted) live in <a href="./decisions/006-skill-stack-picks.md"><code>decisions/006-skill-stack-picks.md</code></a>.</p>\n<ul>\n<li><strong><a href="https://github.com/anthropics/skills"><code>anthropics/skills</code></a></strong> — official Anthropic repo, 171k stars, 20.3k forks. 17 top-level skills (document processing, design/art, web artifacts, writing, dev tooling), including <code>skill-creator</code> and <code>frontend-design</code>. The one entry here with unambiguous provenance and adoption numbers — reasonable default to browse first.</li>\n</ul>\n<p>Beyond it, dozens of community marketplaces exist (section 5) — evaluate like any third-party dependency: who maintains it, how recently touched, does it solve a need you actually have. Star count alone isn&#39;t an adoption signal.</p>\n<h2 id="7-reference-examples-for-writing-a-good-skill">7. Reference examples for writing a good skill</h2>\n<p>Anthropic&#39;s best-practices doc uses <strong><code>pdf-processing</code></strong> and a <strong>BigQuery domain-organized skill</strong> as worked examples — both demonstrate the one-level-deep reference pattern and domain-split reference files (<code>reference/finance.md</code>, <code>reference/sales.md</code>). Beyond the docs, read <a href="https://github.com/anthropics/skills/tree/main/skill-creator"><code>anthropics/skills/skill-creator</code></a> itself — a working example and a generation tool at once.</p>\n<h2 id="8-individual-skills-in-wide-use-front-end-focus">8. Individual skills in wide use — front-end focus</h2>\n<p>These are <em>libraries</em> (many skills), not atomic skills. Normal usage: cherry-pick a handful, don&#39;t install wholesale — section 8a names specific picks. Numbers below are from direct GitHub API checks (stars/forks/last-push), not aggregator claims.</p>\n<p><strong>Chrome DevTools — adoption sits at the MCP-server layer, not the skill layer.</strong> <a href="https://github.com/ChromeDevTools/chrome-devtools-mcp"><code>ChromeDevTools/chrome-devtools-mcp</code></a> — official Google org, 48.1k stars, pushed within the last week, 27 DevTools tools (DOM inspection, network capture, performance tracing, viewport emulation). That&#39;s what front-end devs actually install. A standalone SKILL.md wrapping it isn&#39;t independently popular — marketplace-listing hits (mcpservers.org, claudeskills.info, awesomeskill.ai, mcpmarket.com) just republish the same thin wrapper. Current practice: install the MCP server, write your own thin <code>SKILL.md</code> around it if needed.</p>\n<p><strong>Small-team/individual-maintained collections, ranked by repo-health signal (all confirmed live, not archived):</strong></p>\n<ul>\n<li><strong><a href="https://github.com/MiniMax-AI/skills"><code>MiniMax-AI/skills</code></a></strong> — 13.4k stars, 1.1k forks, active. Dedicated <code>frontend-dev</code> skill. Backed by MiniMax (named commercial lab).</li>\n<li><strong><a href="https://github.com/ConardLi/garden-skills"><code>ConardLi/garden-skills</code></a></strong> — 10.5k stars, 1.4k forks, active. Maintained by Conard Li (named practitioner). Covers web design, image generation, knowledge retrieval alongside front-end skills — broader than pure front-end.</li>\n<li><strong><a href="https://github.com/microsoft/skills"><code>microsoft/skills</code></a></strong> — 2.9k stars, 333 forks, pushed within the last day (most active of this group). Official Microsoft org. Scoped to skills/MCP servers/custom agents/AGENTS.md for grounding coding agents — includes Playwright testing/visual-regression guidance (cross-ref <a href="/library/ai-tooling-crash-course-for-developers/topics/review-and-verification-tooling"><code>review-and-verification-tooling.md</code></a>).</li>\n<li><strong><a href="https://github.com/finfin/awesome-frontend-skills"><code>finfin/awesome-frontend-skills</code></a></strong> — 184 stars, 24 forks. Small, but scoped exactly to this (curated, <code>npx skills add</code>, front-end only). New-but-promising: low absolute signal, scope matches directly.</li>\n<li><strong><a href="https://github.com/colbymchenry/frontend-audit-skill"><code>colbymchenry/frontend-audit-skill</code></a></strong> — 19 stars, 9 forks. One person&#39;s visual-regression auditing skill (compares live renders against design PNGs) — same category as Anthropic&#39;s own worked example (section 7). Low stars, not confirmed wide adoption.</li>\n</ul>\n<p><code>anthropics/skills</code> (<code>web-artifacts-builder</code>, <code>frontend-design</code>, <code>webapp-testing</code>) is already covered in section 6 — the highest-confidence single source for front-end skills.</p>\n<p><strong>Bottom line:</strong> no thriving standalone &quot;popular front-end skill&quot; ecosystem separate from these collections. A handful of named orgs/practitioners (MiniMax, Conard Li, Microsoft) ship front-end skills inside broader multi-skill repos, plus a couple of small, narrowly-scoped, low-star projects. Chrome DevTools is the clearest example: real adoption sits at the MCP-server layer, skill-wrapping stays thin.</p>\n<h2 id="8a-cherry-pick-list-top-individual-front-end-skills">8a. Cherry-pick list — top individual front-end skills</h2>\n<p>Exact folder/skill names, checked against each repo&#39;s README. Install just the one skill, not the whole library.</p>\n<p><strong>From <a href="https://github.com/anthropics/skills"><code>anthropics/skills</code></a></strong> (de facto standard):</p>\n<ul>\n<li><code>frontend-design</code> — general front-end UI/layout generation.</li>\n<li><code>web-artifacts-builder</code> — React 18 + TypeScript + Tailwind + shadcn/ui multi-component builds.</li>\n<li><code>webapp-testing</code> — pairs with the above for testing.</li>\n</ul>\n<p><strong>From <a href="https://github.com/microsoft/skills"><code>microsoft/skills</code></a></strong> (2.9k★, official Microsoft):</p>\n<ul>\n<li><code>frontend-design-review</code> — the front-end-design counterpart to Anthropic&#39;s, worth comparing rather than assuming duplication.</li>\n<li><code>frontend-ui-dark-ts</code> — dark-mode-aware UI patterns in TypeScript.</li>\n<li><code>azure-microsoft-playwright-testing-ts</code> — Playwright-based automated browser testing.</li>\n<li><code>react-flow-node-ts</code> — React Flow node/diagram UI components.</li>\n<li><code>zustand-store-ts</code> — Zustand state-management patterns.</li>\n</ul>\n<p><strong>From <a href="https://github.com/MiniMax-AI/skills"><code>MiniMax-AI/skills</code></a></strong> (13.4k★):</p>\n<ul>\n<li><code>frontend-dev</code> — React/Next.js UI, animation, generative-art-asset workflows. The one relevant skill in the repo — the rest (mobile-native, PDF/DOCX/PPTX, music/voice) is out of scope here.</li>\n</ul>\n<p><strong>From <a href="https://github.com/ConardLi/garden-skills"><code>ConardLi/garden-skills</code></a></strong> (10.5k★):</p>\n<ul>\n<li><code>web-design-engineer</code> — front-end design for web pages, dashboards, prototypes, mockups. Again the one relevant skill in a mixed-purpose repo — pull this one, skip the rest.</li>\n</ul>\n<p><strong>Design-specific, not full design systems:</strong> <code>microsoft/skills/frontend-design-review</code> and <code>anthropics/skills/frontend-design</code> are the only two named, checkable, narrowly-scoped design-critique/generation skills found — both single-job, neither a full design-token suite. No standalone atomic repo doing only design-critique or token-enforcement at meaningful adoption exists beyond these two — a real, modest gap.</p>\n<h2 id="9-individual-atomic-skills-single-skillmd-not-libraries">9. Individual atomic skills (single SKILL.md, not libraries)</h2>\n<p>&quot;Atomic&quot; = does one coherent job, not one trivial thing — &quot;run a Chrome DevTools responsive-breakpoint check and report results&quot; counts; a whole planning/testing workflow system doesn&#39;t, however good.</p>\n<p>Genuinely standalone single-skill repos with real star-based adoption are rare. Most atomic skills live as one subfolder inside a larger collection (section 8). Where standalone repos exist, stars are mostly low-to-modest. Marketplace listings (mcpmarket.com, agensi.io) give each skill its own page and install count — but that&#39;s a self-reported metric, weaker sourcing, labeled as such below.</p>\n<p><strong>Standalone single-purpose repos (confirmed via GitHub API):</strong></p>\n<ul>\n<li><strong><a href="https://github.com/netresearch/context7-skill"><code>netresearch/context7-skill</code></a></strong> — wraps Context7 docs lookup as a lightweight REST call, no MCP overhead. 53 stars, 8 forks, pushed within days, not archived. Maintainer: netresearch (named German dev agency). Cleanest &quot;one repo = one atomic skill&quot; example found, modest adoption.</li>\n<li><strong><a href="https://github.com/mgifford/accessibility-skills"><code>mgifford/accessibility-skills</code></a></strong> → <code>skills/color-contrast/SKILL.md</code> — repo is small (39 stars, 2 forks), but this subfolder specifically: a narrow WCAG contrast-check skill by Mike Gifford (named accessibility practitioner). Cite the skill, not the whole repo.</li>\n<li><strong><a href="https://github.com/softaworks/agent-toolkit"><code>softaworks/agent-toolkit</code></a></strong> → <code>skills/commit-work/SKILL.md</code> — repo (2,378 stars, 221 forks) is a library, but <code>commit-work</code> (stage changes, split into logical commits, write Conventional Commits) is single-job — cite at the subfolder level.</li>\n</ul>\n<p><strong>Marketplace-listed atomic skills — real, self-reported adoption:</strong></p>\n<ul>\n<li><strong><a href="https://agensi.io"><code>agensi.io</code></a><code>/skills/git-commit-writer</code></strong> — staged-diff → Conventional Commits message. Agensi&#39;s most-downloaded skill by their own dashboard — not independently auditable.</li>\n<li><strong><code>agensi.io/skills/pr-description-writer</code></strong> — same marketplace, same caveat, PR-description generation from a diff.</li>\n<li><strong><a href="https://mcpmarket.com"><code>mcpmarket.com</code></a><code>/tools/skills/leaderboard</code></strong> — real, ranked by GitHub stars of each skill&#39;s underlying repo (confirmed, not a self-reported install count). Confirmed entries: <strong>#1 <code>diagram-maker-visualizer</code></strong> (standalone SVG/HTML/Excalidraw diagrams, zero deps), plus <code>next-js-turbopack-optimizer</code>, <code>frontend-slides</code>, <code>react-flow-implementation</code>, <code>frontend-performance-optimizer</code>, <code>context7-documentation-lookup</code>, <code>react-performance-optimization</code>, <code>react-code-fix-linter</code>. <!-- VERIFY: per-entry star counts and exact ranks beyond #1 not re-confirmed against a live page render (rate-limited); existence and star-based ranking confirmed. --></li>\n</ul>\n<p><strong>What doesn&#39;t hold up:</strong> even a high-profile, checkable maintainer — <a href="https://github.com/addyosmani/agent-skills"><code>addyosmani/agent-skills</code></a> (Addy Osmani, Chrome/web-perf work, 89.1k stars, active) — ships a 24-skill collection, not an atomic skill. Reinforces section 8: at the top of the adoption curve, named practitioners ship libraries, not single skills. The atomic layer is real but stays at modest scale.</p>\n<h2 id="sources">Sources</h2>\n<ul>\n<li><a href="https://code.claude.com/docs/en/skills"><code>code.claude.com/docs/en/skills</code></a> — primary source for spec, frontmatter, locations, anti-patterns, progressive disclosure</li>\n<li><a href="https://agentskills.io"><code>agentskills.io</code></a> — the open Agent Skills spec</li>\n<li><code>aiforsystems.substack.com</code> post on Claude Code newcomer anti-patterns — cross-check</li>\n<li><a href="https://github.com/anthropics/skills"><code>github.com/anthropics/skills</code></a> — direct fetch, confirmed stars/forks/structure</li>\n<li>Search aggregation across <code>alirezarezvani.github.io</code>, <code>agensi.io</code>, <code>hidekazu-konishi.com</code>, <code>designrevision.com</code>, <code>firecrawl.dev</code> — cross-checked for scale/mechanics, not independent sourcing on their own</li>\n<li><a href="https://github.com/ChromeDevTools/chrome-devtools-mcp"><code>github.com/ChromeDevTools/chrome-devtools-mcp</code></a> — direct API check, 48.1k stars confirmed</li>\n<li><a href="https://github.com/MiniMax-AI/skills"><code>github.com/MiniMax-AI/skills</code></a>, <a href="https://github.com/ConardLi/garden-skills"><code>github.com/ConardLi/garden-skills</code></a>, <a href="https://github.com/microsoft/skills"><code>github.com/microsoft/skills</code></a>, <a href="https://github.com/finfin/awesome-frontend-skills"><code>github.com/finfin/awesome-frontend-skills</code></a>, <a href="https://github.com/colbymchenry/frontend-audit-skill"><code>github.com/colbymchenry/frontend-audit-skill</code></a> — all direct GitHub API checks</li>\n<li>Marketplace/SEO listing sites (<code>mcpservers.org</code>, <code>claudeskills.info</code>, <code>awesomeskill.ai</code>, <code>mcpmarket.com</code>) — surfaced in search, explicitly NOT treated as adoption-signal sources</content></li>\n</ul>\n';
