// Generated by scripts/build-library-content.mjs. Source: content/library/ai-tooling-crash-course-for-developers/topics/personal-harness-architecture.md
export const HTML =
  '<h1 id="stage-1-personal-harness-architecture-context-bundling-capability-lifecycle-task-graphs">Stage 1 — Personal Harness Architecture: Context Bundling, Capability Lifecycle, Task Graphs</h1>\n<p>Companion to <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> but different in kind: that doc checks vocabulary against industry terms. This one is build-oriented — four concrete architecture pieces for a personal <strong>agent harness</strong> (confirmed industry term: &quot;Agent = Model + Harness,&quot; the tool-access/memory/state/feedback-loop layer around a model). This repo brands that harness &quot;developer stack&quot; — that&#39;s identity, not a competing term; use &quot;harness&quot; in technical writing. No pick here, just patterns to consider.</p>\n<p>Surfaced from a ChatGPT conversation, reframed from Angular-specific advice to a project-agnostic personal harness. Three ideas the user flagged for follow-up, plus a fourth (async execution) from their own idea.</p>\n<hr>\n<h2 id="1-deterministic-context-bundling-before-model-calls">1. Deterministic context bundling before model calls</h2>\n<p><strong>The idea.</strong> Instead of the agent burning early turns on exploratory discovery (read this file, grep that, list this dir), a script generates a compact fact-bundle up front — project facts, architecture map, relevant files, constraints, commands, task state — and the model reasons over that.</p>\n<p><strong>Not just context engineering restated.</strong> <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> §3 already covers context engineering as a discipline. This is a specific <em>mechanism</em> for the front-loading half of it: a deterministic, scriptable step, not a prompting technique. Testable and versionable in a way &quot;write a good system prompt&quot; isn&#39;t — it&#39;s code with inputs/outputs, reviewable on its own.</p>\n<p><strong>Prior art.</strong> The user already has an informal version of this for their &quot;review utils&quot; and finds it works — that&#39;s the reference design, not a from-scratch build. (No details given here — don&#39;t invent them.)</p>\n<p><strong>What the bundle would hold, for this repo:</strong></p>\n<ul>\n<li>Which stage-1 docs exist + one-line scope</li>\n<li>What&#39;s already in <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a> (avoid re-verifying promoted items)</li>\n<li>Claims already checked and discarded (see that doc&#39;s &quot;Checked and discarded&quot; notes)</li>\n<li>Open items from <code>BACKLOG.md</code> / decision records</li>\n</ul>\n<p><strong>MCP vs. CLI script.</strong> Per <a href="/library/ai-tooling-crash-course-for-developers/topics/mcp-model-context-protocol"><code>mcp-model-context-protocol.md</code></a>: this is a CLI-script job, not an MCP server — deterministic, local, versioned with the repo.</p>\n<h3 id="the-concrete-design-a-context-compiler-tool-shelf">The concrete design: a &quot;context compiler&quot; / tool shelf</h3>\n<p><strong>Attribution correction.</strong> This design was attributed to Martin Fowler&#39;s &quot;Context Engineering for Coding Agents&quot; (Birgitta Böckeler, 5 Feb 2026 — real article). Fetched directly: <strong>attribution doesn&#39;t hold.</strong> That article covers context <em>configuration features</em> (CLAUDE.md, Rules, Skills, Subagents, MCP, Hooks, Plugins) and &quot;keep context small&quot; generally — no collector/normalizer/renderer pipeline, no structured subagent contracts. Treat the design below as a proposal from conversation, not a sourced pattern.</p>\n<p><strong>The pipeline:</strong></p>\n<pre><code>raw project data → collector → normalized result → renderer → task-specific bundle → agent/subagent\n</code></pre>\n<ul>\n<li><strong>Collector</strong> — reads a build log, runs <code>git diff</code>, queries a dependency graph.</li>\n<li><strong>Normalized result</strong> — JSON/JSONL/SQLite, not prose.</li>\n<li><strong>Renderer</strong> — turns that into concise Markdown/XML sized for the target agent.</li>\n<li><strong>Prompt</strong> — task + interpretation rules, not a manual dump of logs/source.</li>\n</ul>\n<p><strong>Proposed layout</strong> (starting point, not a spec):</p>\n<pre><code>ai-kit/\n├── contexts/&lt;situation&gt;/{context.yaml, collect.ts, render.md}\n├── tools/{repo-map, test-summary, build-summary, git-context, dependency-graph}\n└── prompts/*.md\n</code></pre>\n<p><strong>Worked example — build failure.</strong> Instead of 5,000 lines of raw output:</p>\n<pre><code class="language-json">{\n  &quot;command&quot;: &quot;npm run build&quot;, &quot;exitCode&quot;: 1, &quot;durationMs&quot;: 42100, &quot;status&quot;: &quot;failed&quot;,\n  &quot;errors&quot;: [{&quot;file&quot;: &quot;src/app/orders/orders.component.ts&quot;, &quot;line&quot;: 42, &quot;column&quot;: 9,\n              &quot;code&quot;: &quot;TS2339&quot;, &quot;message&quot;: &quot;Property &#39;total&#39; does not exist on type &#39;Order&#39;&quot;}],\n  &quot;warnings&quot;: 3,\n  &quot;affectedFiles&quot;: [&quot;src/app/orders/orders.component.ts&quot;, &quot;src/app/orders/order.service.ts&quot;],\n  &quot;rawLogPath&quot;: &quot;.ai/artifacts/build-2026-08-23.log&quot;\n}\n</code></pre>\n<p>Raw log stays on disk if a follow-up needs it. Applies to any noisy command: keep exit code, duration, errors, warnings, affected files, test counts, failed-test names, artifact path, short tail of raw output — not the full scrollback.</p>\n<p><strong>Use structured reporters, not scraping.</strong> <code>tsc --pretty false</code>, <code>jest --json</code>, <code>playwright test --reporter=json</code>, <code>eslint --format=json</code> — parse JSON where available, scrape text only when nothing else exists.</p>\n<p><strong>Contexts are task-shaped, not universal.</strong> <code>repo-orientation</code>, <code>implementation-context</code>, <code>build-failure</code>, <code>change-review</code>, <code>dependency-impact</code>, <code>test-failure</code>, <code>architecture-investigation</code> — each answers a different question, needs different inputs.</p>\n<p><strong>Subagents get different contracts, not copies of one bundle.</strong> A &quot;repo scout&quot; gets read-only search, no edits. An &quot;implementation agent&quot; gets target files, conventions, tests, acceptance criteria. A &quot;review agent&quot; gets the diff, spec, quality gates. Each returns a structured result — <code>{status, confidence, summary, evidence: [{file, line, reason}], recommendedNextAction, needsMoreContext}</code> — not a full transcript. This matches <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> §5&#39;s already-documented &quot;primary owns the task, specialists produce bounded findings&quot; pattern, already in live use in this project&#39;s own fork-dispatch-and-merge workflow. It&#39;s a refinement (typed result shape, per-role permissions), not a new discovery.</p>\n<p><strong>Manifest per context.</strong> A <code>context.yaml</code> (name, version, description, inputs, output schema, limits like <code>maxSourceExcerptLines</code>/<code>maxErrors</code>, activation mode) makes token cost measurable — logs context name, inputs, output bytes, estimated tokens, model, task, result. Shows which context builders are actually worth keeping.</p>\n<p><strong>Not Repomix.</strong> <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a> already lists <code>yamadashy/repomix</code> — a broad repo-packager, pack-everything-into-one-file. This design is the opposite: narrow, task-specific bundles. Different job.</p>\n<p><strong>Starter tool-shelf:</strong> <code>repo-map</code>, <code>git-diff-context</code>, <code>build-summary</code>, <code>test-summary</code>, <code>changed-symbols</code>, <code>dependency-impact</code>, <code>source-excerpt</code>, <code>project-context</code>, <code>task-state</code>. Skills/prompts become thin adapters over these.</p>\n<hr>\n<h2 id="2-capability-lifecycle-management">2. Capability lifecycle management</h2>\n<p><strong>The idea.</strong> Treat skills/tools as managed packages, not files that just exist or don&#39;t:</p>\n<pre><code>discover → review → trust → install → scope → activate → update → disable/remove\n</code></pre>\n<p>Metadata per capability: owner/source, version, trust level, supported agents/runtimes, project-or-global scope, activation conditions, dependencies, removal path.</p>\n<p><strong>Why it&#39;s a real gap.</strong> <a href="/library/ai-tooling-crash-course-for-developers/topics/skill-libraries-and-marketplaces"><code>skill-libraries-and-marketplaces.md</code></a> covers what a skill <em>is</em>; <a href="/library/ai-tooling-crash-course-for-developers/_ai-tooling-recommendations"><code>_ai-tooling-recommendations.md</code></a> has a growing candidate list. Neither covers what happens <em>after</em> a skill is picked — tracked as trusted, version bumps noticed, safe removal. This repo already lives the gap: <code>decisions/001-first-design-skill-picks.md</code> records three picks with no trust level, no version pin, no removal path. That&#39;s steps 1-2 (discover, informally review) done ad hoc — the rest isn&#39;t tracked.</p>\n<p><strong>Ties to security research.</strong> <a href="/library/ai-tooling-crash-course-for-developers/topics/security-and-supply-chain"><code>security-and-supply-chain.md</code></a> already covers why &quot;trust&quot; can&#39;t be assumed — the ClawHub/OpenClaw poisoning (real, 30+ malicious skills at peak) is exactly what a &quot;review → trust&quot; gate exists to catch.</p>\n<p><strong>Minimal version to try first:</strong> a single <code>skills/REGISTRY.md</code> (or small JSON/YAML) per installed capability — source URL, date added, trust note, version/commit pinned at install, scope (global vs. project). Not a package manager — just a trace of &quot;discover → review → trust&quot; so &quot;update → disable/remove&quot; has something to act on.</p>\n<hr>\n<h2 id="3-task-state-as-a-dependency-graph-not-a-flat-list">3. Task state as a dependency graph, not a flat list</h2>\n<p><strong>The idea and source.</strong> <a href="https://github.com/gastownhall/beads"><code>gastownhall/beads</code></a> (26,529 stars, 1,785 forks, pushed within the last day — real, active) replaces flat task lists with a dependency graph: <code>task A blocks task B and task C</code>, agent works the next unblocked item instead of one giant linear plan. Maintainer: Steve Yegge, also behind &quot;Gas Town&quot; (his org <code>gastownhall</code>) — an orchestration experiment the user was correctly advised not to prioritize. Beads is a smaller, more useful idea than Gas Town&#39;s scope — separate, lighter-weight, not tied to that &quot;don&#39;t start here&quot; verdict.</p>\n<p><strong>Beyond Topic #10&#39;s research.</strong> <a href="/library/ai-tooling-crash-course-for-developers/topics/memory-and-progress-ledgers"><code>memory-and-progress-ledgers.md</code></a> evaluates four ledger options (Claude Code memory, dated journals, Obsidian, JSONL extraction) — all linear/append-only, a record of what happened. None model dependencies between <em>not-yet-done</em> work, which is Beads&#39; whole point. A ledger answers &quot;what happened and why,&quot; a task graph answers &quot;what can I work on now.&quot; A real harness plausibly wants both.</p>\n<p><strong>Worth borrowing without adopting Beads.</strong> Minimal version at this repo&#39;s scale: a JSON or SQLite file with per-task status, dependencies, completion condition, files/decisions touched — small enough to hand-maintain. Same &quot;durable external state beats trusting the model to remember&quot; pattern already in <a href="/library/ai-tooling-crash-course-for-developers/topics/harness-engineering-vocabulary"><code>harness-engineering-vocabulary.md</code></a> §2 — the graph structure is what&#39;s new.</p>\n<hr>\n<h2 id="4-asynchronous-non-blocking-long-running-commands">4. Asynchronous, non-blocking long-running commands</h2>\n<p><strong>The gap.</strong> Confirmed via grep across <code>planning/*.md</code> — nothing covers this. Real, uncovered.</p>\n<p><strong>The idea.</strong> Running a test suite synchronously blocks the agent for the full duration, and most of the output is noise (only pass/fail + why matters). Proposal: launch detached, let the agent keep working or go idle, inject a concise result back on completion — event-driven, not polled, not blocked.</p>\n<p><strong>Already happening in Claude Code.</strong> Bash&#39;s <code>run_in_background</code> runs a command detached; on completion the harness delivers a <code>&lt;task-notification&gt;</code> on a later turn instead of blocking the tool call. <code>ScheduleWakeup</code> (check in later without polling) and <code>Monitor</code> (stream events from a background process) are the same family. Every fork in this whole project used exactly this pattern.</p>\n<p><strong>Correction — not yet solved cross-vendor.</strong> The user&#39;s own corporate-environment observation: an agent running tests via Copilot or Gemini just blocks and waits, even though Gemini CLI&#39;s interface does expose background-process management (see <a href="/library/ai-tooling-crash-course-for-developers/topics/session-and-token-economics"><code>session-and-token-economics.md</code></a>). The primitive exists but isn&#39;t used — likely an instruction/skill gap (never told to prefer async), not proof the capability is Claude-only. Copilot&#39;s async story specifically is untested.</p>\n<p><strong>Design implication:</strong> don&#39;t build this as &quot;wrap Claude Code&#39;s <code>run_in_background</code>&quot; — that&#39;s single-vendor, not portable. Needed instead: (1) confirm what each target agent (Claude Code, Copilot CLI, Gemini CLI) actually exposes; (2) where a primitive exists, write the instruction telling the agent to prefer it; (3) where none exists or is confirmed, build a fallback — a detached-process wrapper plus a hook/poll outside the agent&#39;s own tool schema. Goal: a harness-level capability, not a vendor dependency.</p>\n<p><strong>Connects to:</strong></p>\n<ul>\n<li>Structured reporters (section 1) — the right shape for what gets injected on completion: a <code>test-summary</code> JSON object, not raw scrollback.</li>\n<li>Hooks (settled category per <a href="/library/ai-tooling-crash-course-for-developers/README"><code>topic-index.md</code></a>) — a plausible trigger point for generating that summary the moment a background process exits.</li>\n</ul>\n<p><strong>Practical shape:</strong> an async-run wrapper for long commands that (a) launches detached, (b) pipes through a structured summarizer where available, (c) writes the raw log to disk regardless, (d) delivers a short structured result when done. Buildable now — the user already has proof-of-concept exposure via this session&#39;s own tooling.</p>\n<hr>\n<h2 id="how-these-four-relate">How these four relate</h2>\n<p>Not independent: context bundle (#1) reads from the task graph (#3) for current state, and from the capability registry (#2) for what&#39;s active. None needs to exist first — each is useful alone — but build order, if all three happen: registry (cheapest) → task graph (next cheapest) → context bundling (pulls from both). Async execution (#4) is orthogonal — a delivery mechanism for #1&#39;s structured results, not a dependency of the other three.</p>\n<h2 id="open-questions-weak-sourcing">Open questions / weak sourcing</h2>\n<ul>\n<li>User&#39;s &quot;review utils&quot; prior art — reference it directly when building #1, not this doc.</li>\n<li>Martin Fowler citation for #1&#39;s pipeline design doesn&#39;t hold up (see above) — the subagent-contract part stands on its own verified basis; the rest (directory shape, manifest format, build-summary example) is an unsourced proposal. Treat as &quot;worth trying,&quot; not &quot;how the field does it.&quot;</li>\n<li>Beads&#39; star count (26,529) will drift — re-check before citing elsewhere.</li>\n<li>No existing tool found for #2 — it&#39;s a proposed pattern, not a survey. #1 and #3 point at real prior art; #2 is closer to original design.</li>\n<li>#4 has no tool survey — a gap-naming note, not a literature review.</li>\n</ul>\n</content>\n';
